# codex-lb feature map → PolyFlare

> Exhaustive feature inventory of codex-lb, enumerated from the local source at `/Users/wmaididhaiqal/Development/Codex-LoadBalancer/codex-lb` and
> mapped to PolyFlare as **built / partial / absent**. codex-lb is PolyFlare's Python predecessor (deep single-provider Codex pool + impersonator), so it is the closest 1:1 reference for what PolyFlare should eventually cover.

**Coverage: ✅ 98 built · 🟡 60 partial · ❌ 202 absent — of 360 features (27% built, 44% built-or-partial).**

**Legend:** ✅ built + wired in PolyFlare · 🟡 partial (scaffolded, inputs inert, or a subset) · ❌ not yet.

> This is a *complete* inventory (features PolyFlare already has are listed too), one level finer than
> [`COMPARISON.md`](COMPARISON.md). For the routing gaps, the porting recipes are in
> [`PORTING-CODEXLB.md`](PORTING-CODEXLB.md).


## routing  ·  ✅ 27 🟡 13 ❌ 16

| Feature | PF | PolyFlare status / where | Source |
|---|:--:|---|---|
| capacity_weighted strategy | ✅ | Weighted-random pick with probability proportional to each account's remaining secondary (weekly) credits. — crates/polyflare-core/src/select.rs CapacityWeighted + weighted_pick/remaining_secondary_credits; the default strategy, faithful port. | `app/core/balancer/logic.py:610-620,943-950` |
| usage_weighted strategy | 🟡 | Deterministic least-used pick, ranking by secondary-then-primary used% with last_selected + id tiebreaks (secondary_first/primary_first orders). — select.rs UsageWeighted keys only on eff_secondary_used then id; missing secondary_first/primary_first ordering, primary_used + last_selected tiebreaks, and the primary_first budget fallback. | `app/core/balancer/logic.py:637-654,167-178` |
| round_robin strategy | 🟡 | Deterministic pick of the least-recently-selected account (min last_selected_at), id tiebreak. — select.rs RoundRobin reads last_selected_at but nothing ever writes it (always None), so it degenerates to the id tiebreak — documented in code. | `app/core/balancer/logic.py:582-584,608-609` |
| fill_first strategy | 🟡 | Deterministic pick of the most-saturated eligible account (highest primary used%, then higher secondary) to saturate the warmest account for cache locality. — select.rs FillFirst keys on warmth=max(used,secondary) then id; codex-lb keys (-primary,-secondary,id) and supports prefer_earlier_reset — no reset preference in PF. | `app/core/balancer/logic.py:630-636,953-976` |
| sequential_drain strategy | ✅ | Deterministic drain of the smallest-capacity (throwaway) account first, stable sha256 tiebreak. — select.rs SequentialDrain by capacity ascending; tiebreak is plain account_id, not codex-lb's sha256 _stable_tie_breaker. | `app/core/balancer/logic.py:590-592,879-884` |
| reset_drain strategy | ❌ | Deterministic pick by earliest weekly-reset-day bucket, then most remaining primary/secondary. — Absent; RoutingStrategy enum in select.rs has no reset_drain; no weekly-reset-bucket sort key. | `app/core/balancer/logic.py:594-596,895-915` |
| single_account strategy | ❌ | Deterministic pin to the lowest account_id (effectively one account). — Absent from RoutingStrategy enum; the closest analog is a named pool containing a single account. | `app/core/balancer/logic.py:586-588` |
| relative_availability strategy | ❌ | Weighted pick by score = remaining_credits / seconds_until_reset, normalized+exponentiated (power), retaining top_k candidates. — Absent; no credits/seconds-to-reset scoring, no power exponent, no top_k retention, no min-divisor/min-weight-fraction constants. | `app/core/balancer/logic.py:621-629,695-798,822-866` |
| config-selectable strategy factory | ✅ | routing_strategy Literal dispatched inside select_account so operators pick a strategy by name. — select.rs RoutingStrategy::{parse,name,all,selector} (ZST selectors) wired via POLYFLARE_ROUTING_STRATEGY (config.rs). | `app/core/balancer/logic.py:59-68,586-655` |
| per-account routing_policy (normal/burn_first/preserve) | ✅ | Each account carries a burn/normal/preserve policy that shapes drain ordering. — accounts.routing_policy column (migration 0001) → AccountSnapshot.routing_policy → select.rs policy_waterfall. | `app/core/balancer/logic.py:93-95,181-188` |
| burn/normal/preserve policy waterfall | ✅ | Effective pool = burn_first accounts if any, else normal, else preserve — drains expendable capacity before touching preserved. — select.rs policy_waterfall iterates [burn_first, normal, preserve] returning the first non-empty group. | `app/core/balancer/logic.py:602-605` |
| budget-safe preferred pool | ❌ | Pre-filters accounts over budget_threshold_pct / secondary_budget_threshold_pct before applying the strategy, falling back only if none are budget-safe. — Absent; no budget-threshold pre-filter (_state_above_budget_threshold has no analog); select.rs picks straight from the waterfall. | `app/modules/proxy/load_balancer.py:2645-2762,2618-2642` |
| burn_first best-health preference | 🟡 | Within the budget-safe selector, burn_first accounts are tried first from the best health tier with backoff-fallback disabled. — select.rs waterfall drains burn_first first, but there is no _best_health_tier_states pre-stage nor a backoff-disabled burn_first attempt. | `app/modules/proxy/load_balancer.py:2694-2711,2765-2773` |
| preserve floor math (weekly-pace + short-window) | ❌ | Preserve accounts allow opportunistic burn only above dynamic weekly-pace and short-window floors (tightened by recent foreground activity / low preserve count). — Absent; PF preserve is only a waterfall tier — no _weekly_pace_floor_pct/_short_window_floor_pct computation. | `app/core/balancer/logic.py:214-262` |
| opportunistic traffic-class filter/admission | ❌ | foreground vs opportunistic traffic; opportunistic requests may only use expendable capacity via _filter_opportunistic_candidates + check_opportunistic_admission. — Absent; SelectionCtx has no TrafficClass; no opportunistic burn-window gate. | `app/core/balancer/logic.py:295-321,485-489` |
| emergency-floor expendability check | ❌ | burn_first/normal accounts are only opportunistically burnable if other usable foreground capacity exists OR they retain a 5% emergency reserve. — Absent; no _has_other_usable_foreground_capacity / _above_emergency_floor logic. | `app/core/balancer/logic.py:265-293` |
| routing_costs quota-planner tiebreak | ❌ | Request-scoped planner costs are applied after hard eligibility; the lowest-cost candidates win before weighting/tiebreak. — Absent; no quota-planner integration; SelectionCtx carries no routing_costs and _lowest_planner_cost_candidates has no analog. | `app/core/balancer/logic.py:158-164,933-940` |
| health-tier tri-state pooling | ✅ | Selection prefers healthy(0), else probing(2), else draining(1) accounts before applying policy/strategy. — select.rs health_tier_pool iterates [0,2,1] returning the first non-empty tier. | `app/core/balancer/logic.py:598-601` |
| evaluate_health_tier soft-drain state machine | ✅ | RuntimeStates implements HEALTHY→DRAINING→PROBING→HEALTHY from usage and error signals, including drain_entered_at quiet time and probe-success streak; the live tier is overlaid before every selection. | `app/core/balancer/logic.py:1099-1157,app/modules/proxy/load_balancer.py:2176-2210` |
| configurable drain thresholds + soft_drain toggle | 🟡 | Drain primary/secondary %, error window/count, probe quiet/streak are all settings; soft_drain_enabled can disable the whole state machine. — select.rs hardcodes 85/90/60s; codex-lb reads drain_*_threshold settings and a soft_drain_enabled toggle — neither is configurable in PF. | `app/core/balancer/logic.py:87-92,app/modules/proxy/load_balancer.py:2177` |
| cooldown_until eligibility gate + clear-on-expiry | ✅ | Accounts inside durable/runtime cooldown are skipped; elapsed runtime cooldown is dropped before selection so stale timestamps cannot repeatedly erase newer error state. | `app/core/balancer/logic.py:464-469` |
| rate-limit (429) cooldown writeback | ✅ | The shared failure funnel records Retry-After or exponential cooldown with a 30s floor, persists restart-safe routing cooldown, increments error state, and requests an immediate coalesced usage refresh. | `app/core/balancer/logic.py:991-1021,979-988` |
| quota-exceeded cooldown writeback | ✅ | Capacity error codes record quota cooldown without double-counting a health error and trigger an immediate authoritative usage refresh; durable usage polling owns the final quota/reset state. | `app/core/balancer/logic.py:1024,1043-1053` |
| error_count / last_error_at increment on failure | ✅ | RuntimeStates records transient failures and timestamps, feeds exponential backoff/health-tier evaluation, and overlays the live values before selection. | `app/modules/proxy/load_balancer.py:991-995` |
| error-count>=3 exponential backoff gate + clean-slate reset | ✅ | Once error_count>=3, account is held for min(300, 30*2^(n-3))s from last_error_at; past the window its error state is wiped so recovery isn't penalized. — select.rs error_backoff_secs + gate :180-188 (zeros eff error state past window) — correct but inert because error_count is never set. | `app/core/balancer/logic.py:470-483` |
| record_success error-state clear | ✅ | Completed HTTP and WebSocket turns clear transient error state and advance probe success; request-level failed/incomplete terminals never masquerade as success. | `app/modules/proxy/load_balancer.py:1676-1707` |
| failover_decision failure classifier | ✅ | failover_verdict separates retryable transport/rate/quota/transient failures from terminal request failures and refuses replay after downstream commit. | `app/core/balancer/logic.py:1071-1086` |
| cross-account retry loop (bounded, excluded set, committed barrier) | ✅ | The failover loop tracks tried accounts, reselects atomically under admission caps, shares the aggregate logical-turn attempt budget, and surfaces once any downstream byte commits. | `app/modules/proxy/_service/streaming/retry.py:218-1389` |
| exclude_account_ids from selection | ✅ | The failover loop filters every tried account from each fresh candidate snapshot before reselection. | `app/modules/proxy/load_balancer.py:336,347,386-412` |
| selection CAS-retry on stale persist | 🟡 | After selecting, persist status via compare-and-set; on a stale/lost race or a cache-generation bump, release the lease and re-select up to _MAX_SELECTION_ATTEMPTS. — PF has account_cache.rs generation atomics but no per-account CAS persist + re-pick loop during selection (durable status is written by usage_refresh, not the select path). | `app/modules/proxy/load_balancer.py:556-620,1676-1707` |
| anti-starvation backoff-fallback | ✅ | Layer 1 serves the soonest error-backoff account only under the >1 or 1+hardblocked guard; Layer 2 performs bounded keepalive waiting for cooldown recovery with jitter and content-safe outcomes. | `app/core/balancer/logic.py:491-548` |
| retry-hint wait formatting | 🟡 | PolyFlare performs bounded SSE/Anthropic keepalive waits and returns fixed reason codes on exhaustion; it does not expose a textual `Try again in Ns` header/body hint. | `app/core/balancer/logic.py:1026-1040,539-547` |
| sticky session kinds (hard/soft, prompt-cache) | 🟡 | CODEX_SESSION (hard pin), PROMPT_CACHE + STICKY_THREAD (soft) map a session/thread key to an owning account for warm-cache continuity. — PF has a content-free Codex ownership pin (server/continuity.rs via anchor + session_key = hard pin only); no PROMPT_CACHE/STICKY_THREAD kinds and no general prompt_cache_key stickiness (COMPARISON Routing row). | `app/modules/proxy/load_balancer.py:1265-1450,app/modules/sticky_sessions/service.py` |
| sticky temporary-failover-without-overwrite (snap-back) | 🟡 | When a pinned account is temporarily unavailable, pick a fallback WITHOUT overwriting the mapping, so traffic snaps back to the warm account on recovery (only reallocate_sticky opts into permanent reassignment). — ingress.rs Recover path picks a fresh account without deleting the anchor/owner pin so it snaps back; but there is no reallocate_sticky opt-in for permanent reassignment. | `app/modules/proxy/load_balancer.py:1311-1343` |
| sticky budget-pressure rebind | ❌ | A pinned session is proactively rebound once the owner crosses the primary/secondary sticky budget threshold, avoiding edge-of-exhaustion failures. — Absent; no _state_above_sticky_budget_threshold analog and no budget-driven rebind of a pinned session. | `app/modules/proxy/load_balancer.py:2623-2642,1329-1343` |
| sticky-session store + cleanup scheduler + TTL | 🟡 | Persisted sticky mappings with a configurable max-age TTL and a background cleanup/purge scheduler. — PF has continuity_sessions/continuity_anchors tables (migration 0002) but no TTL-based pruning or cleanup scheduler — sticky/continuity rows grow unbounded (COMPARISON retention row). | `app/modules/sticky_sessions/service.py,app/modules/sticky_sessions/cleanup_scheduler.py` |
| rendezvous (HRW) hashing | ❌ | Highest-random-weight hashing to map a key to a node so adding/removing one node remaps only 1/N keys. — Absent; select_node has no analog — PF is a single binary with no bridge-ring/node hashing. | `app/core/balancer/rendezvous_hash.py:7-21` |
| account lease acquire/release (response_create + stream) | ✅ | RAII InFlightGuard spans each HTTP attempt/stream and every generating WebSocket turn; drop releases count and weighted pressure on completion, error, cancellation, or teardown. Idle sockets have a separate guard. | `app/modules/proxy/load_balancer.py:195-318` |
| per-account concurrency cap enforcement | ✅ | Atomic selection enforces configurable global/per-account request, weighted-pressure, and open-WebSocket caps while reserving count and pressure capacity for pinned-owner recovery. | `app/modules/proxy/load_balancer.py:1767-1788,265-279` |
| stale lease reclaim TTL | ✅ | Not needed for the single-process RAII design: task cancellation and stream/socket teardown drop guards, while a process crash destroys the entire in-memory map. A future shared coordinator would require TTL/fencing. | `app/modules/proxy/load_balancer.py:306-318,1754-1764` |
| leased-token + in-flight capacity pressure penalty | ✅ | Live weighted pressure is derived from bounded request size, calibrated from terminal compute-equivalent usage, hard-capped during admission, and folded into the selector's effective-used penalty. | `app/modules/proxy/load_balancer.py:2212-2225` |
| replica cap partitioning | ❌ | Cluster-wide per-account caps are split deterministically per replica (floor(cap/R)+remainder) with share-growth hysteresis. — Absent; partition_cap/CapPartitionHolder have no analog — PF is single-binary by target (COMPARISON coordination row). | `app/modules/proxy/cap_partitioning.py:44-140` |
| last_selected_at write on selection | ✅ | Atomic request/socket selection and exact-owner acquisition stamp live last_selected_at; the runtime overlay supplies it to round-robin and deterministic tiebreaks. | `app/modules/proxy/load_balancer.py:259,1626-1628` |
| gated/additional-quota priority windows | ❌ | priority_used_percent / priority_secondary_used_percent / priority_reset_at / priority_capacity_credits give models gated by a separate quota pool their own routing signals. — Absent; AccountSnapshot has only primary + secondary used% — no priority_* fields and no separate quota pool. | `app/core/balancer/logic.py:129-133,672-687` |
| additional-model gated routing + eligibility | ❌ | For gated models, narrow candidates to accounts with fresh, available additional quota, surfacing data_unavailable/exhausted/no-eligible reasons. — Absent; PF narrows only by provider + pool + synthetic-model alias — no additional_limit_name gating and no ignore_standard_quota/bypass path. | `app/modules/proxy/load_balancer.py:1110-1225,876-879` |
| limit_scoped_usage handling | ❌ | For limit-scoped accounts with no distinct secondary sample, primary usage stands in for the secondary window in scoring + budget checks. — Absent; no limit_scoped_usage flag; PF always uses the resolved weekly window for the secondary signal. | `app/core/balancer/logic.py:133,677-687` |
| prefer_earlier_reset + reset-window bucketing | ❌ | Optionally bias selection toward accounts whose primary/secondary window resets soonest, via day/second-granular reset buckets. — Absent; SelectionCtx has no prefer_earlier_reset; no _reset_preference_bucket / _prefer_earlier_reset_candidates analog. | `app/core/balancer/logic.py:324-347,362-364` |
| terminal/paused hard-exclude + rate/quota reset-recovery | ✅ | reauth_required/deactivated/paused are never eligible; rate_limited/quota_exceeded recover only once reset_at passes, zeroing the right usage windows. — select.rs eligibility :128-164 excludes terminal/paused and recovers rate_limited (zero primary+error) / quota_exceeded (zero primary+secondary) on reset — faithful to codex-lb. | `app/core/balancer/logic.py:444-463` |
| security_work_authorized pre-filter | ✅ | A require_security_work_authorized request is restricted to accounts flagged for security work. — select.rs standard_pool filters on ctx.require_security_work_authorized + snapshot.security_work_authorized (accounts.security_work_authorized column, migration 0001). | `app/modules/proxy/load_balancer.py:357-371` |
| model→plan candidate filtering | 🟡 | Candidates are narrowed to accounts whose plan supports the requested model (else no_plan_support_for_model). — PF narrows by provider + pool + synthetic-model alias→provider (snapshot.rs filter_by_provider_and_pool) but has no plan_types_for_model per-account narrowing. | `app/modules/proxy/load_balancer.py:885-927` |
| all-accounts circuit-breaker → degraded mode + degraded error | ❌ | When every account's circuit breaker is open the pool enters degraded mode and the empty-pool error is rewritten with the degradation reason. — Absent; no circuit breaker and no degradation status — PF returns a plain 503 with no degraded-mode reason. | `app/modules/proxy/load_balancer.py:417-419,815-818,2776-2787` |
| selection-inputs cache + generation invalidation | ✅ | Accounts + latest usage rows are cached off the hot path and rebuilt when a generation counter bumps, avoiding a DB read per request. — PF account_cache.rs holds the assembled AccountSnapshot list with a generation bumped on mutation; usage_refresh warms it off the request path. | `app/modules/proxy/load_balancer.py:193,543,579,828-854` |
| plan capacity table + alias normalization | ✅ | Per-plan secondary credit capacities (free/plus/pro/…) with a plan-alias map (education→edu, guest/go→free, unknown→free) after trim/lowercase. — select.rs plan_capacity_secondary + plan_alias (free 1134 / plus 7560 / pro 50400 …), UNKNOWN_PLAN_FALLBACK=free. | `app/core/balancer/logic.py:73-81,350-356` |
| per-account capacity_credits override | 🟡 | An account can override its plan-derived secondary capacity used as the routing weight base. — AccountSnapshot.capacity_credits field exists and select.rs honors it, but assemble_snapshots never sets it (always None → plan-derived); no per-account capacity column. | `app/core/balancer/logic.py:658-669,873-876` |
| remaining_secondary_credits weight + seeded deterministic pick | ✅ | The capacity weight is max(0, capacity*(1-secondary_used/100)); the weighted draw is seedable and falls back to a deterministic usage-sort when all weights are zero. — select.rs remaining_secondary_credits + sample_weighted (seeded StdRng via ctx.rng_seed) + deterministic_min fallback; note ingress passes rng_seed:None so prod is non-deterministic (deterministic under a test seed). | `app/core/balancer/logic.py:658-669,943-950` |
| permanent/auth-failure status writeback | 🟡 | Auth error codes map to reauth_required (refresh/token/session codes) or deactivated (suspended/deleted), setting deactivation_reason and leaving cooldown null. — PF classifies the same permanent-code table on the OAuth refresh path (crates/polyflare-codex oauth) and sets reauth_required/deactivated, but NOT from proxy request failures; select.rs hard-excludes those statuses. | `app/core/balancer/logic.py:1056-1068,16-48` |

## continuity  ·  ✅ 18 🟡 5 ❌ 37

| Feature | PF | PolyFlare status / where | Source |
|---|:--:|---|---|
| store:false always forced | ✅ | A field_validator hard-forces store=false on every Responses request, so no continuity ever relies on durable upstream retrieval. — PF passes the client's store:false through verbatim on the native path; the translated Codex body is built store:false. No PF code sends store:true. Matches by construction. | `app/core/openai/requests.py:665-668 (+ v1_requests.py:47-50)` |
| previous_response_id normalization | 🟡 | Validator strips whitespace and coerces empty previous_response_id to None before use. — PF extracts previous_response_id via raw_as_str (session_key.rs:182) but does NOT trim/empty-normalize it; a whitespace-only anchor would be carried as-is. | `app/core/openai/requests.py:670-676` |
| conversation vs previous_response_id mutual-exclusion | ❌ | Model validator rejects a request that sets both conversation and previous_response_id. — PF is byte-passthrough on native and never validates this pairing; it would forward both upstream and let OpenAI reject. | `app/core/openai/requests.py:696-699` |
| Anchor injection site A (durable fresh-reattach) | ❌ | When a durable row exists but no live/remote owner and the client sent no anchor, injects durable_lookup.latest_response_id (the wedge path). — DELIBERATELY absent. PF's R1 rule never injects a bookkeeping anchor; the full-resend is always sent whole. This is the exact site PF exists to remove. | `app/modules/proxy/_service/http_bridge/streaming.py:798-852` |
| Anchor injection site B (session-level) | ❌ | For a live in-memory bridge session that completed a turn, injects the session's last_completed_response_id so the trim can strip the stored prefix. — Absent by design. PF has no in-memory bridge session and never re-imposes a proxy anchor; owner routing + full pass-through replace it. | `app/modules/proxy/_service/http_bridge/streaming.py:1331-1393` |
| Anchor injection site C (native WebSocket) | ❌ | Parallel _WebSocketContinuityState anchor injection on the native-WS surface, with no account-owner field. — PF has no WebSocket transport (HTTP-SSE only), so this surface doesn't exist. COMPARISON: WS transport ❌. | `app/modules/proxy/_service/websocket/helpers.py:409-432` |
| Store-context prefix trim | ❌ | When an anchor is present and the incoming input's first stored_count items fingerprint-match, sends only input[stored_count:] + anchor. — DELIBERATELY absent (R1). PF never trims; on the recovery path it strips the anchor and sends the FULL input instead (RecoveryPlan::ResendFull, continuity.rs:105-137). | `app/modules/proxy/_service/http_bridge/streaming.py:1394-1434` |
| Prefix-fingerprint match gate | 🟡 | _input_prefix_matches_stored_context compares the incoming prefix fingerprint to stored bookkeeping to decide whether a trim/injection is safe. — PF computes an input fingerprint (watchdog.rs input_fingerprint_and_count) and persists last_input_fingerprint, but uses it for diagnostics ONLY — never to gate a trim (there is no trim). | `app/modules/proxy/_service/http_bridge/streaming.py:810,1352-1356` |
| Previous-response output-items prefix trim | ❌ | _trim_http_bridge_previous_response_input_items drops leading already-stored assistant/reasoning/tool output items when an anchor carries them. — Absent. PF forwards the client's input array untouched; it does no item-level prefix surgery. | `app/modules/proxy/_service/http_bridge/helpers.py:460-495` |
| Full-resend heuristic | ✅ | Classifies a payload as a full-resend when input is a multi-item array, a single ≥4096-serialized item, or a ≥4096-char string. — Ported faithfully in session_key.rs input_shape (≥2 items, or 1 item canonicalizing to ≥4096 code points, or string ≥4096 code points); code-point vs byte fidelity is unit-tested. | `app/modules/proxy/_service/http_bridge/helpers.py:849-861` |
| Anchor→owner resolution (previous_response_id alias) | ✅ | A client previous_response_id resolves to the owning account via the durable alias / latest_response_id lookup. — PF's continuity_anchors table maps response_id→owning_account_id; get_anchor_owner resolves it and it's the authoritative pin source in prepare (continuity.rs:60-69). | `app/modules/proxy/durable_bridge_coordinator.py:65-94` |
| Durable bridge session record (owner_instance/epoch/lease) | ❌ | HttpBridgeSessionRecord persists owner_instance_id, owner_epoch, and lease_expires_at for cross-replica ownership. — PF is single-binary in-process by design; no instance id / epoch / lease. Ownership is a content-free response_id→account map, not a leased cross-replica record. COMPARISON: durable bridge ownership ❌. | `app/modules/proxy/durable_bridge_repository.py:40-47 (+ db/models.py:1436-1496)` |
| claim_session (epoch advance + takeover) | ❌ | Atomically claims/takes over a session, advancing owner_epoch on takeover or account change, guarded by lease expiry. — No lease-claim primitive. PF resolves owner by lookup only; concurrency is handled by in-process ordering, not an epoch-guarded DB claim. | `app/modules/proxy/durable_bridge_repository.py:156-234` |
| renew_session (lease heartbeat) | ❌ | Extends lease_expires_at and updates latest_response_id/turn_state/fingerprint under owner_epoch guard. — Absent — no lease to renew. PF records completion state (record_completion) but there is no TTL heartbeat. | `app/modules/proxy/durable_bridge_repository.py:267-305` |
| release_session (epoch-guarded release) | ❌ | Releases ownership (clears/expires the lease) only if instance_id + owner_epoch still match. — No epoch-guarded release; PF ownership is not leased so it is never released, only re-homed on recovery. | `app/modules/proxy/durable_bridge_repository.py:309-333` |
| mark_owner_draining | ❌ | On graceful shutdown, marks all of an instance's owned sessions draining so peers may take over. — Absent — single binary has no peer to hand off to; process exit ends all in-flight work. | `app/modules/proxy/durable_bridge_coordinator.py:210-212` |
| lease_is_active check | ❌ | Determines if a durable owner's lease is still live (owner set + lease_expires_at in the future). — No lease concept. PF's nearest equivalent is 'is the pinned owner account currently eligible' (apply_ownership → selector.pick), decided at request time, not by a TTL. | `app/modules/proxy/durable_bridge_coordinator.py:40-45` |
| force_owner_epoch_advance on account change | ❌ | Bumps owner_epoch when the bound account changes so stale in-flight writers are fenced off. — Absent (no epoch). PF re-homes ownership atomically in record_recovery instead of fencing via an epoch. | `app/modules/proxy/durable_bridge_repository.py:226` |
| Owner-forwarding to active remote owner | ❌ | When another replica owns the live session, forwards the HTTP request cross-instance to that owner endpoint. — Absent — no multi-instance topology. _http_bridge_can_forward_to_active_owner has no PF analog. | `app/modules/proxy/_service/http_bridge/owner_forwarding.py:251-435` |
| has-live-local-session gate | ❌ | Checks whether this instance already holds a live local bridge for the session key before reattaching. — Absent — PF holds no in-memory bridge; every request re-derives session state from the DB row. | `app/modules/proxy/_service/http_bridge/owner_forwarding.py:168` |
| Poisoned local-compact fallback stripping | ❌ | Detects and discards a client's fake 'Local compact fallback preserved the latest encrypted reasoning state.' placeholder item. — Absent. Confirms codex-lb has no server-side reasoning reconstruction; PF likewise has none but also doesn't strip the placeholder (would forward it). | `app/core/openai/requests.py:793-856` |
| Interleaved-reasoning input sanitizer | ❌ | Strips reasoning echo fields (reasoning_content/reasoning_details/encrypted_content) from assistant input items the anchor is assumed to carry. — Absent — PF neither strips nor caches reasoning; it passes the input through unmodified. | `app/core/openai/requests.py:424-455` |
| reasoning alias normalization | ❌ | Normalizes reasoningEffort/reasoningSummary/thinking aliases into a canonical reasoning.{effort,summary} object. — PF reads reasoning.effort directly (session_key.rs effort_from_reasoning) and the translated path emits reasoning.effort, but there is no camelCase/thinking-alias normalizer. | `app/core/openai/requests.py:1298-1326` |
| Downstream duplicate tool-call marking | ❌ | Marks repeated tool-call output items (by dedupe key) as duplicates in the streamed response so the client doesn't double-execute. — Absent. PF streams upstream bytes through unchanged (sniffing only the response.id); no tool-call dedupe. COMPARISON: tool-call dedupe ❌. | `app/modules/proxy/tool_call_dedupe.py:95-160` |
| Side-effect tool-call taxonomy | ❌ | Frozensets classify which tool names/types are side-effecting (apply_patch, exec_command, spawn_agent, …) to drive dedupe/replay safety. — Absent — PF has no notion of tool-call side-effect classes. | `app/modules/proxy/tool_call_dedupe.py:17-58` |
| ToolCallDedupeKey composite key | ❌ | A 6-tuple (response/item/call-id/name/type/args-hash) identity used to detect a replayed tool call. — Absent. | `app/modules/proxy/tool_call_dedupe.py:60` |
| Tool-call dedupe cache bound | ❌ | Caps the seen-tool-call key cache at 1024 entries to bound memory. — Absent (no dedupe cache). PF's only bounded buffer here is the 64 KiB response-id sniffer. | `app/modules/proxy/tool_call_dedupe.py:15` |
| Interrupted tool-output injection on reattach | ❌ | On reattach after an interrupted turn, fabricates the missing tool_call_output items for last_pending_tool_calls so upstream isn't left waiting. — Absent. PF's recovery is coarse-grained (resend full or signal client); it does not reconstruct partial tool state. | `app/modules/proxy/_service/http_bridge/streaming.py:307-350` |
| Pending tool-call tracking on session | ❌ | Bridge session records last_pending_tool_calls (call_id→type) captured from the terminal request state. — Absent — PF persists no per-turn tool-call state (content-free continuity only). | `app/modules/proxy/_service/support.py:526 (set at upstream_events.py:796)` |
| WS self-contained fresh-replay detection | ❌ | Detects when a WS input array is a self-contained fresh replay (all tool calls have matching outputs) so it can drop the anchor safely. — Absent — no WS path; PF's full-resend detection is coarser (item-count / size heuristic). | `app/modules/proxy/_service/websocket/helpers.py:443` |
| /responses/compact endpoint | ❌ | A dedicated compact endpoint that summarizes/continues a conversation, resolving an owner and handling continuity. — Absent — PF exposes no compact route. COMPARISON lists codex-lb's 809-LOC dedupe + /responses/compact as unported. | `app/modules/proxy/_service/compact.py:524` |
| Compact previous_response_not_found recovery | ❌ | On compact, detects an upstream previous_response_not_found and recovers rather than failing. — Absent (no compact path). | `app/modules/proxy/_service/compact.py:306,973-983` |
| Compact turn-state owner resolution | ❌ | Resolves the owning account of a compact request from its turn-state/previous-response anchor. — Absent (no compact path). PF's owner resolution exists only on the main /responses path. | `app/modules/proxy/_service/compact.py:442` |
| ResponsesCompactRequest schema + field stripping | ❌ | A distinct request model plus compact-specific unsupported-field stripping. — Absent — PF has no compact schema. | `app/core/openai/requests.py:897 (_strip_compact_unsupported_fields)` |
| In-process HTTP bridge session | ❌ | A persistent upstream WS session per bridge multiplexing turns, holding continuity + tool state in memory. — Absent by architecture — PF is stateless HTTP-SSE pass-through; continuity lives in SQLite, not an in-memory bridge object. | `app/modules/proxy/_service/support.py:490-539 (_HTTPBridgeSession)` |
| response_create_gate semaphore | ❌ | A per-bridge Semaphore(1) serializing response.create; a wedged holder blocks queued turns (the wedge amplifier). — No bridge gate exists to wedge. PF's structural analog is the per-request silence watchdog: a stalled anchored request is cancelled at N seconds instead of holding a shared gate. | `app/modules/proxy/_service/support.py:504` |
| In-process anchor state (last_completed_*) | ✅ | Bridge session tracks last_completed_response_id / _input_count / _input_prefix_fingerprint in memory. — PF persists the equivalent DURABLY in continuity_sessions (anchor_response_id, last_input_count, last_input_fingerprint) via record_completion — cross-process, not in-memory. | `app/modules/proxy/_service/support.py:523-525` |
| Durable session id/epoch on bridge | ❌ | Bridge session caches its durable_session_id + durable_owner_epoch for lease renewal. — Absent (no lease). PF's session identity is the derived session_key; no epoch is tracked. | `app/modules/proxy/_service/support.py:527-528` |
| Multi-alias durable reattach lookup | 🟡 | lookup_request_targets resolves a session by trying turn_state, then previous_response_id, then session_header aliases, then canonical key, then latest-* fallbacks, all api-key-scoped. — PF resolves owner by (client previous_response_id → continuity_anchors) then (session_key → continuity_sessions) — two of the five alias paths. No turn_state/session_header alias table and no api-key scoping. | `app/modules/proxy/durable_bridge_coordinator.py:52-97` |
| Reattaching state marking | ✅ | An in-flight reattach transitions the session to a 'reattaching' state while the anchored turn is outstanding. — PF sets state='reattaching' on any anchor-bearing prepare via a single-write ensure_session_reattaching UPSERT (continuity_repo.rs:87-110); part of its explicit Fresh→Anchored→Reattaching→Recover machine. | `app/modules/proxy/_service/http_bridge/streaming.py (durable state transitions)` |
| Model-transition isolation | ❌ | Drops the durable lookup when the incoming model is incompatible with the stored model, so an anchor from a different model class isn't reused. — Absent — PF's ownership pin is content/model-agnostic; it does not invalidate an anchor on a model switch. | `app/modules/proxy/_service/http_bridge/streaming.py:781-797` |
| The store:false anchor wedge (defect) | ✅ | A dead ephemeral anchor makes upstream silently accept response.create and emit nothing; the request hangs indefinitely and recurs on retry. — STRUCTURALLY FIXED in PF. The silence watchdog converts the invisible stall into a bounded, recoverable event; wedge_regression.rs guards it. COMPARISON: wedge structurally fixed ✅ / codex-lb ❌. | `reference: docs/reference/codex-lb-continuity-reference.md §The wedge (streaming.py 617-1852)` |
| Silence watchdog (arm-on-anchor, race first byte) | ✅ | Proactive bounded timer on any anchor-bearing request: races the first upstream chunk against N seconds; silence ⇒ dead anchor. — PF-original, the reason the rebuild exists: execute_with_watchdog arms Armed{timeout} only when an anchor is present and recovers on Ok(None)\|timeout. codex-lb has NO proactive watchdog. | `(absent in codex-lb) — PF crates/polyflare-server/src/watchdog.rs:103-192` |
| Peek-before-relay | ✅ | No client byte is written until the first upstream chunk arrives, so a stalled attempt can be dropped and restarted safely. — PF buffers exactly the first chunk (stream::once(first).chain(stream)) so a restart is always safe. codex-lb streams as it goes and cannot cleanly restart mid-relay. | `(absent in codex-lb) — PF watchdog.rs:137-149` |
| Recovery Strategy A — anchor-stripped full resend | ✅ | On a dead anchor for a full-resend, re-execute the request with previous_response_id removed (send the whole history). — PF RecoveryPlan::ResendFull builds the anchorless body in prepare (native path reparses raw_body) and execute_recovery runs it, re-homing ownership. codex-lb has a reactive fresh_upstream_request replay but only on EXPLICIT errors, not silence. | `app/modules/proxy/_service/http_bridge/streaming.py:945-992 (fresh_upstream_request replay)` |
| Recovery Strategy B — signal client to resend | ✅ | For a bare-tail anchored request that can't be safely resent full, emit previous_response_not_found so the client self-heals. — PF signal_client_stream emits a synthetic response.failed{previous_response_not_found}. codex-lb only REWRITES an upstream-produced not-found error; it never manufactures one on silence. | `app/modules/proxy/_service/http_bridge/streaming.py:2068-2099` |
| Reactive previous_response_not_found masking | 🟡 | When upstream DOES return previous_response_not_found, rewrites/masks it and can retry a fresh (anchorless) request. — PF handles the not-found SHAPE proactively (arms on the anchor and recovers on silence) rather than reactively on an upstream error; the reactive-rewrite bookkeeping (previous_response_not_found_rewritten) has no PF equivalent. | `app/modules/proxy/_service/http_bridge/streaming.py:2068-2099` |
| Stuck-gate retirement (reactive) | ❌ | A second request contending on a ≥300s-stuck response_create_gate retires the session for FUTURE requests (never unsticks the wedged holder). — Not needed / not ported. PF's per-request watchdog unsticks the ACTUAL wedged request at N seconds with no second-request contention required. | `app/modules/proxy/_service/http_bridge/streaming.py:1906 (+ helpers.py:1745 _record_http_bridge_stuck_retire)` |
| Client-facing stream idle timeout (7200s) | ✅ | A 7200s stream-idle / request-budget timeout — fires long after a wedged client has already given up. — PF replaces this useless 2-hour ceiling with a bounded, config-driven watchdog (POLYFLARE_WATCHDOG_SECS, config.rs:77) measured in seconds on anchored requests. | `app/core/config/settings.py:164,166` |
| Ownership routing pre-filter (HARD) | ✅ | An anchored/file-pinned conversation is routed back to its owning account as a hard constraint above the selector. — PF apply_ownership narrows candidates to [owner] before Selector::pick (no selector-trait change); owner eligible ⇒ Route, else ⇒ Recover. Mirrors codex-lb preferred_account_id / hard_account_bound. | `app/modules/proxy/_service/http_bridge/helpers.py:915-975` |
| Owner-unavailable → Recover (no hard fail) | ✅ | Behavior when the pinned owner is ineligible (excluded/paused/rate-limited). — PF degrades to Recover (RouteDecision::Recover → resend-full on a fresh account, or signal client) rather than failing. codex-lb's hard-bound path instead RAISES 502 'continuity account is excluded'. | `app/modules/proxy/_service/http_bridge/helpers.py:963-975 (_require_http_bridge_bound_account_not_excluded)` |
| Re-home ownership on cross-account recovery | ✅ | After a successful cross-account recovery, the conversation's owner is updated to the new account. — PF record_recovery atomically re-homes owning_account_id + writes the new response_id→owner anchor (observe_recovered_rehomes_owner test). codex-lb rebinds preferred_account_id similarly on its reactive path. | `app/modules/proxy/_service/http_bridge/streaming.py:1378 (preferred_account_id rebind)` |
| Observe-before-end ordering | ✅ | Turn state must be persisted before the client is told the stream ended, so ownership for turn N is durable before turn N+1 can race. — PF ObservingStream awaits continuity.observe(...) INLINE before yielding the terminal None (watchdog.rs:356-397). codex-lb persists via renew/register during the bridge lifecycle. | `app/modules/proxy/durable_bridge_coordinator.py:160-262 (register_* / renew_session)` |
| Streamed response.id capture | ✅ | Extracts the new response.id from response.created/response.completed SSE to record the next anchor. — PF ResponseIdSniffer accumulates ≤64 KiB, parses the id from response.created/completed, then stops buffering. codex-lb reads it via response_id_from_payload during event processing. | `app/modules/proxy/tool_call_dedupe.py:77-92 (response_id_from_payload)` |
| Session-key derivation (hard/soft tiers) | ✅ | turn-state header ⇒ hard key; session header (+prompt_cache_key) ⇒ hard; else soft key from request-id/cache-key/content hash. — Ported in session_key.rs derive_session_key with the same precedence and KeyStrength::{Hard,Soft}; values are sha256-hashed so no raw header/content is stored. Header names carry a VERIFY-vs-live-CLI caveat. | `app/modules/proxy/_service/http_bridge/helpers.py:988-1064` |
| prompt_cache_key thread isolation | ✅ | One process session hosting multiple agent threads is disambiguated by folding the explicit prompt_cache_key into the session key. — PF appends prompt_cache_key to the session-header basis (session_key.rs:131-142), unit-tested by prompt_cache_key_isolates_session_thread. Matches codex-lb's session_header fallback key. | `app/modules/proxy/_service/http_bridge/helpers.py:1020-1064` |
| Synthesized turn-state detection | ❌ | Distinguishes a proxy-minted turn_state placeholder from a real client marker so a synthesized value doesn't hide a durable session on reconnect. — Absent — PF does not synthesize downstream turn-states (no bridge), so it has no placeholder to detect/ignore. | `app/modules/proxy/affinity.py:233-260 (_is_synthesized_turn_state / ensure_downstream_turn_state)` |
| Derived prompt_cache_key (server-side) | ❌ | When the client omits prompt_cache_key, derives a stable one scoped to (model-class, api-key, instructions, first-user-input) for cache affinity. — Absent. Native path forwards the client's own key; the translated alias path sets neither a derived key nor a session pin (NoopContinuity). Flagged as the top MEDIUM gap in COMPARISON's prompt-cache adoption brief. | `app/modules/proxy/affinity.py:63-99 (_derive_prompt_cache_key)` |
| Sticky-session store + prompt-cache max-age eviction | 🟡 | A StickySession table (codex-session / prompt-cache / sticky-thread kinds) with TTL-based eviction backs general affinity routing. — PF has the content-free continuity ownership pin only; no general prompt_cache_key/sticky-thread stickiness table and no max-age eviction. COMPARISON: session affinity 🟡. | `app/modules/proxy/sticky_repository.py:38-55,193-204` |
| Reasoning-replay cache (R3) | ❌ | A server-side cache of chain-of-thought so reasoning survives a recover instead of being lost when the anchor dies. — Neither system ships it. PF has the schema seam (continuity_sessions.reasoning_cache_ref, migration 0002) but it is INERT (NULL in M3-core); codex-lb has nothing at all. Explicitly R3 follow-up work. | `(absent in codex-lb — it only strips reasoning, requests.py:424-455)` |

## identity-oauth-accounts  ·  ✅ 38 🟡 16 ❌ 25

| Feature | PF | PolyFlare status / where | Source |
|---|:--:|---|---|
| Native-vs-SDK request detection | 🟡 | Classifies an inbound /responses request as a first-party Codex client (native UA prefix set OR originator in the native set) vs a foreign SDK. — PolyFlare decides native (bare /responses raw pass-through) vs translated (Claude alias path) by ROUTE, not by sniffing UA/originator headers; there is no `_is_native_codex_request` header inspection. crates/polyflare-server/src/ingress.rs:394 (native path) vs messages_handler_codex_aliased. | `app/core/clients/proxy.py:594-615 (_is_native_codex_request), :170-180 (_NATIVE_CODEX_ORIGINATORS), :538-546 (_NATIVE_CODEX_USER_AGENT_PREFIXES)` |
| Non-native fingerprint persona rewrite | 🟡 | For a non-native request, rewrites the outbound fingerprint in place: sets UA to codex_cli_rs, strips SDK headers, replaces originator/version with canonical Codex values. — synthesize_codex_forward_headers builds a codex persona (UA, originator, session/thread ids, turn-metadata) for the TRANSLATED path only, from scratch — it never strips an inbound SDK header set. crates/polyflare-server/src/ingress.rs:125-150. | `app/core/clients/proxy.py:618-637 (_normalize_non_native_upstream_fingerprint)` |
| Codex CLI User-Agent synthesis | ✅ | Builds the codex_cli_rs UA string `codex_cli_rs/<version> (<os>; <arch>) <terminal>` matching codex-rs get_codex_user_agent. — crates/polyflare-codex/src/codex_headers.rs:91 codex_user_agent(); capture-verified vs codex-cli 0.144.4; os_info-derived os/arch, terminal pinned to headless 'unknown'. | `app/core/clients/proxy.py:570-584 (build_codex_user_agent)` |
| Live Codex version resolution for UA | ✅ | Resolves the latest codex release (GitHub releases → npm registry fallback → settings default) with TTL cache, so the synthesized UA tracks the real fleet's version. — crates/polyflare-codex/src/codex_version.rs cache + main.rs warm loop; GitHub→npm→CODEX_CLI_VERSION floor, same source order. | `app/core/clients/codex_version.py:79-137 (CodexVersionCache._fetch_from_github/_fetch_from_npm)` |
| x-stainless-* header stripping | ❌ | Strips the Stainless SDK header family by prefix (x-stainless-*) so upstream can't distinguish SDK traffic from the Codex CLI. — Not stripped. DROPPED_INBOUND_HEADERS is a 5-item list (host/content-length/connection/transfer-encoding/authorization) with no x-stainless entry; documented gap in ingress.rs:88-91 and COMPARISON.md. | `app/core/clients/proxy.py:556-562, :625-634; app/modules/proxy/api.py:415` |
| x-openai-client-* SDK header stripping | ❌ | Removes the SDK fingerprint headers x-openai-client-version/os/arch/id/user-agent from a normalized non-native request. — Absent — PolyFlare's native forward list drops only 5 transport headers and never targets x-openai-client-*. | `app/core/clients/proxy.py:547-555 (_SDK_FINGERPRINT_HEADER_KEYS)` |
| OpenAI SDK request classification | ❌ | Flags a request as an OpenAI SDK caller via any x-stainless-* header, 'openai' in UA, or the Responses payload shape. — No equivalent SDK-classification predicate on the ingress. | `app/modules/proxy/api.py:409-425 (_is_openai_sdk_request)` |
| Inbound header drop-list / filter | 🟡 | Drops secrets and hop/proxy headers before forwarding upstream: authorization, chatgpt-account-id, content-length, host, forwarded, x-real-ip, x-codex-installation-id, true-client-ip, x-forwarded-*, cf-*, internal openai headers. — DROPPED_INBOUND_HEADERS drops host/content-length/connection/transfer-encoding/authorization only; no x-forwarded-*/cf-*/internal-header coverage. crates/polyflare-server/src/ingress.rs:92-98. | `app/core/clients/proxy.py:88-102, :465-479 (filter_inbound_headers)` |
| chatgpt-account-id paired with selected bearer | ✅ | Attaches the selected account's chatgpt-account-id header alongside its bearer so the (token, account) pair is always consistent; native lowercased vs non-native canonical casing. — Executor inserts chatgpt-account-id from the SELECTED row (insert=replace) so a client's forwarded value can't survive. crates/polyflare-codex/src/executor.rs:99-105. Single canonical casing (no native/non-native split). | `app/core/clients/proxy.py:658-663; app/core/clients/headers.py:6-29` |
| Synthetic account-id header omission | ❌ | Omits the chatgpt-account-id header when the id is a synthetic email_/local_ prefix, matching upstream behavior. — No such guard; executor emits chatgpt-account-id whenever the column is set. PolyFlare ids are codex_<id> so the case rarely arises, but the omission rule is not implemented. | `app/core/clients/headers.py:25-26` |
| Per-account codex installation-id + header/metadata rewrite | ❌ | Stores a per-account codex_installation_id and rewrites the outbound x-codex-installation-id header and turn-metadata installation_id to the selected account's value. — No persisted per-account installation_id and no inbound rewrite; PolyFlare instead SYNTHESIZES a deterministic installation_id into the translated-path turn-metadata only (codex_headers.rs:172). | `app/core/clients/proxy.py:495-535 (apply_codex_installation_headers/metadata); app/db/models.py:80 (codex_installation_id)` |
| x-codex-turn-metadata synthesis | ✅ | Emits the x-codex-turn-metadata JSON (installation/session/thread/turn/window ids + request_kind/sandbox/thread_source/workspaces/turn_started_at) for a client that has none. — crates/polyflare-codex/src/codex_headers.rs:193 turn_metadata_json(); compact JSON, field set verified vs CodexTurnMetadataPayload. codex-lb only relays+rewrites native metadata rather than synthesizing. | `app/core/clients/proxy.py:181-187 (_NATIVE_CODEX_STREAM_HEADER_KEYS relay)` |
| Deterministic synthetic session/thread/turn/window ids | ✅ | Derives stable UUID-shaped session/thread/turn ids + `<thread>:<n>` window id from a per-conversation key so a translated client presents a consistent (non-random) identity. — crates/polyflare-codex/src/codex_headers.rs:151-211 (TurnIdentity::derive, conversation_key prefers prompt_cache_key). PF-original for the translated path; codex-lb relays real ids. | `n/a (PolyFlare-side; codex-lb relays real client ids, app/core/clients/proxy.py:150-169 trace allowlist)` |
| Native codex stream/continuity header passthrough | ✅ | Forwards native Codex continuity headers (x-codex-turn-state / turn-metadata / beta-features) untouched, and treats them as NON-native signals so an SDK replaying a turn-state token can't skip normalization. — Native /responses path forwards ALL surviving inbound headers verbatim (forward_headers_from_inbound). crates/polyflare-server/src/ingress.rs:104-119. | `app/core/clients/proxy.py:181-187, :594-604` |
| Configurable fingerprint OS/arch/terminal | 🟡 | Operator-settable codex_fingerprint_os / _arch / _terminal feeding the synthesized UA. — OS/arch are os_info-derived at runtime (not operator-config), terminal is pinned to 'unknown' (headless-codex) rather than a configurable iTerm token. crates/polyflare-codex/src/codex_headers.rs:91-114. | `app/core/config/settings.py:284-286` |
| Upstream TLS/SSL context | ✅ | Builds the TLS context for upstream calls (certifi CA bundle, default verification). — PolyFlare pins rustls + aws-lc-rs (far stronger than codex-lb's stdlib ssl default context). crates/polyflare-codex/src/executor.rs:40-64. | `app/core/clients/http.py:89-92 (_build_ssl_context); app/core/clients/codex.py:236-240` |
| Real rustls/JA3 ClientHello control | ✅ | Pins the TLS stack (provider, cipher list, key shares) so the ClientHello structurally matches the real codex-rs transport. — PF-unique: aws-lc-rs provider install + use_rustls_tls, X25519MLKEM768 PQ hybrid share proven on the wire. codex-lb fakes only the application layer (aiohttp, stdlib TLS). crates/polyflare-codex/src/executor.rs:40-64. | `(codex-lb has NO TLS-layer control; app/core/clients/codex.py uses aiohttp TCPConnector)` |
| TLS ClientHello capture + PQ-hybrid parity test | ✅ | Raw-TCP capture of the executor's ClientHello, parsed to assert the X25519MLKEM768 (0x11EC) hybrid group is offered. — PF-unique guardrail: crates/polyflare-codex/tests/tls_fingerprint.rs. Structural parity only; byte-exact vs a live codex capture deferred. | `(none in codex-lb)` |
| Upstream HTTP keepalive / DNS-cache TTFT tuning | 🟡 | Keeps idle upstream connections + resolved DNS across turns (keepalive_timeout=90, ttl_dns_cache=300) to avoid a fresh handshake per turn. — Uses reqwest defaults with a 10s connect_timeout; no explicit keepalive/DNS-cache tuning. crates/polyflare-codex/src/executor.rs:56-62. | `app/core/clients/http.py:135-146` |
| OAuth refresh (refresh_token grant) | ✅ | POST /oauth/token with grant_type=refresh_token+client_id+scope to mint fresh access/refresh/id tokens. — crates/polyflare-codex/src/oauth.rs:262 OAuthClient::refresh (JSON body, same client_id/scope). | `app/core/auth/refresh.py:88-184 (refresh_access_token)` |
| Refresh-due gate | ✅ | Decides when to refresh a stored token. — PolyFlare improves on codex-lb: should_refresh reads the access token's own exp with a 2-day margin (age-gate fallback), vs codex-lb's fixed 8-day interval on last_refresh. crates/polyflare-codex/src/oauth.rs:89. | `app/core/auth/refresh.py:75-79 (should_refresh, TOKEN_REFRESH_INTERVAL_DAYS=8)` |
| Refresh-token rotation handling | ✅ | Persists the rotated refresh token, keeping the previous one when the response omits it. — crates/polyflare-codex/src/oauth.rs:302 refresh_token.unwrap_or(old). | `app/core/auth/refresh.py:160-184; app/modules/accounts/auth_manager.py:229-232` |
| Permanent-failure classification table | ✅ | 12-code PERMANENT_FAILURE_CODES set with a 9-code REAUTH_REQUIRED subset driving reauth_required vs deactivated. — crates/polyflare-codex/src/oauth.rs:105 classify_failure pins all 12 codes + the reauth/deactivate split, table-tested against logic.py. | `app/core/balancer/logic.py:16-48, :1065-1068 (account_status_for_permanent_failure)` |
| Per-account refresh singleflight | ✅ | Serializes/dedupes concurrent refreshes of the same account so peers don't present a rotated-away refresh token and get wrongly reauth-marked. — crates/polyflare-server/src/refresh_locks.rs per-account async mutex + double-check-staleness-after-lock against the freshly stored token. ingress.rs:223-240. | `app/modules/accounts/auth_manager.py:78-154 (_RefreshSingleflight)` |
| Recent-refresh-failure negative cache | ❌ | Caches a recent refresh failure per (account, token) for a TTL so repeated selections don't hammer the OAuth endpoint. — No negative cache; PolyFlare relies on the persisted status write + per-account lock to throttle re-attempts. | `app/modules/accounts/auth_manager.py:81, :122-135 (proxy_refresh_failure_cooldown_seconds)` |
| Global refresh-admission concurrency cap | ❌ | Bounds the number of concurrent OAuth token refreshes fleet-wide (proxy_token_refresh_limit=64) via an admission gate. — No global refresh concurrency limiter; only per-account serialization exists. | `app/modules/proxy/work_admission.py:54-70 (WorkAdmissionController.acquire_token_refresh)` |
| Refresh isolated from client-cancellable session | 🟡 | Runs the shielded refresh against a fresh DB session so a client disconnect can't close the session under the still-running refresh and strand a pooled connection. — PolyFlare runs refresh inline under the per-account lock with bounded persist retries; no separate session, but the sqlx pool + busy_timeout absorb it. ingress.rs:243-279. | `app/modules/accounts/auth_manager.py:184-206 (_run_refresh, refresh_repo_factory)` |
| Peer-refresh detection / avoid double re-mark | 🟡 | After acquiring the lock, detects that a peer already rotated the token (material changed) and adopts it instead of re-refreshing / re-marking. — Detects via re-read + should_refresh(exp) after lock rather than by refresh-token fingerprint compare; also bails if a peer already marked the account non-active. ingress.rs:225-301. | `app/modules/accounts/auth_manager.py:212-219, :401-420 (_refresh_token_material_changed)` |
| Rotated-token persist with retry backstop | ✅ | Persists the rotated tokens with bounded retries and loud content-safe logging on final failure (the one uniquely critical write). — crates/polyflare-server/src/ingress.rs:259-279 (PERSIST_MAX_ATTEMPTS + backoff, serves in-memory token on final failure). codex-lb does a single in-transaction update. | `app/modules/accounts/auth_manager.py:282-309 (update_tokens)` |
| PKCE pair generation (S256) | ✅ | Generates a random verifier and its base64url(SHA256) S256 challenge for the login flow. — crates/polyflare-codex/src/oauth.rs:224 generate_pkce (64-byte verifier). | `app/core/clients/oauth.py:55-62 (pkce_challenge/generate_pkce_pair)` |
| Authorization URL builder | ✅ | Builds the authorize URL with response_type/client_id/redirect_uri/scope/code_challenge/state/originator + id_token_add_organizations + codex_cli_simplified_flow + offline_access. — crates/polyflare-codex/src/oauth.rs:313 build_authorize_url with the same param set + codex_cli_rs originator. | `app/core/clients/oauth.py:65-91 (build_authorization_url)` |
| Authorization-code token exchange | ✅ | Exchanges the auth code + PKCE verifier for tokens via form-urlencoded POST /oauth/token. — crates/polyflare-codex/src/oauth.rs:342 exchange_code (.form()). | `app/core/clients/oauth.py:94-168 (exchange_authorization_code)` |
| Browser login + loopback callback server | ✅ | Runs a local 127.0.0.1:1455 /auth/callback listener to catch the OAuth redirect automatically. — crates/polyflare-codex/src/login.rs:82 wait_for_callback (best-effort loopback bind on 1455, races paste+timeout). | `app/modules/oauth/service.py:278-303, :389-432 (OAuthCallbackServer/_start_browser_flow)` |
| Manual paste-callback fallback | ✅ | Accepts a manually pasted full redirect URL for remote/headless hosts where the loopback isn't reachable. — crates/polyflare-codex/src/login.rs:125 read_pasted_callback + parse_callback (full-URL or path). | `app/modules/oauth/service.py:434-500 (manual_callback)` |
| OAuth state/CSRF validation | ✅ | Validates the returned state matches the flow's issued state before exchanging the code. — crates/polyflare-codex/src/login.rs:74 StateMismatch guard. | `app/modules/oauth/service.py:138, :450-473 (_state_token_index / state check)` |
| Device-code login flow | ❌ | Headless enrollment via usercode request + token polling (device_auth_id/user_code) with pending/slow_down handling. — Not implemented — PolyFlare offers only authorization_code+PKCE (loopback or paste). COMPARISON.md marks device-code as codex-lb-only. | `app/core/clients/oauth.py:171-348 (request_device_code/exchange_device_token); app/modules/oauth/service.py:502-623` |
| OAuth flow state store (multi-flow) | ❌ | In-memory multi-flow store with per-flow status, terminal-flow retention cap, and pending-flow TTL pruning. — Login is a one-shot CLI command; there is no persistent flow store, status polling, or flow pruning. | `app/modules/oauth/service.py:133-276 (OAuthStateStore)` |
| OAuth start/status/complete/manual-callback HTTP API | ❌ | Dashboard-driven OAuth endpoints (POST /start, GET /status, POST /complete, POST /manual-callback). — OAuth onboarding is CLI-only (`polyflare login`); no HTTP OAuth surface for the dashboard. | `app/modules/oauth/api.py:35-79` |
| Reauth seat-mismatch guard | 🟡 | On a targeted reauth, rejects a callback that authenticated a different seat/workspace than the account being repaired. — Login picks onboard-vs-reauth by chatgpt_account_id match but does NOT reject a mismatched seat; no ReauthSeatMismatchError analog. crates/polyflare-server/src/main.rs:230-242. | `app/modules/oauth/service.py:103-112, :676-743 (ReauthSeatMismatchError/_save_oauth_account)` |
| Onboard-vs-reauth in-place update | ✅ | Existing seat → update tokens in place; new seat → insert, avoiding a duplicate account row. — crates/polyflare-server/src/main.rs:234-278 via AccountRepo::find_by_chatgpt_account_id → update_tokens else insert. | `app/modules/oauth/service.py:676-743; app/modules/accounts/repository.py:266-349 (upsert_account_slot/replace_reauthorized)` |
| OAuth via per-account egress proxy route | ❌ | Routes OAuth token/refresh/device calls through the account's resolved upstream (SOCKS/HTTP) proxy for IP diversity. — No per-account egress proxy; OAuth goes direct. COMPARISON.md lists egress proxy routing as absent. | `app/core/clients/oauth.py:103-136 (route/allow_direct_egress); app/modules/oauth/service.py:87-101 (_oauth_route)` |
| chatgpt-account-id backfill from id_token | ❌ | Backfills a missing chatgpt_account_id by decoding the stored id_token, persisting it. — No lazy backfill path; chatgpt_account_id is set at import/login only. | `app/modules/accounts/auth_manager.py:354-388 (_ensure_chatgpt_account_id)` |
| Proactive/background OAuth token refresh | ❌ | Refreshes near-due tokens off the hot path (usage-refresh scheduler calls ensure_fresh with should_refresh). — PolyFlare refreshes tokens LAZILY only on the request hot path; the usage_refresh loop refreshes usage windows, not OAuth tokens. crates/polyflare-server/src/usage_refresh.rs. | `app/modules/accounts/auth_manager.py:176-182; app/core/usage/refresh_scheduler.py` |
| At-rest token encryption | ✅ | Encrypts the three OAuth tokens at rest. — PolyFlare uses XChaCha20-Poly1305 (24-byte random nonce/blob) — stronger than codex-lb's Fernet (AES128-CBC+HMAC). crates/polyflare-store/src/crypto.rs. | `app/core/crypto.py:20-31 (TokenEncryptor/Fernet)` |
| Key file get-or-create (0600) | ✅ | Generates and persists the encryption key file with 0600 perms on first run. — crates/polyflare-store/src/crypto.rs:26 load_or_create (32 raw bytes, chmod 0600 on Unix). codex-lb writes a Fernet base64 key. | `app/core/crypto.py:10-17 (_get_or_create_key)` |
| Decrypted-token zeroization | ✅ | Wipes decrypted token plaintext from memory when dropped. — PF-only: PlainTokens is ZeroizeOnDrop. crates/polyflare-store/src/account.rs:45. codex-lb (Python) has no memory-wipe. | `(none in codex-lb)` |
| Redacted Debug/logging on token structs | ✅ | Ensures token material never renders in debug/log output. — PlainTokens/RefreshedTokens Debug print ***; content-safety lint test. crates/polyflare-store/src/account.rs:52, oauth.rs:192. | `app/core/auth/refresh.py (logs status only, never token); implicit` |
| Encryption-key fingerprint sentinel (replica guard) | ❌ | Stores sha256(key) as a DB sentinel to detect a replica whose encryption key differs from the shared one. — Single-binary target; no multi-replica key-mismatch guard. | `app/core/config/key_fingerprint.py:19-34` |
| Single-SELECT account+token hot-path decrypt | ✅ | Reads the account row + its three encrypted tokens in one SELECT and decrypts in memory (one round-trip on the hot path). — crates/polyflare-store/src/account.rs:233 get_with_tokens (SELECT_ACCOUNT_WITH_TOKENS). | `app/modules/proxy/account_cache.py (account/token caching); app/modules/accounts/repository.py` |
| Token vs snapshot cache-generation split | ✅ | Separate write generations so a token rotation invalidates the token cache while the usage/status snapshot cache stays warm. — crates/polyflare-store/src/account.rs:130-161,384-388 (generation vs token_generation atomics); update_tokens bumps only token_generation. | `app/core/cache/invalidation.py; app/modules/proxy/account_cache.py` |
| Named account pools (first-class routable) | ✅ | Tags accounts to named pools reachable via dedicated ingress paths. — PF beyond codex-lb: accounts.pool + /{pool}/responses\|/v1/messages + per-pool selection. migrations/0006_account_pool.sql; account.rs update_pool. codex-lb has a single global pool. | `(codex-lb: single global pool; no named-pool column)` |
| Provider partitioning of accounts | ✅ | Discriminates accounts by backend provider so a provider's ingress only selects its own accounts. — accounts.provider column ('codex'\|'anthropic'); /responses picks only Codex accounts. migrations/0003_provider.sql; ingress.rs Provider parse. | `(codex-lb is Codex-only; no provider column)` |
| Account alias | 🟡 | Human-friendly per-account alias, settable and shown in the dashboard. — alias COLUMN exists and is imported/selected (account.rs), but there is NO set-alias write path — write_api PATCH covers only pool/routing_policy/status. | `app/modules/accounts/service.py:672-676 (set_account_alias); app/modules/accounts/repository.py (update_alias); app/modules/accounts/api.py:322 (PUT /alias)` |
| Per-account routing policy | ✅ | normal \| burn_first \| preserve routing policy per account. — crates/polyflare-store/src/account.rs:303 update_routing_policy + write_api PATCH validates normal\|burn_first\|preserve. | `app/modules/accounts/service.py:654-660 (set_routing_policy); alembic add_account_routing_policy` |
| Account status lifecycle enum | 🟡 | active/paused/rate_limited/quota_exceeded/reauth_required/deactivated status with defined transitions. — status is a free string; active/paused settable via API; reauth_required/deactivated written on OAuth failure; rate_limited/quota_exceeded only partly (usage-refresh maps quota). No enum, no formal transitions. | `app/db/models.py (AccountStatus); app/modules/accounts/repository.py:500-568` |
| Pause / reactivate with CAS guard | 🟡 | Manual pause/reactivate using compare-and-set on expected status/reason/reset/blocked to avoid stomping concurrent lifecycle changes; reauth_required can't be directly reactivated. — PATCH sets status active\|paused with no expected-state CAS and no reauth_required block. crates/polyflare-server/src/write_api.rs:57-94. | `app/modules/accounts/service.py:590-634 (reactivate_account/pause_account, update_status_if_current)` |
| Workspace/seat identity model | 🟡 | Per-account workspace_id/label, seat_type, chatgpt_user_id captured from id-token claims with multi-alias field resolution. — All columns present and populated at login/import (decode_claims), but PolyFlare has no slot-identity conflict logic around them. crates/polyflare-codex/src/oauth.rs:126-164. | `app/core/auth/__init__.py:38-149 (OpenAIAuthClaims/resolve_seat_identity); app/modules/accounts/schemas.py` |
| Workspace-slot conflict / duplicate reconciliation | ❌ | Detects a workspace slot already owned by another account and reconciles chatgpt-identity duplicates on reauth/import. — No slot-conflict detection or duplicate reconciliation; login is a plain find-by-chatgpt-id upsert. | `app/modules/accounts/repository.py:280-472 (upsert_account_slot/_reconcile_chatgpt_identity_duplicates/workspace_slot_taken)` |
| Deterministic account-id generation | 🟡 | Derives a unique account id from chatgpt_account_id + workspace/email hash, with email_/local_ fallbacks when no upstream id exists. — Login uses codex_<chatgpt_account_id\|sub\|now> (simpler, no workspace/email hash); the importer preserves codex-lb's original ids verbatim. crates/polyflare-server/src/main.rs:244-251. | `app/core/auth/__init__.py:199-222 (generate_unique_account_id/fallback_account_id)` |
| id_token claims decode (unverified JWT) | ✅ | Decodes (without verifying) the id_token payload into identity claims, honoring the nested https://api.openai.com/auth claim and multiple field aliases. — crates/polyflare-codex/src/oauth.rs:126 decode_claims (nested auth-claim precedence, sub fallback). | `app/core/auth/__init__.py:158-186 (extract_id_token_claims/claims_from_auth)` |
| Seat identity resolution precedence | ✅ | Resolves the stable per-seat principal as chatgpt_user_id (auth claim) > top-level chatgpt_user_id > sub. — crates/polyflare-codex/src/oauth.rs:146-151 chatgpt_user_id precedence incl. sub fallback (tested). | `app/core/auth/__init__.py:141-149 (resolve_seat_identity)` |
| plan_type coercion/normalization | 🟡 | Coerces the raw claim plan into a canonical account plan_type with a default fallback. — PolyFlare stores plan_type as the raw claim string with an 'unknown' default; no coercion/normalization table like coerce_account_plan_type. crates/polyflare-server/src/main.rs:261-264. | `app/core/plan_types.py (coerce_account_plan_type); app/modules/accounts/service.py:510` |
| Access-token expiry epoch extraction | ✅ | Reads the token exp claim to compute expiry (used for refresh timing / auth export). — crates/polyflare-codex/src/oauth.rs:78 token_exp (drives should_refresh). | `app/core/auth/__init__.py:189-196 (token_expiry_epoch_ms)` |
| Account probe / validate | ❌ | Refreshes an account then fires a test upstream request to validate it, returning a probe status code. — No probe/validate endpoint or path. | `app/modules/accounts/service.py:707-751 (probe_account); app/modules/accounts/api.py:275` |
| security_work_authorized flag | 🟡 | Per-account flag gating security/trusted-access routing. — Column present and imported/stored (account.rs), but no API to toggle it and no routing consumer wired. | `app/modules/accounts/service.py:640-646 (update_security_work_authorized); alembic add_account_security_work_authorized` |
| Account delete (+ optional history purge) | ❌ | Deletes an account, optionally its request history, with routing-cache invalidation. — No delete endpoint/path; the accounts table only grows (deleted_at is importer-set only). | `app/modules/accounts/service.py:660-670 (delete_account)` |
| API keys (issuance/management) | ❌ | First-class API keys minted and managed for downstream clients (hash, limits, enforcement). — PolyFlare has NO API-key concept; all endpoints are unauthenticated (network-boundary trust). write_api.rs module doc. | `app/modules/api_keys/service.py, repository.py, api.py` |
| Per-API-key account scoping | ❌ | Restricts an API key to a specific set of accounts (ApiKeyAccountAssignment / assigned_account_ids / account_assignment_scope_enabled). — Absent (no API keys). COMPARISON.md lists per-API-key account scoping as codex-lb-only. | `app/modules/api_keys/repository.py:433-436 (replace_account_assignments); api.py:55-57,147` |
| Per-API-key source scoping | ❌ | Restricts an API key to specific request sources (source_assignment_scope_enabled). — Absent (no API keys). | `app/modules/api_keys/repository.py:348-350; api.py:56` |
| Per-API-key model steering (apply_to_codex_model) | ❌ | Binds an API key to force a specific Codex model. — Absent (no API keys); PolyFlare steering is by model-alias only. | `app/modules/api_keys/api.py:46,135,192; repository.py:324-326` |
| Zero-re-auth importer from codex-lb | ✅ | Reads a codex-lb store.db read-only, Fernet-decrypts each account's three tokens, re-encrypts XChaCha, and copies accounts+usage+request_logs with no re-login. — PF-unique: crates/polyflare-store/src/import.rs import_from_codex_lb; ~148k rows + accounts moved, tokens decrypt via shared Fernet key. | `(none in codex-lb; PolyFlare-side migration)` |
| Import codex CLI auth.json | ❌ | Imports an account from a codex/OpenCode auth.json (idToken/accessToken/refreshToken/accountId + lastRefreshAt) with claim parsing. — PolyFlare imports from a codex-lb store.db, not from a raw auth.json; no auth.json ingest endpoint. | `app/modules/accounts/service.py:500-555 (import_account); app/core/auth/__init__.py:25-36,152-155 (AuthFile/parse_auth_json)` |
| Export account auth (codex/opencode) | ❌ | Exports an account's tokens back into codex/OpenCode auth.json shapes for reuse in other tools. — No export-auth path. | `app/modules/accounts/service.py:425-498 (export_auth/export_opencode_auth); api.py:166-203` |
| Import dry-run preview | ✅ | Runs the full import (decrypt/re-encrypt/stage) inside a transaction then rolls back, reporting would-write counts side-effect-free. — crates/polyflare-store/src/import.rs:129,350 dry_run rollback. codex-lb has no dry-run import. | `(none in codex-lb)` |
| Idempotent import | ✅ | Re-import skips already-present rows via OR IGNORE + import_source_id unique index. — crates/polyflare-store/src/import.rs (accounts INSERT OR IGNORE; request_log import_source_id unique). | `(none; PolyFlare-side)` |
| Import content-safety filter | ✅ | Never SELECTs codex-lb's free-form/PII columns (useragent/client_ip/error_message/failure_detail) during migration. — crates/polyflare-store/src/import.rs:1-15 module doc + fixed SELECT lists. | `(PolyFlare-side content-safety rule)` |
| Import pause-until-proxy-ready | ❌ | Pauses an imported account until its required upstream egress proxy binding/default pool is available. — No egress proxy concept, so no import-time proxy gate. | `app/modules/accounts/service.py:557-588 (_import_usage_refresh_allowed / IMPORT_PROXY_REQUIRED_PAUSE_REASON)` |
| OAuth success page on callback | 🟡 | Serves a styled success/error HTML page to the browser after the redirect completes. — crates/polyflare-codex/src/login.rs:104 writes a minimal inline success page (no error template, no external HTML file). | `app/modules/oauth/service.py:63,819-842 (templates/oauth_success.html, _success_html/_error_html)` |
| Anthropic subscription OAuth refresh/login | ❌ | Refresh/login for Anthropic subscription accounts. — Codex-lb is Codex-only (N/A there). PolyFlare has Anthropic ACCOUNTS but uses their stored access_token as-is; refresh/login is Task 7, VERIFY-gated, no confirmed endpoint. ingress.rs:212-214. | `(N/A in codex-lb — listed to close the OAuth sub-area)` |

## quota-usage-observability  ·  ✅ 10 🟡 19 ❌ 63

| Feature | PF | PolyFlare status / where | Source |
|---|:--:|---|---|
| Duration-based window classification (5h vs weekly) | ✅ | Classify each rate-limit window as the 5h (primary) or weekly (secondary) limit and normalize slot keys (5h/7d/30d aliases). — PF re-implements this MORE robustly by DURATION not slot in crates/polyflare-server/src/usage_windows.rs (classify(): <1440min=FiveHour else Weekly), used by snapshot.rs + read_api.rs. | `app/core/usage/__init__.py:58-79 (_normalize_window_key, normalize_rate_limit_windows)` |
| Monthly (30d) third usage window | 🟡 | Third window kind (monthly/30d) with its own capacity + default window minutes, distinct from 5h/weekly, used by free plans. — PF usage_windows.classify() lumps any >=1440min window into Weekly; no monthly concept. usage_history."window" column can hold 'monthly' (migration 0001) but nothing classifies/gates it. | `app/core/usage/__init__.py:42-48,181-186 (PLAN_CAPACITY_CREDITS_MONTHLY, capacity_for_plan monthly)` |
| Weekly-only-in-primary-slot remap | ✅ | When upstream reports the weekly window in the primary slot (free plan / promo), remap it to secondary so 5h/7d consumers stay consistent. — PF usage_windows.rs resolve() picks the freshest window per KIND across both slots; regression-tested by promo_weekly_in_primary_slot (usage_refresh.rs derive_gate + usage_windows tests). | `app/core/usage/__init__.py:232-281 (should_use_weekly_primary, normalize_weekly_only_rows)` |
| Elapsed-window expiry to available | 🟡 | A window whose reset_at has passed is treated as reset (0% used, no reset) so stale samples never freeze availability. — PF surfaces a stale flag (usage_windows STALE_AFTER_SECS=1800) and derive_gate treats an absent/non-exhausted window as available, but does not rewrite an elapsed window's used_percent to 0. | `app/core/usage/__init__.py:243-258 (expire_elapsed_window_rows)` |
| Plan capacity credit tables | ✅ | Per-plan secondary/primary/monthly credit capacities (free 1134, plus 7560, pro 50400, prolite 37800, etc.). — Secondary table ported verbatim in crates/polyflare-core/src/select.rs plan_capacity_secondary() incl aliases + free fallback; primary/monthly tables NOT ported (only weekly capacity drives routing). | `app/core/usage/__init__.py:20-44 (PLAN_CAPACITY_CREDITS_PRIMARY/SECONDARY/MONTHLY)` |
| Credit/percent derivation helpers | 🟡 | Convert used_percent<->used_credits<->remaining_credits/remaining_percent against plan capacity. — PF select.rs Candidate::remaining_secondary_credits() computes remaining weekly credits inline for routing weight; the full used/remaining credit + percent helper set is not ported. | `app/core/usage/__init__.py:99-125 (used_credits_from_percent, remaining_credits_from_used, remaining_percent_from_used)` |
| Window-minutes defaults + resolution | 🟡 | Default window durations (300/10080/43200 min) and per-window resolution from mixed rows, incl is_weekly/is_monthly/is_primary predicates. — PF stores window_minutes (migration 0001) and splits on SHORT_WINDOW_MAX_MINUTES=1440, but has no defaults table and no is_*_window_minutes predicates. | `app/core/usage/__init__.py:46-48,189-229 (default_window_minutes, resolve_window_minutes)` |
| Pooled usage-window summary aggregation | ❌ | Sum capacity + used across all accounts for a window to produce a fleet-wide used_percent/reset_at summary. — PF read_api surfaces per-account windows only; no cross-account usage-window rollup summary. | `app/core/usage/__init__.py:138-172 (summarize_usage_window)` |
| Credits snapshot capture (has/unlimited/balance) | 🟡 | Capture ChatGPT credit-pack state (has_credits, unlimited, balance) alongside usage windows. — usage_history columns credits_has/credits_unlimited/credits_balance exist (migration 0001) and the importer carries them (import.rs), but PF's native usage_refresh.rs never parses credits from /wham/usage — they stay NULL on live rows. | `app/modules/usage/updater.py:893-900 (_credits_snapshot)` |
| Credit-override quota logic | ❌ | An account with usable credits (unlimited/has/balance>0) overrides a QUOTA_EXCEEDED gate back to active. — PF derive_gate keys purely off used_percent>=100; no credit-override path. | `app/core/usage/quota.py:29-60,101-129 (_has_credit_override, _has_usable_credits)` |
| Sibling-window freshness proof | 🟡 | A stale window row can't freeze freshness if a strictly newer sibling-window row proves a later fetch happened. — PF uses a simpler per-window STALE_AFTER_SECS staleness flag (usage_windows.rs) rather than cross-sibling recency reasoning. | `app/modules/usage/updater.py:425-461 (_usage_is_fresh_with_siblings)` |
| Background usage-refresh loop | ✅ | Periodic per-account poll of the usage endpoint to keep windows + reset times live. — PF crates/polyflare-server/src/usage_refresh.rs spawn_usage_refresh() runs every 600s (codex-lb default 60s), then warms the snapshot cache + sweeps token cache. | `app/modules/usage/updater.py:256-356 (refresh_accounts); settings usage_refresh_interval_seconds=60` |
| Per-account usage-refresh singleflight | ❌ | Dedupe concurrent refreshes of the same account (join-existing task) so only one upstream fetch runs at a time. — PF's refresh loop is a sequential per-account for-loop; no singleflight/inflight-dedup (RefreshLocks exists only for OAuth token refresh, not usage). | `app/modules/usage/updater.py:156-228 (_UsageRefreshSingleflight)` |
| Usage-refresh auth-failure cooldown | ❌ | After a 401/403 usage fetch, put the account in a refresh cooldown (default 300s) instead of hammering it. — PF refresh_account() silently returns Ok on any non-2xx; no per-account usage-refresh cooldown map. | `app/modules/usage/updater.py:1225-1252 (_mark_usage_refresh_auth_cooldown, _is_usage_refresh_in_cooldown)` |
| Post-reset / quota-recovery freshness bypass | ❌ | Force a fresh usage fetch (ignoring the interval) once a rate-limited/quota account's reset or cooldown elapses. — PF refresh loop is a fixed-interval sweep with no per-account reset-driven bypass. | `app/modules/usage/updater.py:1132-1162 (_quota_recovery_should_bypass_freshness, _account_needs_post_reset_refresh)` |
| Usage-payload identity/slot mismatch guard | ❌ | Refuse to write usage/plan when the payload reports a workspace/plan slot owned by (or inconsistent with) another account. — PF trusts the /wham/usage payload for the fetched account; no workspace-slot ownership guard. | `app/modules/usage/updater.py:728-777,903-932 (_sync_identity_metadata, _payload_mismatches_account_slot)` |
| Quota-based status inference from usage | ✅ | Primary exhausted -> RATE_LIMITED with 5h reset; secondary/weekly exhausted -> QUOTA_EXCEEDED with weekly reset; else ACTIVE. — PF usage_refresh.rs derive_gate() implements the same mapping (weekly>=100 -> quota_exceeded, 5h>=100 -> rate_limited, else active), classified by duration. | `app/core/usage/quota.py:9-91 (apply_usage_quota)` |
| Quota status recovery from fresh usage | 🟡 | Recover a RATE_LIMITED/QUOTA_EXCEEDED account to ACTIVE when fresh usage shows quota, honoring the persisted cooldown/reset deadline and compare-and-set. — PF derive_gate flips status active/rate_limited/quota_exceeded on every refresh but does NOT honor a cooldown/blocked_at deadline (a fresh sample can clear a throttle early) and uses plain update, not compare-and-set. | `app/modules/usage/updater.py:779-859 (_recover_quota_status_from_usage)` |
| Additional (per-model) rate-limit capture + merge | ❌ | Capture extra per-feature quotas (e.g. image models) from additional_rate_limits, canonicalize/merge duplicate aliases, sync + prune stale rows. — PF parses only rate_limit.{primary,secondary}_window; additional_rate_limits ignored. No additional_usage_history table. | `app/modules/usage/updater.py:1003-1055 (_merge_additional_rate_limits); app/db/models.py:225 (AdditionalUsageHistory)` |
| Additional-quota registry (model->quota_key) | ❌ | JSON-backed registry mapping model ids to canonical quota keys, display labels, routing policy, plan scoping and alias sets. — Absent; PF has no per-model auxiliary quota concept. | `app/modules/usage/additional_quota_keys.py:22-60 (AdditionalQuotaDefinition, registry)` |
| Additional model-limit resolution for routing | ❌ | Resolve a request's model to its additional quota key + display label so routing/limits can consider it. — Absent. | `app/modules/proxy/additional_model_limits.py:19-44 (get_additional_model_limit)` |
| 429 rate-limit writeback | ❌ | On upstream 429: error_count++, last_error_at/blocked_at=now, status=RATE_LIMITED, cooldown_until=now+delay, persist reset_at. — PORTING-CODEXLB.md Phase A2: PF's cooldown_until/error_count/reset runtime fields are read by select.rs:144-188 but NOTHING writes them on an upstream 429 (RuntimeState substrate unbuilt). | `app/core/balancer/logic.py:991-1021 (handle_rate_limit)` |
| Retry-After hint parsing | ❌ | Parse 'try again in Xs' hints incl compound/multi-unit durations (6m0s, 1h2m3s, ms) with unit-boundary safety. — Absent; PF never inspects upstream 429 bodies for a retry hint. | `app/core/utils/retry.py:51-68 (parse_retry_after)` |
| Exponential backoff with jitter | ❌ | Backoff delay = 200ms * 2^(attempt-1) * jitter(0.9-1.1) for successive failures. — select.rs has error_backoff_secs (min(300,30*2^(n-3))) as the eligibility gate, but the 200ms exponential retry-backoff for actual retries is absent (no retry loop). | `app/core/utils/retry.py:71-77 (backoff_seconds)` |
| 30s minimum cooldown floor | ❌ | Persisted reset_at cooldown is floored at 30s (rounded up to whole second) so peer replicas don't flip an account back to active instantly. — Absent (no writeback path to floor). | `app/core/balancer/logic.py:988,1004,1021 (RATE_LIMITED_MIN_COOLDOWN_SECONDS)` |
| Quota-exceeded writeback | ❌ | On quota exhaustion: status=QUOTA_EXCEEDED, used_percent=100, cooldown=now+120s, reset_at=upstream else now+3600, no error_count bump. — PORTING-CODEXLB.md Phase A6; the select.rs quota_exceeded eligibility gate exists but nothing writes the status from an upstream failure (only usage-refresh derive_gate sets it). | `app/core/balancer/logic.py:1024-1053 (handle_quota_exceeded, QUOTA_EXCEEDED_COOLDOWN_SECONDS=120)` |
| Permanent/auth-failure writeback + code table | 🟡 | Map a permanent failure code to REAUTH_REQUIRED vs DEACTIVATED and set deactivation_reason. — PF has the exact reauth-vs-deactivate code table for the OAuth refresh path (classify_failure), but it is NOT wired to upstream request failures (PORTING A7). | `app/core/balancer/logic.py:1056-1068 (handle_permanent_failure, account_status_for_permanent_failure)` |
| Upstream reset-at extraction | ❌ | Extract resets_at / resets_in_seconds from an upstream error/metadata to seed the cooldown deadline. — Absent on the failure path; PF only reads reset_at from the /wham/usage window payload. | `app/core/balancer/logic.py:1089-1096 (_extract_reset_at); app/core/clients/proxy.py:1192-1208` |
| Selector empty-pool retry hint | ❌ | When zero candidates are selectable, surface a clamped 'Try again in Ns' (300s max) hint derived from the soonest reset. — PF ingress no_eligible() returns a bare 503 with no soonest-reset retry hint (PORTING B5/B10 note). | `app/core/balancer/logic.py:1035-1040 (SELECTOR_RETRY_HINT_MAX_SECONDS, _format_retry_hint)` |
| Rate-limit headers response cache | ❌ | Cache computed x-ratelimit response headers with TTL = usage_refresh_interval to avoid recompute per request. — PF is byte-verbatim pass-through and synthesizes no rate-limit response headers. | `app/modules/proxy/rate_limit_cache.py:11-46 (RateLimitHeadersCache)` |
| Health-tier drain state machine | 🟡 | HEALTHY->DRAINING->PROBING->HEALTHY transitions driven by used%>=85 / secondary>=90 / recent errors, frozen on blocked statuses. — select.rs computes should_drain + effective_tier per selection (85/90/2-errors-in-60s) and pools healthy>probing>draining, but base health_tier is always 0, there are no PROBING transitions, and it's never persisted (PORTING B8). | `app/core/balancer/logic.py:1099-1157 (evaluate_health_tier)` |
| Error-backoff eligibility gate | ✅ | Once error_count>=3, exclude the account until last_error_at + min(300, 30*2^(n-3)) elapses, then clear its error state. — Ported faithfully in select.rs:180-188 error_backoff_secs + eligibility(); inputs are inert today (nothing writes error_count). | `app/core/balancer/logic.py:470-483` |
| Cooldown eligibility gate + clear-on-expiry | ✅ | Skip an account while cooldown_until is active; on expiry clear cooldown + error state together. — Ported in select.rs:166-175; inert inputs (nothing writes cooldown_until). | `app/core/balancer/logic.py:464-469` |
| Reset-recovery in eligibility | ✅ | A rate_limited/quota_exceeded account auto-recovers (usage/error zeroed) once its reset_at passes, without early admission. — Ported in select.rs:142-164 (rate_limited zeros primary+error_count; quota_exceeded zeros primary+secondary), driven by the live derive_gate-set reset_at. | `app/core/balancer/logic.py:448-463` |
| WeeklyCreditPace forecasting | ❌ | Server-computed weekly quota pace: schedule-gap (actual vs linear-expected remaining) + forecast shortfall + status/confidence. — Absent (COMPARISON.md 'Depletion/pace/burn-rate forecasting' = no; PORTING D16 deferred). | `app/modules/dashboard/weekly_pace.py:52-214 (build_weekly_credit_pace)` |
| EWMA burn-rate core | ❌ | Exponential-weighted d(used%)/dt with alpha=0.4, rate reset on usage drop or window change. — Absent; PORTING D16 flags this pure-math core as the first thing to port. | `app/core/usage/depletion.py:12-59 (EWMAState, ewma_update)` |
| Depletion risk classification | ❌ | Compute depletion risk / burn-rate / safe-usage-percent and classify safe/warning/danger/critical at 0.60/0.80/0.95. — Absent. | `app/core/usage/depletion.py:6-8,62-105 (compute_depletion_risk, classify_risk)` |
| Per-account depletion metrics + exhaustion ETA | ❌ | Per-account depletion metrics incl projected_exhaustion_at and seconds_until_exhaustion within the window. — Absent. | `app/modules/usage/depletion_service.py:68-162 (compute_depletion_for_account)` |
| EWMA state + history-signature memoization | ❌ | Cache EWMA state keyed by (account,limit,window) with a content-signature so dashboard polls skip full history replay (issue #537). — Absent. | `app/modules/usage/depletion_service.py:38-114 (_ewma_states, _history_signature)` |
| Aggregate (worst-account) depletion | ❌ | Fleet depletion = the max-risk account's full metric set (internally consistent). — Absent. | `app/modules/usage/depletion_service.py:165-187 (compute_aggregate_depletion)` |
| Weekly-pool discrete-event simulation | ❌ | Simulate the pooled weekly credits forward through per-account resets to project shortfall, min-remaining, and depletion hours. — Absent. | `app/modules/dashboard/weekly_pace.py:310-367 (_project_weekly_pool)` |
| Working-days/hours schedule fraction | ❌ | Compute expected consumption vs a workday-aware (not flat) linear schedule across the weekly window. — Absent. | `app/modules/dashboard/weekly_pace.py:391-441 (_used_schedule_fraction, _working_duration_ms)` |
| Smoothed remaining credits | ❌ | Average remaining-credits over a recent N-minute smoothing window (same reset/window) to de-noise the pace gap. — Absent. | `app/modules/dashboard/weekly_pace.py:273-307 (_smoothed_remaining_credits)` |
| Pace throttle recommendations | ❌ | Derive pause-for-break-even hours, throttle-to-%, reduce-by-%, pace multiplier, and pro-account-equivalent to cover overspend. — Absent. | `app/modules/dashboard/weekly_pace.py:162-184 (throttle_to_percent, pause_for_break_even_hours, pro_accounts)` |
| Pace status + confidence grading | ❌ | Grade pace behind/on_track/ahead/danger and confidence high/medium/low from sample counts + staleness. — Absent. | `app/modules/dashboard/weekly_pace.py:452-471 (_weekly_pace_status, _confidence)` |
| Model pricing table | ❌ | Per-model per-1M-token input/cached/output rates across ~25 GPT-5.x/codex/image models, incl priority/flex/long-context tiers. — Absent (COMPARISON.md 'Pricing data + cost accounting' = no). | `app/core/usage/pricing.py:90-305 (DEFAULT_PRICING_MODELS, ModelPrice)` |
| Model-alias pricing resolution | ❌ | Glob-pattern, longest-match resolution of a raw model string to a canonical priced model. — Absent (PF has model aliasing for routing, not for pricing). | `app/core/usage/pricing.py:280-342 (DEFAULT_MODEL_ALIASES, resolve_model_alias, get_pricing_for_model)` |
| Per-request cost breakdown | ❌ | Compute input/cached/output/total USD for a request (billable input = input - cached tokens) with rounding. — request_log has cost_usd (migration 0005) but PF computes no cost on the native path; column filled only by the importer. | `app/core/usage/pricing.py:430-499 (calculate_cost_breakdown_from_usage, calculate_costs)` |
| Service-tier / long-context pricing | ❌ | Apply priority/flex/fast tier rates (or a multiplier) and a long-context threshold rate to cost. — Absent; service_tier columns exist in request_log (0005) but aren't priced. | `app/core/usage/pricing.py:345-415 (_effective_rates, _uses_priority_tier, _uses_flex_tier)` |
| Per-request token capture | 🟡 | Persist input/output/cached/reasoning token counts per request into request_logs. — Columns input_tokens/output_tokens/cached_input_tokens/reasoning_tokens exist (migration 0005) and the importer fills them (import.rs); PF's native request path leaves them NULL (COMPARISON.md line 112). | `app/modules/proxy/_service/request_log.py:389-431 (_persist_request_log tokens); app/db/models.py:280-283` |
| Account lifetime usage rollup | ❌ | Fold request-log token/cost sums older than a 24h watermark into a per-account rollup (dedup by max id, exclude warmup kinds), advanced in bounded slices. — Absent; PF request_log grows unbounded with no rollup/watermark (COMPARISON.md 'Data retention' = no). | `app/modules/accounts/usage_rollup.py:313-457 (run_fold_pass, _fold_next_slice); app/db/models.py:170-222` |
| Per-API-key lifetime usage rollup | ❌ | Same fold-watermark rollup of token/cost sums per API key (no dedup, soft-deleted rows included). — Absent (PF has no API-key concept). | `app/modules/accounts/usage_rollup.py:95-127,245-283 (api_key_usage_aggregate_stmt, read_api_key_rollup_state); app/db/models.py:190` |
| Deduped usage aggregate query | ❌ | Collapse duplicate request-log rows sharing (account,request_id,requested_at) to the latest id before summing tokens/cost. — Absent. | `app/modules/accounts/usage_rollup.py:45-92 (deduped_usage_aggregate_stmt)` |
| Windowed usage metrics/cost summary | ❌ | SQL-aggregate token/cost/error metrics over the secondary (7d) window for the usage summary + dashboard. — Absent; PF read_api has no usage-metric aggregation endpoint. | `app/modules/usage/service.py:37-65 (get_usage_summary, aggregate_usage_metrics_since)` |
| Per-API-key limit rules | ❌ | Configurable limit rules per key: limit_type x limit_window x max_value x optional model_filter, tracking current_value + reset_at. — Absent (COMPARISON.md 'Per-API-key usage limits' = no; no api_keys tables in PF schema). | `app/db/models.py:1034-1066 (ApiKeyLimit)` |
| Limit types + windows enums | ❌ | Limit dimensions total/input/output tokens, cost_usd, credits over windows 5h/7d/daily/weekly/monthly. — Absent. | `app/db/models.py:1018-1032 (LimitType, LimitWindow)` |
| Limit reset scheduling | ❌ | Compute next reset and advance-past-elapsed reset for each limit window; background reset scheduler. — Absent. | `app/modules/api_keys/limit_windows.py:8-41 (next_limit_reset, advance_limit_reset); app/modules/api_keys/reset_scheduler.py` |
| Two-phase usage reservation | ❌ | Reserve estimated usage before a request, settle actuals after (or release on failure), with per-limit reservation items + idempotency. — Absent (no ApiKeyUsageReservation tables/flow). | `app/db/models.py:1069-1131 (ApiKeyUsageReservation, ApiKeyUsageReservationItem); app/modules/api_keys/service.py:131-185` |
| Request usage-budget estimation | ❌ | Locally size a request's input tokens (byte proxy, opaque-input/prev-response aware) for reservation admission. — Absent. | `app/modules/proxy/api_key_usage.py:30-78 (estimate_api_key_request_usage)` |
| Per-key enforced model/effort/tier | ❌ | Force a model / reasoning-effort / service-tier per API key (also a usage-shaping control). — Absent. | `app/modules/api_keys/service.py:452-454,291-296 (enforced_model/reasoning_effort/service_tier)` |
| Prometheus /metrics endpoint + registry | ❌ | Expose a Prometheus scrape endpoint over a CollectorRegistry with optional multiprocess aggregation. — Absent (COMPARISON.md 'Prometheus / external metrics endpoint' = no; PORTING C11 LOW). PF has no prometheus dependency. | `app/core/metrics/prometheus.py:45,279-293 (REGISTRY, make_scrape_registry, MULTIPROCESS_MODE)` |
| HTTP request metrics + middleware | 🟡 | requests_total{method,path,status} + request_duration_seconds histogram + active_connections gauge via ASGI middleware with path normalization. — PF emits exactly ONE content-safe tracing::info completion event per request (crates/polyflare-server/src/observability.rs carrying method/path/provider/status/duration_ms) but no Prometheus counters/histograms. | `app/core/metrics/middleware.py:15-60 (MetricsMiddleware, _normalize_path)` |
| Upstream request/transport metrics | ❌ | upstream_requests_total{account_id,status}, upstream_request_duration_seconds, and upstream_transport_decisions_total{downstream,upstream,policy,sticky,status}. — Absent. | `app/core/metrics/prometheus.py:59-75 (upstream_*); app/modules/proxy/_service/observability.py:49-51` |
| rate_limit_hits_total metric | ❌ | Counter of rate-limit hits labeled by type. — Absent (PORTING C11 lists this as a portable-contract metric name). | `app/core/metrics/prometheus.py:99-104 (rate_limit_hits_total)` |
| accounts_total status gauge | ❌ | Gauge of account count by status (active/rate_limited/quota_exceeded/...). — Absent (PF read_api /api/pools computes an active count on demand, not as a metric). | `app/core/metrics/prometheus.py:112-118 (accounts_total)` |
| circuit_breaker_state gauge | ❌ | Per-service circuit-breaker state gauge (0 closed / 1 open / 2 half-open). — Absent. | `app/core/metrics/prometheus.py:105-111 (circuit_breaker_state)` |
| Account pressure-lease metrics | ❌ | account_lease_acquired/released/stale_reclaimed_total, account_inflight_leases gauge, account_cap_rejections_total (in-flight lease accounting). — Absent; PF's AccountSnapshot.in_flight is defaulted 0 and never live-tracked (PORTING C9). | `app/core/metrics/prometheus.py:204-234; app/modules/proxy/load_balancer.py:1847-1873` |
| Bridge continuity metrics family | ❌ | ~13 continuity/bridge counters+histograms (instance_mismatch, owner_forward, durable_recover, reattach, same_account_takeover, first_turn_timeout, drain_recovery_allowed, owner_mismatch, local_rebind, forward_latency, public_contract_error, prompt_cache_locality_miss, soft_local_rebind). — PF has the M3 ownership/watchdog continuity path but emits NO metrics for it (PORTING C11 says these should be emitted under identical names). | `app/core/metrics/prometheus.py:119-191 (bridge_* family)` |
| continuity resolution metrics | ❌ | continuity_owner_resolution_total{surface,source,outcome} + continuity_fail_closed_total{surface,reason}. — Absent. | `app/core/metrics/prometheus.py:192-203 (continuity_*)` |
| Image-route metrics | ❌ | image_requests_total + image_request_duration_seconds labeled by route/model/stream/status/outcome. — Absent (PF has no images route). | `app/core/metrics/prometheus.py:76-87 (image_*)` |
| Proxy phase-latency histogram | ❌ | proxy_phase_latency_seconds{phase,transport,upstream_transport,model_class} recording ttft/response_created/first_upstream_event/gate_wait/bridge_queue_wait phases. — Absent; PF captures only total duration (COMPARISON.md 'Latency percentiles / phase breakdown' = no). | `app/core/metrics/prometheus.py:249-254; app/modules/proxy/_service/request_log.py:35-54,253-292` |
| Prewarm + cache-invalidation metrics | ❌ | http_bridge_prewarm_total, http_bridge_stuck_retire_total, cache_invalidation_bump/poll_failures_total, cap_partition_replicas gauge. — Absent (single-binary PF has no bridge-prewarm/cache-invalidation subsystems). | `app/core/metrics/prometheus.py:243-277 (cap_partition_replicas, http_bridge_*, cache_invalidation_*)` |
| Persisted request-log (full column set) | 🟡 | One fire-and-forget request_logs row per outcome across ~53 columns (ids, model, tokens, cost, latency phases, failure diagnostics, proxy-route, prewarm, service-tier, useragent/ip). — PF persists a deliberately content-SAFE subset (7 base cols in migration 0004 + nullable widened cols in 0005 populated only by the importer); codex-lb's content-full row (useragent/client_ip/error_message/failure_detail) is intentionally excluded. | `app/db/models.py:240-320 (RequestLog); app/modules/proxy/_service/request_log.py:367-465 (_persist_request_log)` |
| Request-log query API + filter options | 🟡 | Paginated request-log listing plus filter-option endpoints (account ids, model+effort options, api keys, statuses). — PF read_api GET /api/requests does limit/offset pagination + total count (read_api.rs requests_handler) but has no filter-options endpoint. | `app/modules/request_logs/api.py; app/modules/request_logs/schemas.py:59-81 (RequestLogsResponse, RequestLogFilterOptionsResponse)` |
| Latency phase columns | 🟡 | Per-request TTFT + response_created + first_upstream_event + gate-wait + bridge-queue-wait latency columns on request_logs. — PF request_log has latency_first_token_ms (migration 0005) filled by the importer only; the native path records solely total duration_ms, and the other phase columns aren't carried. | `app/db/models.py:286-296 (latency_first_token_ms, latency_response_created_ms, latency_first_upstream_event_ms, latency_response_create_gate_wait_ms, latency_bridge_queue_wait_ms)` |
| Failure-diagnostics columns | ❌ | failure_phase/detail/exception_type + upstream_status_code/error_code + bridge_stage per request row. — PF stores status (int) + outcome + error_code only; free-form failure text is content-safety-excluded (migration 0005 comment). | `app/db/models.py:300-305 (failure_phase, failure_detail, failure_exception_type, upstream_status_code, upstream_error_code, bridge_stage)` |
| Upstream-proxy routing columns | ❌ | Per-request egress route mode / pool id / endpoint id / fallback_used / fail_closed_reason. — Absent (PF has no per-account egress proxy; COMPARISON.md 'Per-account egress proxy' = no). | `app/db/models.py:306-311 (upstream_proxy_route_mode/pool_id/endpoint_id/fallback_used/fail_closed_reason)` |
| Prewarm request-log columns | ❌ | prewarm_status/latency/canary_bucket/eligible_reason recorded per request. — Absent (no bridge prewarm in PF). | `app/db/models.py:292-295 (prewarm_status, prewarm_latency_ms, prewarm_canary_bucket, prewarm_eligible_reason)` |
| Useragent grouping + client IP | ❌ | Store useragent, a normalized useragent_group, and client_ip with dedicated indexes for per-client analytics. — Deliberately excluded from PF for content-safety/PII (migration 0005 comment lists useragent/useragent_group/client_ip as not carried). | `app/db/models.py:242-245,273-275 (useragent, useragent_group, client_ip + indexes)` |
| Session threading columns | 🟡 | session_id + session_previous_gap_ms + archive_request_id to thread turns of one conversation. — PF request_log has session_id (migration 0005, importer-filled) but the native path leaves it NULL and there is no previous-gap/archive-id capture. | `app/db/models.py:259,261,296 (session_id, archive_request_id, session_previous_gap_ms)` |
| Service-tier request columns | 🟡 | service_tier + requested_service_tier + actual_service_tier per request row. — Columns exist (migration 0005) and the importer fills them; PF's native path leaves them NULL. | `app/db/models.py:277-279 (service_tier, requested_service_tier, actual_service_tier)` |
| Post-hoc request-log model rewrite | ❌ | Rewrite the request_logs.model field after the fact (e.g. images route) with retry-until-row-appears. — Absent. | `app/modules/proxy/_service/request_log.py:71-143 (rewrite_request_log_model)` |
| Request-kind classification + usage exclusion | 🟡 | Tag rows normal/warmup/limit_warmup and exclude warmup kinds from usage rollups/summaries. — PF request_log has request_kind (migration 0005, importer-filled) but generates no warmup traffic and has no rollup to exclude it from. | `app/db/models.py:64,262-267 (RequestKind, request_kind); app/modules/accounts/usage_rollup.py:33 (_EXCLUDED_REQUEST_KINDS)` |
| Persistence-task tracking + shutdown drain | ❌ | Detached request-log/settlement inserts are tracked and drained at shutdown so final rows aren't lost. — PF inserts the request_log row fire-and-forget (request_log_repo.insert) with no tracked task set or shutdown drain. | `app/modules/proxy/_service/request_log.py:294-365 (drain_persistence_tasks, _track_request_log_task)` |
| Request-log soft-delete tombstone | 🟡 | deleted_at column soft-deletes request rows (excluded from account usage aggregates). — PF request_log has deleted_at (migration 0005) but it is importer-set only; no PF pruning/soft-delete logic writes it (COMPARISON.md 'deleted_at is importer-set only'). | `app/db/models.py:269 (RequestLog.deleted_at)` |
| Audit log persistence | ❌ | Persist admin/security actions (action, actor_ip, JSON details, request_id, indexed timestamp). — Absent (COMPARISON.md 'Audit log / conversation archive' = no). | `app/db/models.py:455-463 (AuditLog); app/modules/audit/repository.py` |
| Audit log query service/API | ❌ | List audit entries filtered by action, paginated, parsing the JSON details back into typed values. — Absent. | `app/modules/audit/service.py:27-55 (AuditLogsService.list_logs)` |
| Reset-epoch surfacing + dashboard countdown | ✅ | Surface each window's reset_at epoch + the durable routing-gate reset_at for live per-window reset countdowns. — PF read_api.rs AccountView exposes reset_at + five_hour/weekly.reset_at; the embedded dashboard renders 1s-ticking countdown pills (COMPARISON.md 'Reset-time epochs + dashboard countdowns' = yes). | `app/modules/dashboard/schemas.py (UsageWindow reset_at); app/modules/usage/schemas.py` |
| Per-window stale flag | ✅ | Flag a usage window as stale when upstream stopped refreshing it, so a last-known value is shown but never reads as live. — PF-original per-window stale flag (usage_windows.rs STALE_AFTER_SECS=1800) surfaced in read_api WindowView.stale; codex-lb has freshness logic in the updater/weekly_pace but not an equivalent surfaced per-window stale marker. | `app/modules/usage/updater.py:1107-1121 (_latest_usage_is_fresh)` |
| Dashboard usage overview payload | 🟡 | Aggregated overview: primary/secondary windows, cost, metrics, previous-period comparison, trends, plus depletion + weekly-pace blocks. — PF dashboard has account/pool/request views + reset countdowns but no cost/trends/comparison/pace dataset (COMPARISON.md 'Rich analytics dashboard' = partial). | `app/modules/dashboard/schemas.py:104-119 (DashboardOverviewResponse, DashboardProjectionsResponse)` |
| Quota-window observations store | ❌ | Periodic per-account remaining%/reset snapshots (primary+secondary, source, confidence) feeding the quota planner/forecast. — Absent; PF keeps usage_history rows but no separate observation/confidence table. | `app/db/models.py:1381-1393 (QuotaWindowObservation)` |
| Rate-limit reset-credit redeem ledger | ❌ | Idempotent (account, redeem_request_id)->credit_id ledger to redeem ChatGPT rate-limit reset credits without double-burning. — Absent (Codex-specific reset-credit feature not ported). | `app/db/models.py:475-515 (ResetCreditRedeemRequest, ResetCreditRedeemClaim); app/modules/rate_limit_reset_credits/` |

## ops-dashboard-admin-config  ·  ✅ 5 🟡 7 ❌ 61

| Feature | PF | PolyFlare status / where | Source |
|---|:--:|---|---|
| Embedded SPA dashboard | ✅ | React/Vite dashboard bundle served from the app process under the web root. — crates/polyflare-server/src/dashboard.rs embeds dashboard/dist via rust-embed and serves /dashboard + /dashboard/{*path} with SPA fallback. | `app/main.py (frontend static mount) + frontend/src/App.tsx` |
| Dashboard navigation sections | 🟡 | Multi-section nav: dashboard, reports, accounts, automations, apis, settings. — PF dashboard (dashboard/src/App.tsx) is a single minimal SPA with only accounts/pools/requests views; no reports/automations/apis/settings pages. | `frontend/src/i18n/locales/en.json (nav.*) + frontend/src/features/*` |
| Internationalization (EN + zh-CN) | ❌ | Full UI translation via i18next with English and Simplified Chinese locale bundles. — No i18n in PF dashboard (dashboard/src has only App.tsx/api.ts/styles.css); strings are hardcoded English. | `frontend/src/i18n/locales/{en,zh-CN}.json` |
| Language switcher + auto-detection | ❌ | Language chosen via querystring/localStorage/navigator, persisted to localStorage, zh-* folded to zh-CN. — Absent; no i18n runtime in PF. | `frontend/src/i18n/index.ts:37-70` |
| Email blur / show-hide toggle | ❌ | Privacy control to blur or reveal account emails in the dashboard. — PF dashboard shows account identifiers with no masking control. | `frontend/src/components/blur-email.tsx + en.json nav.showEmails/hideEmails` |
| Status bar (sync/version/update/live) | ❌ | Footer bar with last-sync time, routing label, running version, live indicator, and update-available banner. — PF dashboard has live countdown pills for resets but no version/update/last-sync status bar. | `frontend/src/i18n/locales/en.json (statusBar.*)` |
| Analytics charts (donut/sparkline/quota bars) | 🟡 | Recharts-based donut, sparkline, and mini quota-bar visualizations lazy-loaded into the dashboard. — PF has account/pool/request tables + countdowns but no charting dataset/components (COMPARISON.md:131). | `frontend/src/components/{donut-chart,sparkline-chart,mini-quota-bar,lazy-recharts}.tsx` |
| Inline account edits from dashboard | ✅ | Edit an account's pool / routing policy / paused state from the UI. — PF PATCH /api/accounts/{id} (write_api.rs:57 patch_account_handler) updates pool/routing_policy/status. | `frontend/src/features/accounts/* + app/modules/accounts/api.py` |
| Password admin login (bcrypt) | ❌ | Dashboard admin authenticates with a bcrypt-hashed password. — PF has ZERO admin auth; all endpoints trust the network boundary. Planned POLYFLARE_ADMIN_TOKEN (write_api.rs:3). | `app/modules/dashboard_auth/service.py:262-289,459-467` |
| First-run password setup + bootstrap-token gating | ❌ | Initial password setup is gated for remote callers by a dashboard bootstrap token (local requests exempt). — No bootstrap/setup flow; PF is unauthenticated. | `app/modules/dashboard_auth/api.py:257-311 + app/core/bootstrap.py:84-144` |
| Auto-generated bootstrap token | ❌ | Server auto-mints and logs a one-time bootstrap token on first run / after password removal. — Absent. | `app/core/bootstrap.py:101-144 (ensure_auto_bootstrap_token)` |
| Password change / remove | ❌ | Authenticated endpoints to change or remove the admin password (remove re-arms bootstrap token). — Absent. | `app/modules/dashboard_auth/api.py:422-488` |
| Password length/byte validation | ❌ | Enforces >=8 chars and <=72 UTF-8 bytes (bcrypt hard limit) with clear 400s. — Absent (no password concept). | `app/modules/dashboard_auth/api.py:212-223` |
| TOTP/2FA enrollment | ❌ | Generate TOTP secret, otpauth URI, and an inline QR SVG data-URI (segno) for authenticator setup. — Absent. | `app/modules/dashboard_auth/service.py:329-359 + app/core/auth/totp.py:20-29` |
| TOTP verify with replay protection | ❌ | 6-digit TOTP verify with ±1 step window and a monotonic last_verified_step to block code replay. — Absent. | `app/core/auth/totp.py:32-56 + service.py:361-403` |
| TOTP disable + require-on-login toggle | ❌ | Disable TOTP (requires a TOTP-verified session) and a totp_required_on_login enforcement flag. — Absent. | `app/modules/dashboard_auth/service.py:405-423 + app/db/models.py:623` |
| Encrypted-at-rest TOTP secret | ❌ | TOTP shared secret stored encrypted via the token encryptor. — PF has token encryption (XChaCha20) for account tokens but no TOTP secret to store. | `app/db/models.py:649 (totp_secret_encrypted) + service.py:358` |
| Stateless encrypted session cookie | ❌ | Dashboard session is an encrypted JSON blob (exp/pw/tv/role/gv) — no server-side session table. — Absent; no session concept. | `app/modules/dashboard_auth/service.py:105-189` |
| Configurable session TTL + loopback long-session | ❌ | Admin-configurable dashboard session TTL with an optional loopback-host long-session override. — Absent. | `app/core/auth/dashboard_session_ttl.py + app/db/models.py:611` |
| RBAC roles + permissions | ❌ | admin vs guest roles mapped to write/read permission sets on a DashboardPrincipal. — Absent; no principal/role model. | `app/core/auth/dashboard_access.py:9-48` |
| Write-access guard (read-only guests) | ❌ | require_dashboard_write_access blocks mutations for read-only (guest) principals. — Absent; PF write endpoints are ungated. | `app/core/auth/dependencies.py:228-235` |
| Guest access mode | ❌ | Optional read-only guest access, with or without a separate guest password, incl. public-guest. — Absent. | `app/modules/dashboard_auth/service.py:273-296 + api.py:314-367` |
| Dashboard auth modes (standard/trusted_header/disabled) | ❌ | Selectable auth mode: password (standard), reverse-proxy SSO header, or fully disabled. — Absent; PF is effectively always 'disabled' with no toggle. | `app/core/auth/dashboard_mode.py:34-91` |
| Trusted reverse-proxy header SSO | ❌ | Accept an upstream Remote-User header only from trusted proxy CIDRs, with forbidden-header hardening. — Absent. | `app/core/auth/dashboard_mode.py:94-125 + settings dashboard_auth_proxy_header` |
| DB-backed login rate limiting | ❌ | 8-attempts/60s limiter on password, guest-password, and TOTP endpoints keyed by client IP. — Absent. | `app/modules/dashboard_auth/service.py:430-448 + core/rate_limiter/db_rate_limiter.py` |
| Auth audit log | ❌ | Records login_success/failed, totp_enabled/disabled with actor IP and method. — Absent; PF emits per-request tracing only, no auth audit events. | `app/core/audit/service.py:41-71` |
| Proxy API-key auth (sk-clb-) | ❌ | Bearer sk-clb- keys validated (sha256 hash), cached with expiry, gate proxy + self-service usage. — Absent; PF ingress is unauthenticated. Planned POLYFLARE_ADMIN_TOKEN gate. | `app/core/auth/dependencies.py:61-128 + auth/api_key_cache.py` |
| api_key_auth toggle + unauth-remote CIDR guard | ❌ | Global api_key_auth_enabled flag; when off, remote callers rejected unless in an allowed socket CIDR. — Absent. | `app/core/auth/dependencies.py:69-85,244-255 + settings proxy_unauthenticated_client_cidrs` |
| IP firewall allowlist CRUD | ❌ | Add/remove/list allowlisted IPs; empty list = allow_all, non-empty = allowlist_active mode. — Absent (COMPARISON.md:140). | `app/modules/firewall/service.py:49-93 + api.py:29-77` |
| Firewall enforcement + IP cache | ❌ | Request-time IP allowlist check via a TTL IP cache with cross-replica invalidation on change. — Absent. | `app/core/middleware/firewall_cache.py + firewall/api.py:54-57 (NAMESPACE_FIREWALL bump)` |
| Firewall trusted-proxy header trust | ❌ | Optionally honor X-Forwarded-For from configured trusted-proxy CIDRs for client-IP resolution. — Absent. | `app/core/config/settings.py:289-293 (firewall_trust_proxy_headers, firewall_trusted_proxy_cidrs)` |
| Hourly data-retention pruner | ❌ | Background pruner deletes aged request_log, usage_history, and additional_usage_history rows. — PF has request_log.deleted_at soft-delete column (importer-set only); anchors/sessions/usage/request_log grow unbounded (COMPARISON.md:164). | `app/core/retention/job.py:22-42 + scheduler.py:60-75` |
| Rollup-watermark-safe pruning | ❌ | Never deletes request_log rows above the usage-rollup fold watermark; skips while fold is catching up. — Absent; no rollup/retention subsystem. | `app/core/retention/job.py:45-94` |
| Latest-sample protection in usage pruning | ❌ | Protects max(recorded_at) per (account,window[,quota_key]) so lifetime totals never shrink. — Absent. | `app/core/retention/job.py:97-161` |
| Batched delete under writer lock | ❌ | Deletes in 10k batches inside the SQLite writer section to avoid long transactions. — Absent. | `app/core/retention/job.py:164-181` |
| Retention config + bounds validation | ❌ | retention_days must be 0 (off) or >=30 (request logs) / >=45 (usage); scheduler auto-enables. — Absent; no retention settings. | `app/core/config/settings.py:236-237,368-380 + retention/scheduler.py:70-75` |
| Scheduled keep-warm automations (cron-like) | ❌ | Per-account daily jobs at HH:MM in a timezone on selected weekdays with a jitter threshold window. — Absent; PF synthetic warming deliberately OFF (COMPARISON.md:165). | `app/modules/automations/service.py:262-401,538-598` |
| Automation ping execution + run cycles | ❌ | Sends a compact 'ping' per account with cross-account failover; tracks run cycles + per-account progress. — Absent. | `app/modules/automations/service.py:699-965,1081-1253` |
| Automations scheduler (leader-gated) | ❌ | Interval loop that dispatches due jobs, guarded by leader election. — Absent. | `app/modules/automations/scheduler.py:30-80` |
| Limit-warmup after quota reset | ❌ | Auto re-warms an account when a real reset_at forward jump is detected post usage-refresh. — Absent. | `app/modules/limit_warmup/service.py:315-430,657-696` |
| Staggered-idle warmup | ❌ | Spreads accounts across the rolling window to pre-open the short window during idle cycles. — Absent. | `app/modules/limit_warmup/service.py:699-793` |
| Warmup windows/thresholds + cheapest-model select | ❌ | Configurable windows (primary/secondary/both), exhausted/idle/min-available thresholds, cooldown, and auto cheapest priced text model. — Absent; no limit_warmup settings. | `app/modules/limit_warmup/service.py:486-508 + app/db/models.py:698-762` |
| Quota-planner warmup engine | ❌ | Mode (off/shadow/dry_run/active), daily count/credit budgets, execution gate, decision ledger, manual warm-now. — Absent. | `app/modules/quota_planner/warmup.py:73-439 + scheduler.py` |
| Auth guardian proactive token refresh | 🟡 | Batched background loop that pre-refreshes account tokens before expiry with jitter + failure backoff. — PF does exp-driven refresh (refresh-at-8-day, per-account singleflight RefreshLocks) but no batch guardian scheduler with jitter/backoff. | `app/core/auth/guardian.py + settings auth_guardian_* (settings.py:184-191)` |
| Per-account egress proxy routing | ❌ | Route an account's upstream traffic through a bound HTTP/HTTPS/SOCKS proxy for IP diversity/anti-ban. — Absent (COMPARISON.md:171); PORTING D13 planned. PF egress is always direct rustls. | `app/core/upstream_proxy/resolver.py:26-73` |
| Proxy route precedence + pool fallbacks | ❌ | Resolution order account-binding > default-pool > direct; pools yield ordered endpoint + fallbacks. — Absent. | `app/core/upstream_proxy/resolver.py:36-73,109-133` |
| Proxy scheme handling + rdns | ❌ | http/https/socks5/socks5h with socks5→socks5h rDNS rewrite in the proxy URL. — Absent. | `app/core/upstream_proxy/types.py:16-22 + resolver.py:15,137-139` |
| Encrypted proxy credentials | ❌ | Proxy endpoint passwords stored encrypted and decrypted at route-resolution time. — Absent. | `app/core/upstream_proxy/resolver.py:136-153` |
| Upstream-proxy admin API | ❌ | CRUD for proxy endpoints/pools/members, default-pool select, per-account binding, and a live endpoint test. — Absent; PF settings API has no proxy management (no settings table at all). | `app/modules/settings/api.py:185-429` |
| Optional proxy-schema tolerance | ❌ | Missing proxy tables/columns are treated as 'not configured' → direct egress (rollout-safe). — N/A; PF has no proxy schema. | `app/core/upstream_proxy/resolver.py:76-90` |
| Model catalog endpoints | ✅ | Content-negotiated model listing across /models, /v1/models, and codex backend shape. — PF catalog.rs serves /models, /v1/models, /backend-api/codex/models from synthetic_models (COMPARISON.md:147). | `app/modules/model_sources/* + app/core/openai/model_registry.py` |
| Live upstream model-catalog fetch | 🟡 | GET {base}/codex/models?client_version with Bearer + chatgpt-account-id parsed into UpstreamModel. — PF serves a hardcoded 5-slug bootstrap floor pinned to one codex version; no live fetch (COMPARISON.md:148, PORTING D15). | `app/core/clients/model_fetcher.py:104-190` |
| Model-refresh scheduler + plan merge | ❌ | Periodic refresh grouping active accounts by plan, cross-account failover, merges same-plan slugs. — Absent; static catalog only. | `app/core/openai/model_refresh_scheduler.py:89-303` |
| Model registry persist + reconcile-from-store | ❌ | Leader persists a registry snapshot; followers reconcile from the store instead of fetching upstream. — Absent (single-binary, no leader/followers). | `app/core/openai/model_refresh_scheduler.py:157-188 + model_registry_store.py` |
| Model-fetch transport recovery | ❌ | On transport error, refreshes the shared HTTP client and retries the model fetch once. — Absent. | `app/core/openai/model_refresh_scheduler.py:318-367` |
| Custom model sources (openai_compatible) | ❌ | BYO relay sources with base_url, encrypted api_key, capabilities, and per-model pricing/metadata CRUD. — Absent; PF has only the two native backends. | `app/modules/model_sources/service.py:47-121,157-182` |
| Leader election | ❌ | Postgres single-row lease (id=1, TTL, ON CONFLICT) electing one scheduler leader; SQLite = always leader. — Absent by design — PF is a single tokio binary (COMPARISON.md:158). | `app/core/scheduling/leader_election.py:16-94` |
| Leader lease renewal | ❌ | Periodic expiry extension of the held leadership lease. — Absent. | `app/core/scheduling/leader_election.py:65-92` |
| Scheduler leader gating | ❌ | Every background scheduler (retention/automations/quota/model-refresh) acquires leadership before running. — Absent; PF runs its loops (usage-refresh, codex-version) unconditionally in-process. | `app/core/retention/scheduler.py:60-62 + automations/scheduler.py:62-63` |
| Cross-replica cache-invalidation poller | ❌ | DB-versioned namespaces (api_key/firewall/settings/model_registry/...) polled 0.5s + bumped on write. — Absent; PF uses in-process cache-generation atomics (account_cache.rs) instead (COMPARISON.md:156). | `app/core/cache/invalidation.py:23-53 + CacheInvalidationPoller` |
| Durable bridge instance ring | ❌ | Multi-instance ownership coordination for HTTP session bridges via an advertised instance ring. — Absent; PF continuity is in-process single-binary (COMPARISON.md:73). | `app/modules/proxy/durable_bridge_coordinator.py + settings http_responses_session_bridge_instance_ring` |
| In-process concurrency coordination | ✅ | Locks + cache generations + observe-before-end ordering to serialize hot-path mutations. — PF has RefreshLocks + cache-generation atomics + observe-before-end ordering (COMPARISON.md:156). | `app/modules/proxy/* locks + account_cache.py` |
| Env-driven settings (prefix + .env files) | 🟡 | Pydantic BaseSettings with CODEX_LB_ prefix reading env plus .env/.env.local files. — PF reads POLYFLARE_* env directly (config.rs from_env) but has no .env file loading. | `app/core/config/settings.py:136-139` |
| DB-backed dashboard settings (hot-applied) | ❌ | Mutable runtime settings persisted in a DashboardSettings row, editable via API and cached. — Absent; PF has no settings table — all config is process-env, fixed at startup. | `app/db/models.py:600-780 + app/modules/settings/api.py:172-532` |
| Settings cache w/ TTL + invalidation | ❌ | 5s-TTL settings cache invalidated locally and across replicas on any settings write. — Absent. | `app/core/config/settings_cache.py:13-53` |
| Config file + hot reload | ❌ | A dedicated config file (beyond env) with live reload of settings. — PF is env-only, no file, no hot reload (COMPARISON.md:172); codex-lb is partial via .env + DB settings. | `app/core/config/settings.py + settings_cache.py` |
| Per-provider upstream base-URL override | ✅ | Override the Codex/auth (and self-hosted relay) upstream base URLs via config. — PF POLYFLARE_UPSTREAM_URL / ANTHROPIC_UPSTREAM_URL / AUTH_URL (config.rs:70-75). | `app/core/config/settings.py:157,175 (upstream_base_url, auth_base_url)` |
| Upstream transport override | ❌ | Force upstream stream transport (http / websocket / auto) and downstream transport policy. — PF is HTTP-SSE only; no WebSocket transport (COMPARISON.md:40). | `app/core/config/settings.py:158-159` |
| Outbound proxy env detection | ❌ | Detects HTTPS_PROXY-style outbound proxy env to set websocket trust_env behavior. — Not modeled as a config feature in PF; reqwest handles egress directly with pinned rustls. | `app/core/config/settings.py:162 + utils/proxy_env.py` |
| PostgreSQL storage option | ❌ | Full Postgres backend (asyncpg engine, pool sizing, dialect-aware upserts). — PF is SQLite/WAL only via sqlx SqlitePool (COMPARISON.md:174). | `app/db/session.py:40-124 + core/cache/invalidation.py (pg_insert vs sqlite_insert)` |
| Dialect-aware migration URL remap | ❌ | Rewrites async driver URLs (aiosqlite/asyncpg) to sync drivers for Alembic runs. — PF uses embedded forward-only sqlx migrations, SQLite-only, no Alembic/dialect remap. | `app/db/migration_url.py:10-13` |
| SQLite pre-migrate backup + integrity check | 🟡 | Rotating pre-migration SQLite backups and a quick/full integrity check on startup. — PF store.rs configures WAL + synchronous but has no pre-migrate backup rotation or startup integrity-check mode. | `app/db/backup.py + sqlite_utils.py + settings.py:152-154` |
| Migration lock + auto-remap | 🟡 | Advisory migration lock (timeout) and Alembic head auto-remap to avoid concurrent/branching migrations. — PF runs embedded forward-only sqlx migrations at open (store.rs); no lock needed single-node, no auto-remap. | `app/db/migration_lock.py + settings.py:155-156` |
