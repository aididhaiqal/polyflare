# better-ccflare feature map → PolyFlare

> Exhaustive feature inventory of better-ccflare, enumerated from the local source at `/Users/wmaididhaiqal/Development/Codex-LoadBalancer/better-ccflare` and
> mapped to PolyFlare as **built / partial / absent**. better-ccflare is an Anthropic-first TypeScript pool; PolyFlare is Codex-first, so a lower 'built' share is expected — many rows are Anthropic-specific machinery (its analytics dashboard, prompt-cache keepalive) that PolyFlare has no equivalent for yet.

**Coverage: ✅ 30 built · 🟡 83 partial · ❌ 246 absent — of 359 features (8% built, 31% built-or-partial).**

**Legend:** ✅ built + wired in PolyFlare · 🟡 partial (scaffolded, inputs inert, or a subset) · ❌ not yet.

> This is a *complete* inventory (features PolyFlare already has are listed too), one level finer than
> [`COMPARISON.md`](COMPARISON.md). For the routing gaps, the porting recipes are in
> [`PORTING-CODEXLB.md`](PORTING-CODEXLB.md).


## providers-translation  ·  ✅ 10 🟡 34 ❌ 49

| Feature | PF | PolyFlare status / where | Source |
|---|:--:|---|---|
| convertAnthropicRequestToOpenAI (request converter) | ❌ | Top-level Anthropic Messages request -> OpenAI Chat Completions request body converter. — PF Format enum has only OpenAIResponses+AnthropicMessages (crates/polyflare-core/src/format.rs); it has NO Chat Completions wire format at all. PF's analog targets Codex Responses, not Chat Completions. | `packages/openai-formats/src/converters.ts:55` |
| Chat-completions param mapping (max_tokens/temperature/top_p/stop/stream) | ❌ | Copies Anthropic scalar params to OpenAI equivalents; stop_sequences->stop; sets stream_options.include_usage when streaming. — No Chat Completions path. On the Anthropic->Responses path PF only maps max_tokens->max_output_tokens + stream passthrough (crates/polyflare-anthropic/src/translate.rs map_request); temperature/top_p/stop/stream_options are dropped. | `packages/openai-formats/src/converters.ts:70-87` |
| Tools -> OpenAI function tools (+removeUriFormat, empty-tools guard) | 🟡 | Maps Anthropic tools to {type:function,function:{...}}, strips format:uri/$schema, omits empty arrays (Qwen rejects []). — PF map_tools (translate.rs:274) maps Anthropic tools to Responses FLAT function shape (no nested function wrapper) but does NOT strip format:uri/$schema and has no empty-tools guard. | `packages/openai-formats/src/converters.ts:106-123` |
| tool_choice mapping (auto/any/none/tool), tools-gated | ❌ | Maps Anthropic tool_choice to OpenAI (any->required, tool->function), only emitted when tools present. — PF's Anthropic->Responses map_request does NOT translate tool_choice in either direction; the field is dropped. (Responses translateToolChoice exists only in ccflare's inverse adapter.) | `packages/openai-formats/src/converters.ts:125-140` |
| System prompt hoist + cache_control preservation | 🟡 | Anthropic top-level system -> OpenAI system message; preserves per-block cache_control (DashScope ephemeral cache). — PF maps system->instructions (translate.rs:98) but passes it through verbatim and drops all cache_control (PF has no prompt-cache machinery). | `packages/openai-formats/src/converters.ts:142-174` |
| Message text-block extraction with cache_control retention | 🟡 | Joins text blocks to string, or keeps array form when any block has cache_control. — PF maps each text block to an input_text/output_text part by role (translate.rs text_part) but never retains cache_control. | `packages/openai-formats/src/converters.ts:186-211` |
| tool_use blocks -> OpenAI tool_calls | 🟡 | Maps Anthropic tool_use to assistant message tool_calls with JSON-stringified arguments. — PF flattens tool_use to a TOP-LEVEL Responses function_call item (translate.rs function_call_item), not Chat Completions message-nested tool_calls. | `packages/openai-formats/src/converters.ts:219-228` |
| thinking blocks -> reasoning_content passthrough | ❌ | Passes Anthropic thinking back as reasoning_content in history (DeepSeek/reasoning providers need it). — PF DROPS thinking blocks entirely on the request path (translate.rs map_messages, drops_thinking_blocks test) — no valid Responses reasoning-item id to replay. | `packages/openai-formats/src/converters.ts:230-235` |
| tool_result -> 'tool' role messages (ordered, image-dropped) | 🟡 | Emits tool_result as separate tool-role messages before text; serializes array content; drops images with placeholder. — PF maps tool_result to a Responses function_call_output item (translate.rs function_call_output_item); array content -> input_text/input_image parts, KEEPS images (no placeholder), no tool-role concept. | `packages/openai-formats/src/converters.ts:237-273` |
| Thinking-only assistant turn preservation | ❌ | Sets content="" so a thinking-only assistant turn stays non-null in OpenAI. — PF drops thinking; a thinking-only assistant turn produces no message item at all. | `packages/openai-formats/src/converters.ts:275-283` |
| convertOpenAIResponseToAnthropic (non-stream response) | ❌ | OpenAI Chat Completions non-streaming response -> Anthropic message (text + tool_use + usage + stop_reason). — No Chat Completions path. PF only translates streaming SSE (translate.rs translate_response_event); it has no non-streaming Chat->Anthropic collapse. | `packages/openai-formats/src/converters.ts:309-370` |
| OpenAI error-shape -> Anthropic error | 🟡 | Maps OpenAI {error:{type,message}} to Anthropic error object in the non-stream converter. — PF build_error_event (translate.rs:629) maps Responses errors to Anthropic error events but only on the SSE path and passes code/type through verbatim. | `packages/openai-formats/src/converters.ts:313-321` |
| stripCacheControlFromOpenAIRequest (GLM-5.1) | ❌ | Post-conversion pass deleting cache_control from all message content blocks for providers rejecting unknown fields. — PF never emits cache_control, so no stripping pass exists or is needed. | `packages/openai-formats/src/converters.ts:39-50` |
| transformStreamingResponse (Chat SSE -> Anthropic SSE) | ❌ | Stateful OpenAI Chat Completions SSE -> Anthropic Messages SSE transform (message_start/content_block_*/message_delta/stop). — This is Chat Completions SSE. PF's SSE translator (translate.rs AnthropicToResponses.translate_response_event + translate_stream.rs) consumes Codex RESPONSES SSE, a different event vocabulary. | `packages/openai-formats/src/stream.ts:160` |
| Streaming tool-call arg buffering (qwen-code parser) | ❌ | Buffers ALL tool-call argument chunks, emits one complete input_json_delta at [DONE]/flush (handles incremental DashScope chunks). — PF forwards each Responses input_json_delta 1:1 as it arrives (translate.rs on_function_call_arguments_delta) — no accumulate-and-emit-at-end buffering. | `packages/openai-formats/src/stream.ts:37-67,436-447` |
| message_start + ping emission on first chunk | 🟡 | Emits Anthropic message_start then a ping event on the first OpenAI chunk. — PF emits message_start once on response.created (translate.rs on_response_started) but never emits a ping keepalive event. | `packages/openai-formats/src/stream.ts:314-344` |
| reasoning_content -> thinking block (streaming) | ✅ | Maps streamed reasoning_content deltas to Anthropic thinking blocks with monotonic block index; coexists with content. — PF on_reasoning_delta maps response.reasoning_summary_text.delta/reasoning_text.delta -> thinking_delta with its own monotonic next_block_index (translate.rs:508). | `packages/openai-formats/src/stream.ts:449-507` |
| Monotonic block index + text/thinking/tool close-before-open ordering | 🟡 | Assigns Anthropic content-block indices via nextBlockIndex and closes prior block before opening next across text/thinking/tool transitions. — PF assigns a monotonic next_block_index per opened item and de-dupes closes via a per-item stopped flag (translate.rs BlockState), but block open/close is driven by discrete Responses output_item/content_part events, not by delta-type transitions. | `packages/openai-formats/src/stream.ts:366-572` |
| Cache-token extraction from prompt_tokens_details (streaming) | 🟡 | Reads cache_creation_input_tokens/cached_tokens from Qwen/DashScope usage details into Anthropic usage. — PF map_usage_from_openai reads input_tokens_details.cached_tokens->cache_read_input_tokens and output_tokens_details.reasoning_tokens->thinking_tokens; it never populates cache_creation_input_tokens. | `packages/openai-formats/src/stream.ts:298-311` |
| Tool-call index/length bounds guards | ❌ | Rejects tool-call indices >=100 or argument chunks > 1MB during streaming. — PF has no per-tool-call index/length guards in the SSE translator. | `packages/openai-formats/src/stream.ts:354-364,439-444` |
| Stream-without-[DONE] flush recovery | 🟡 | On truncated stream, emits buffered tool JSON + content_block_stop/message_delta/message_stop so client gets valid response. — PF's TranslatingStream ends when the inner stream ends (translate_stream.rs poll_next None) but does NOT synthesize terminal Anthropic events if the upstream cuts off before response.completed. | `packages/openai-formats/src/stream.ts:581-651` |
| sanitizeHeaders (strip x-ratelimit-/openai- headers) | 🟡 | Removes provider-specific response headers, forces content-type, keeps the rest. — PF sanitizes/rebuilds Codex response headers in the codex executor and sets text/event-stream on the translated stream (translate_stream.rs), but with a different drop-list. | `packages/openai-formats/src/stream.ts:10-31` |
| repairTruncatedToolJson (suffix repair) | ❌ | Repairs mid-stream-truncated tool-call JSON by trying closing-suffix candidates. — PF has no truncated-JSON repair (it forwards partial_json verbatim and never reassembles/validates the full object). | `packages/openai-formats/src/utils.ts:10-27` |
| removeUriFormat (schema sanitizer) | ❌ | Recursively strips format:uri and $schema from tool JSON schemas some providers reject. — PF forwards tool input_schema verbatim as Responses parameters (translate.rs map_tools) with no schema sanitization. | `packages/openai-formats/src/utils.ts:32-53` |
| mapOpenAIFinishReason (finish_reason -> stop_reason) | 🟡 | Maps stop->end_turn, length->max_tokens, tool_calls->tool_use, content_filter->end_turn. — PF maps Responses status->stop_reason (completed->end_turn, incomplete->max_tokens; translate.rs on_response_completed) but has NO tool_use/max_tokens finish-reason table and never sets tool_use stop_reason. | `packages/openai-formats/src/utils.ts:58-72` |
| convertAnthropicPathToOpenAI (path rewrite) | ❌ | Rewrites /v1/messages -> /v1/chat/completions for OpenAI-compatible upstreams. — PF has no Chat Completions upstream path; ingress routes are fixed axum routes (crates/polyflare-server/src/app.rs). | `packages/openai-formats/src/utils.ts:77-86` |
| Reasoning-effort 6-level scale + rank (minimal..max) | 🟡 | Defines ordered effort values minimal/low/medium/high/xhigh/max with numeric ranks. — PF recognizes high/medium/low/minimal via tier_from_effort (ingress.rs:46) but has no ranked 6-level enum, no xhigh/max, and no validation. | `packages/openai-formats/src/reasoning.ts:9-26` |
| Per-family Claude effort ceilings (haiku capped low/medium) | ❌ | CLAUDE_REASONING_EFFORTS caps each Claude family's supported efforts. — PF hardcodes one fixed effort per synthetic model in the alias table (alias.rs: opus->high, sonnet->medium, haiku->low) with no per-family ceiling table. | `packages/openai-formats/src/reasoning.ts:28-36` |
| Per-target-model effort support tables (gpt-5*/codex/mini) | ❌ | TARGET_REASONING_EFFORTS + gpt-5 prefix rule define which efforts each target model accepts. — PF has no per-target-model effort capability table; it injects the alias's fixed effort unconditionally. | `packages/openai-formats/src/reasoning.ts:38-66` |
| resolveReasoningEffort (clamp-to-nearest + downgrade log) | ❌ | Clamps requested effort to nearest supported for the target, records downgrades, clamps up to model minimum. — PF does NO effort clamping/downgrade. Aliased path writes translated_body.reasoning={effort} verbatim from the alias (ingress.rs:704-709); native /responses path forwards the client's effort byte-verbatim. | `packages/openai-formats/src/reasoning.ts:80-151` |
| validateReasoningEffort (+ValidationError) | ❌ | Validates effort is one of the allowed values, throwing ValidationError otherwise. — PF performs no reasoning.effort validation. | `packages/openai-formats/src/reasoning.ts:88-97,153-158` |
| handleResponsesRequest (Codex CLI /v1/responses -> Anthropic backend) | ❌ | Full adapter: parse Responses request, translate to Anthropic, proxy to /v1/messages, translate response back to Responses. — This is the INVERSE of PF's built direction (M4c). PF's /responses ingress forwards verbatim to a CODEX backend (ingress.rs responses_handler_impl); it never translates a Responses request into an Anthropic-backend call. | `packages/openai-responses-adapter/src/handler.ts:10` |
| Request-body decompression (zstd/gzip/deflate) | ❌ | Manually decompresses Codex CLI's compressed request bodies before parsing. — PF's native /responses path forwards raw compressed bytes verbatim to Codex (ingress.rs raw_body) so it never decompresses; the aliased path assumes JSON. No zstd/gzip handling exists. | `packages/openai-responses-adapter/src/handler.ts:18-41` |
| input string->array normalization | ❌ | Normalizes a plain-string Responses input into a single user message item. — Belongs to the Responses-ingress-translation direction PF doesn't implement (M4c). PF's parse_inbound only reads scalars/shape off the verbatim body. | `packages/openai-responses-adapter/src/handler.ts:69-80` |
| mapGptModelToClaudeFamily (*-pro/-mini/-nano/gpt-5*) | ❌ | Maps OpenAI model names to Claude family aliases so Codex-CLI traffic resolves per-account mappings. — PF's alias table maps the OPPOSITE direction (claude-* -> gpt-5.6-sol/terra/luna; alias.rs synthetic_models). No gpt->claude family inference. | `packages/openai-responses-adapter/src/request-translator.ts:28-35` |
| translateRequestToAnthropic (Responses input -> Anthropic messages) | ❌ | Converts Responses input items (message/function_call/output/custom_tool_call) into Anthropic messages. — Inverse direction (M4c, deferred). PF does Anthropic->Responses request mapping only (translate.rs map_request). | `packages/openai-responses-adapter/src/request-translator.ts:135` |
| Responses content-item translation (input_text/image/refusal/file_id) | 🟡 | Maps Responses content parts to Anthropic content, incl. data-URL/url images and file_id/omitted placeholders. — PF handles the reverse (image_part maps Anthropic image base64/url -> Responses input_image, translate.rs:192); it has no refusal or file_id handling and doesn't consume Responses content parts. | `packages/openai-responses-adapter/src/request-translator.ts:74-118` |
| developer-role -> system merge | ❌ | Extracts Codex CLI's developer-role message text and merges it (+instructions) into the Anthropic system prompt. — PF's map_request copies system->instructions but has no developer-role extraction/merge; on the codex-side convertMessage direction PF also lacks it. | `packages/openai-responses-adapter/src/request-translator.ts:146-155,200-205` |
| mergeConsecutiveSameRole | ❌ | Coalesces adjacent same-role Anthropic messages after flattening tool items. — PF does not merge consecutive same-role messages in either translation direction. | `packages/openai-responses-adapter/src/request-translator.ts:120-133` |
| translateTools (Responses -> Anthropic, skip built-ins) | 🟡 | Maps Responses function tools to Anthropic tools; warns+skips built-in web_search/code_interpreter/file_search. — PF map_tools does the reverse (Anthropic->Responses) and does NOT skip built-in tool types (it maps every tool as function). | `packages/openai-responses-adapter/src/request-translator.ts:45-59` |
| translateAnthropicResponseToResponses (non-stream) | ❌ | Anthropic non-streaming message -> Responses response with output items + usage totals. — Inverse, non-streaming (M4c). PF has no non-streaming response translation in any direction. | `packages/openai-responses-adapter/src/response-translator.ts:8` |
| translateAnthropicStreamToResponses (Anthropic SSE -> Responses SSE) | ❌ | Stateful Anthropic Messages SSE -> OpenAI Responses SSE translator with sequence numbers and output-item lifecycle. — This is exactly the inverse of PF's AnthropicToResponses.translate_response_event. PF ships Responses-SSE->Anthropic-SSE; the Anthropic-SSE->Responses-SSE direction is M4c-deferred. | `packages/openai-responses-adapter/src/stream-translator.ts:402` |
| sequence_number injection on Responses events | ❌ | Attaches a monotonically increasing sequence_number to every emitted Responses SSE event. — PF emits Anthropic events (no sequence_number); its test asserts sequence_number is absent (translate.rs response_created_emits_message_start_once). | `packages/openai-responses-adapter/src/stream-translator.ts:24-37` |
| content_block -> output_item.added/content_part.added lifecycle | ❌ | Maps Anthropic content_block_start to Responses output_item.added + content_part.added (text) or function_call added. — PF implements the reverse lifecycle (Responses output_item.added/content_part.added -> Anthropic content_block_start; translate.rs on_output_item_added/on_content_part_added). | `packages/openai-responses-adapter/src/stream-translator.ts:109-177` |
| Thinking-block output-slot skipping | ❌ | Skips allocating a Responses output_index for Anthropic thinking blocks to keep contiguous indices. — Reverse-direction concern; PF instead OPENS a thinking block (content_block_start type:thinking) for Responses reasoning items (translate.rs on_output_item_added reasoning arm). | `packages/openai-responses-adapter/src/stream-translator.ts:113-118` |
| Per-request stateful TextDecoder + \n\n SSE framing | ✅ | Uses a per-request TextDecoder and buffers SSE events split on double-newline with remainder carry. — PF's TranslatingStream carries a per-turn line_buf, splits on \n, strips \r, and reassembles data: lines split across chunks (translate_stream.rs feed_chunk; test reassembles_a_data_line_split_across_two_chunks). | `packages/openai-responses-adapter/src/stream-translator.ts:411-461` |
| Session-identity preservation into metadata.user_id | 🟡 | Carries prompt_cache_key / session_id header into Anthropic metadata.user_id for session accounting. — PF preserves prompt_cache_key verbatim on the native path and uses it for soft session affinity (session_key.rs) + fingerprint conversation_key (codex_headers.rs conversation_key); it does not synthesize a metadata.user_id. | `packages/openai-responses-adapter/src/handler.ts:95-107` |
| Synthetic /v1/messages request build (anthropic-version, exclude oauth) | ❌ | Builds an internal Anthropic request: sets anthropic-version, forces x-...-exclude-providers=anthropic-oauth to protect Claude OAuth accounts. — PF routes aliased traffic to Codex accounts directly via the provider filter (ingress.rs filter_by_provider_and_pool); no synthetic re-injected request or oauth-account exclusion header. | `packages/openai-responses-adapter/src/handler.ts:109-128` |
| Non-200 Anthropic -> OpenAI error-shape translation | 🟡 | Converts upstream Anthropic error JSON into OpenAI {error:{message,type,code}} with status passthrough. — PF maps Codex/Responses errors to Anthropic error events (translate.rs build_error_event) — the opposite target shape; no Anthropic->OpenAI error mapping. | `packages/openai-responses-adapter/src/handler.ts:164-204` |
| CodexProvider.convertToCodexFormat (Anthropic -> Codex Responses) | ✅ | Core Anthropic Messages request -> Codex /responses request builder (model, input, tools, reasoning, store:false, stream:true). — PF's map_request (translate.rs) is the direct analog for the aliased path; the native /responses path needs no conversion (verbatim Codex bytes). | `packages/providers/src/providers/codex/provider.ts:1012` |
| Codex default model map (haiku->gpt-5.4-mini, sonnet/opus->gpt-5.3-codex) | 🟡 | Fallback Anthropic-family -> Codex-model mapping when no account mapping matches. — PF's alias table maps opus/sonnet/haiku -> gpt-5.6-sol/terra/luna (alias.rs), different targets, config-in-code, marked U2-speculative in docs/COMPARISON.md. | `packages/providers/src/providers/codex/provider.ts:77-81,606-619` |
| convertMessage (roles, developer->system, string content by role) | ✅ | Maps each Anthropic message to Codex items; developer->system; string content becomes input_text (user) or output_text (assistant). — PF map_messages + text_part reproduces the role-based input_text/output_text split (translate.rs:113,181); PF has no explicit developer->system remap though (developer text goes through role passthrough). | `packages/providers/src/providers/codex/provider.ts:633-707` |
| tool_use/tool_result flattening to top-level Codex items | ✅ | Flattens Anthropic tool_use->function_call and tool_result->function_call_output as sibling top-level input items. — PF flattens identically via function_call_item / function_call_output_item (translate.rs:210-269), including the 'tool_use-only message emits no empty message wrapper' behavior (tested). | `packages/providers/src/providers/codex/provider.ts:667-707` |
| sanitizeToolUseInput (Read.pages / WebSearch domains) | ❌ | Per-tool argument sanitization: strips empty Read.pages, cleans WebSearch allowed_domains, drops blocked_domains. — PF forwards tool arguments verbatim (function_call_item JSON-stringifies input as-is); no per-tool-name argument sanitization. | `packages/providers/src/providers/codex/provider.ts:709-747` |
| sanitizeToolUsePartialJson (streamed args) | ❌ | Applies the same per-tool sanitizer to streamed partial tool JSON on the response side. — PF passes streamed input_json_delta through untouched (translate.rs on_function_call_arguments_delta). | `packages/providers/src/providers/codex/provider.ts:757-770` |
| Skill-tool continuation injection | ❌ | After a Skill tool output at conversation end, injects a synthetic user message telling the model to continue. — PF injects no synthetic continuation messages; conversation shaping is untouched. | `packages/providers/src/providers/codex/provider.ts:1027-1058` |
| reasoning effort injection + default medium | 🟡 | Resolves reasoning effort (via resolveReasoningEffort) into Codex request; defaults to medium. — PF aliased path sets reasoning={effort} from the fixed alias value with no resolver/default (ingress.rs:704); native path forwards client effort verbatim. | `packages/providers/src/providers/codex/provider.ts:1072-1094` |
| store:false + stream:true forcing on upstream | 🟡 | Always sends store:false and stream:true to Codex; non-stream clients handled on response side. — PF native path forwards whatever the Codex client sent (verbatim). PF's aliased map_request passes stream through but does NOT force store:false — a potential gap vs Codex requirements. | `packages/providers/src/providers/codex/provider.ts:1086-1094` |
| prompt_cache_key derivation (conversation vs session mode) | 🟡 | Derives an OpenAI prompt_cache_key by hashing session_id (+instructions+first item), with conversation/session modes and OpenAI-host gating. — PF derives a conversation_key from prompt_cache_key for deterministic fingerprint ids (codex_headers.rs conversation_key) and soft session affinity (session_key.rs), but does NOT inject/rewrite a prompt_cache_key into the request. | `packages/providers/src/providers/codex/provider.ts:936-1010` |
| isOpenAiPromptCacheEndpoint gating | ❌ | Only attaches prompt_cache_key when the account endpoint resolves to chatgpt.com/api.openai.com. — PF injects no prompt_cache_key so there is no host-gating; endpoint override is env-driven only. | `packages/providers/src/providers/codex/provider.ts:51,276-299` |
| Codex prepareHeaders (strip + set Version/Openai-Beta/UA/originator) | ✅ | Strips client auth/anthropic headers; sets Bearer, Version, Openai-Beta: responses=experimental, User-Agent, originator: codex_cli_rs. — PF synthesizes a fuller codex identity set (UA+originator+Version + session/thread/turn/window ids + x-codex-turn-metadata) in codex_headers.rs, capture-verified vs codex-cli 0.144.4; native path forwards the real client's headers. | `packages/providers/src/providers/codex/provider.ts:410-431` |
| CODEX_USER_AGENT constant | ✅ | Hardcoded codex-cli/0.144.4 (Windows 10.0.26100; x64) User-Agent. — PF improves on this: codex_user_agent(version) live-resolves the codex version (GitHub/npm cache) and uses real os_info for os/arch, with a headless 'unknown' terminal token (codex_headers.rs:91). | `packages/providers/src/providers/codex/provider.ts:52` |
| transformStreamingResponse (Codex Responses SSE -> Anthropic SSE) | ✅ | Stateful Codex Responses SSE -> Anthropic Messages SSE translator (message lifecycle, tool blocks, usage, stop_reason). — PF's AnthropicToResponses.translate_response_event + wrap_translating_stream is the direct analog and is used for the aliased /v1/messages->Codex path (translate.rs, translate_stream.rs). | `packages/providers/src/providers/codex/provider.ts:1309` |
| Codex event mapping (response.created/output_item/content_part/delta/done/completed) | ✅ | Maps each Codex Responses SSE event to Anthropic events incl. tool_use blocks and terminal message_delta/message_stop. — PF covers the same event set (response.created/in_progress, output_item.added, content_part.added, output_text.delta, function_call_arguments.delta, reasoning_*.delta, *.done family, completed/incomplete, failed, error) in translate.rs. | `packages/providers/src/providers/codex/provider.ts:1620-1889` |
| tool_use stop_reason detection (sawToolUse) | ❌ | Tracks whether a tool call was emitted and sets stop_reason=tool_use accordingly on message_delta. — PF only maps Responses status->end_turn/max_tokens (translate.rs on_response_completed); it never sets stop_reason=tool_use, so tool-ending turns are mislabeled end_turn. | `packages/providers/src/providers/codex/provider.ts:1487,1866` |
| context_window injection into message_delta | ❌ | Injects a context_window block (current_usage + context_window_size) into the terminal message_delta for the client's context gauge. — PF does not synthesize a context_window block on message_delta. | `packages/providers/src/providers/codex/provider.ts:772-813,1876-1878` |
| MODEL_CONTEXT_WINDOWS + longest-prefix lookup | ❌ | Static per-model context-window sizes with exact + longest-prefix (dated-variant) resolution. — PF has no context-window size table (it serves a bootstrap model catalog but no per-model context windows for the gauge). | `packages/providers/src/providers/codex/provider.ts:87-118` |
| transformSseResponseToJson (non-stream collapse) | ❌ | For non-streaming clients, collapses the Codex SSE stream into a single Anthropic JSON message (content, usage, stop_reason, error->HTTP status). — PF's translated path always emits SSE (wrap_translating_stream); it has no non-streaming collapse, so a stream:false aliased request still gets SSE. | `packages/providers/src/providers/codex/provider.ts:1111-1307` |
| Codex error normalization + HTTP-status mapping | 🟡 | normalizeCodexStreamError/toAnthropicErrorPayload/httpStatusForAnthropicErrorPayload map Codex error type/code/status to Anthropic error type + HTTP status (429/529/400/401/403). — PF build_error_event passes the Codex code/type string through verbatim as Anthropic error.type (translate.rs:629) with no rich type-normalization or HTTP-status mapping table. | `packages/providers/src/providers/codex/provider.ts:1510-1618` |
| Codex parseRateLimit (x-codex-*-reset-at, min reset, 429) | 🟡 | Parses primary/secondary/legacy Codex reset headers, uses soonest reset, flags 429. — PF parses Codex usage/reset headers for window tracking + countdown, but per docs/COMPARISON.md its cooldown/error writeback is inert (rate-limit state not written from failures). | `packages/providers/src/providers/codex/provider.ts:568-593` |
| processResponse SSE-content-type recovery probe | 🟡 | Detects Codex 200 responses lacking event-stream content-type by probing for a leading 'event:' and transforms anyway. — PF's codex executor handles SSE, but this specific 'successful-but-missing-content-type' recovery probe is a ccflare robustness feature not mirrored in PF. | `packages/providers/src/providers/codex/provider.ts:502-566` |
| requestStreamById correlation map + TTL sweep | ❌ | Correlates the client's stream flag across request/response via an internal map with 30s TTL sweep. — PF threads stream intent directly through the turn (facts.effort/ctx in ingress.rs) rather than via an out-of-band id->stream map. | `packages/providers/src/providers/codex/provider.ts:307-319,452-521` |
| count_tokens synthetic endpoint (local estimate) | ❌ | Handles /v1/messages/count_tokens with a local char/3 token estimate over prompt material (Codex has no real endpoint). — PF has NO count_tokens support anywhere (no matches in crates/ or docs); the endpoint is unimplemented. | `packages/providers/src/providers/codex/provider.ts:815-934` |
| AnthropicProvider transformRequestBody (model mapping) | 🟡 | Rewrites the request model via per-account model_mappings before relaying to Anthropic. — PF's native Anthropic path forwards the body unchanged (ingress.rs messages_handler_native); model aliasing happens at ingress via lookup_alias, not per-account model_mappings. | `packages/providers/src/providers/anthropic/provider.ts:260-270` |
| AnthropicProvider prepareHeaders (oauth-beta injection) | 🟡 | Strips client auth, sets Bearer, injects anthropic-beta: oauth-2025-04-20 for OAuth accounts, else x-api-key. — PF's Anthropic executor sets auth from the selected account; per docs/COMPARISON.md Anthropic accounts use the stored access_token as-is (Task 7) and the oauth-beta header handling is not confirmed. | `packages/providers/src/providers/anthropic/provider.ts:295-334` |
| transformStreamToOpenAIFormat (add finish_reason) | ❌ | Injects OpenAI-style finish_reason alongside Anthropic stop_reason in the SSE stream for OpenAI clients; skipped for native Anthropic clients. — PF's native Anthropic path relays the stream unchanged (no finish_reason dual-emit). | `packages/providers/src/providers/anthropic/provider.ts:472-581` |
| Anthropic credit/overage classifiers (out_of_credits / extra_usage_exhausted) | ❌ | Detects model/beta-scoped credit depletion (header) and extra-usage-exhausted (400) to fail over without benching the account. — PF has no Anthropic credit/overage classification (no failover-not-bench signal); Anthropic-side failure writeback is inert per docs/COMPARISON.md. | `packages/providers/src/providers/anthropic/provider.ts:58-93` |
| AnthropicProvider parseRateLimit (unified headers + 429/529 clamp) | 🟡 | Parses unified-status/reset/remaining headers, handles 529 overload, Retry-After, x-ratelimit-reset, clamps reset to 24h. — PF reads Anthropic reset epochs for countdowns but does not write cooldown/error state from failures (inert, per docs/COMPARISON.md); no 24h clamp / Retry-After parsing. | `packages/providers/src/providers/anthropic/provider.ts:336-464` |
| AnthropicProvider extractUsageInfo (SSE + JSON usage/cost) | 🟡 | Extracts input/output/cache tokens + billing-cost from streaming message_start and non-stream JSON. — PF polls /wham usage windows but does not extract per-request token/cost from response bodies on the native path (docs/COMPARISON.md: native path fills only method/path/provider/status/duration). | `packages/providers/src/providers/anthropic/provider.ts:628-833` |
| OpenAICompatibleProvider (Anthropic<->Chat relay) | ❌ | Full generic OpenAI-compatible relay: buildUrl path-convert, Bearer auth, request->OpenAI, response->Anthropic (JSON+SSE), pricing/usage. — PF has no OpenAI-compatible / BYO-relay provider; only Codex + Anthropic executors exist (docs/COMPARISON.md marks Chat Completions ingress and local providers as absent). | `packages/providers/src/providers/openai/provider.ts:21` |
| DashScope Alibaba cache_control injection | ❌ | Injects ephemeral cache_control on system + last-2 messages for Qwen models on DashScope/OpenCode endpoints. — PF has no provider-specific prompt-cache injection (no DashScope path, no cache_control emission). | `packages/providers/src/providers/openai/provider.ts:347-439` |
| DashScope enable_thinking injection | ❌ | Adds enable_thinking:true for reasoning models on DashScope so they emit reasoning_content. — PF has no enable_thinking / DashScope reasoning-toggle logic. | `packages/providers/src/providers/openai/provider.ts:446-477` |
| OpenAI-compatible cost table + extractUsageInfo | ❌ | calculateCost pricing table + prompt/completion/cache token extraction for OpenAI-compatible responses. — PF computes no per-request cost on the native path (docs/COMPARISON.md: pricing/cost accounting absent). | `packages/providers/src/providers/openai/provider.ts:215-327` |
| AnthropicCompatibleProvider + path-prefix dedup | 🟡 | Config-driven generic Anthropic-shaped upstream (baseUrl/authHeader/authType) with /v1 path-prefix deduplication. — PF supports an Anthropic base-URL override (env) but not the full config-driven auth-header/type abstraction or /v1 path dedup (anthropic-compatible/provider.ts:40). | `packages/providers/src/providers/anthropic-compatible/provider.ts:23` |
| getModelFamily (fable/opus/haiku/sonnet substring) | 🟡 | Ordered case-insensitive substring detection of Claude model family from a model id. — PF's lookup_alias does ordered case-insensitive substring matching on opus/sonnet/haiku needles (alias.rs:76) but has no 'fable' family and returns a routing target, not a family label. | `packages/core/src/model-mappings.ts:20-30` |
| parseModelMappings + getModelMappings (per-account, env, legacy) | ❌ | Parses per-account JSON model_mappings (string\|string[]) merged with env override and legacy custom_endpoint mappings. — PF's model aliasing is a hardcoded code table (alias.rs synthetic_models); there are no per-account JSON model mappings or env-override merging. | `packages/core/src/model-mappings.ts:97-188` |
| getModelList + model-fallback cycling | ❌ | Returns ordered [primary,...fallbacks] per family/model (merging model_fallbacks) so the balancer can try fallbacks in order. — PF has NO model-fallback cycling; lookup_alias yields a single target_model with no ordered fallback list. | `packages/core/src/model-mappings.ts:167-250,319-337` |
| mapModelName / transformRequestBodyModel(Force) | 🟡 | Resolves the first mapped model and rewrites the request body model (generic + force-all variants). — PF's aliased path rewrites translated_body[model]=alias.target_model (ingress.rs:703); there is no force-all-models variant and no per-account mapping resolution. | `packages/core/src/model-mappings.ts:256-271;packages/providers/src/utils/model-mapping.ts:99-186` |
| Claude model catalog constants (CLAUDE_MODEL_IDS, LATEST_*) | 🟡 | Central Claude model id catalog + LATEST_OPUS/SONNET/HAIKU/FABLE aliases used across mapping/adapters. — PF hardcodes three synthetic Claude ids (claude-opus-4-1/sonnet-4-5/haiku-4-5) in alias.rs/catalog.rs; no broad dated-model catalog or LATEST_* alias layer. | `packages/core/src/models.ts:7-71` |
| Model-name validation (isValidClaudeModel / allowed-models message) | ❌ | Validates inbound model names against family patterns and produces an allowed-models error message. — PF does not validate inbound model names; a non-aliased model simply takes the native path (ingress.rs messages_route). | `packages/core/src/model-mappings.ts:39-62` |
| Content-negotiated model catalog endpoints | ✅ | Serves /v1/models (OpenAI list vs Codex catalog by client_version), /models, /backend-api/codex/models with merged real+synthetic models. — PF catalog.rs mirrors this: v1_models_handler negotiates OpenAI-list vs Codex-catalog on client_version, /models + /backend-api/codex/models always Codex-shape, merging a bootstrap floor + synthetic aliases. | `packages/providers/src/providers/codex/provider.ts;packages/core/src/model-mappings.ts` |
| Live upstream Codex model-catalog fetch/merge | 🟡 | On-demand Codex request eliciting model/usage metadata to keep the served catalog live. — PF serves a hardcoded 5-slug bootstrap floor + synthetic aliases (catalog.rs codex_bootstrap); live upstream catalog fetch/merge is a documented TODO (docs/PORTING-CODEXLB.md D15). | `packages/providers/src/providers/codex/on-demand-fetch.ts:38` |
| Translator registry / N×M (from,to) dispatch | 🟡 | ccflare wires each provider's transform directly (no central translator registry); pairing is implicit per-provider. — PF actually has a MORE explicit design here: a TranslatorRegistry keyed by (Format,Format) storing per-turn factories (crates/polyflare-core/src/translate.rs), though ingress currently constructs AnthropicToResponses directly rather than dispatching through the registry. | `packages/providers/src/registry.ts` |

## cache-ratelimit  ·  ✅ 1 🟡 9 ❌ 37

| Feature | PF | PolyFlare status / where | Source |
|---|:--:|---|---|
| prompt_cache_key derivation (conversation mode, default) | 🟡 | Attaches an OpenAI prompt_cache_key = 'ccflare-convo-'+sha256(sessionId \0 instructions \0 JSON(input[0]))[:48] to translated Anthropic->Codex requests so one conversation lands on one warm cache machine. — PF forwards the real Codex client's own prompt_cache_key BYTE-VERBATIM on the native /responses path (covered) and USES an inbound prompt_cache_key as an ownership/session-affinity signal (session_key.rs:117-154; codex_headers.rs conversation_key:131-141). But the translated aliased path (messages_handler_codex_aliased, ingress.rs:694-732) builds a fresh Codex body under NoopContinuity + RequestCtx::default() and never DERIVES/injects a convo cache key -> aliased Claude traffic gets no cache affinity. Exactly COMPARISON.md gap #5 / PORTING adoption brief #1. | `packages/providers/src/providers/codex/provider.ts:975-1010` |
| Session-mode cache key (coarse per-session) | ❌ | CCFLARE_CODEX_CACHE_KEY_MODE=session (or empty input array) yields 'ccflare-session-'+sha256(sessionId)[:48], one key for the whole session. — PF has no configurable cache-key mode and derives no key on the translated path. On native it forwards whatever mode/key the client already set. | `packages/providers/src/providers/codex/provider.ts:987-995` |
| Anti-thrash per-conversation partitioning rationale | ❌ | Keys per-conversation not per-session so a 170-conversation, 5-min subagent fan-out over one session_id doesn't funnel onto one cache machine and blow OpenAI's ~15 req/min-per-key bound. — PF's session_key does the INVERSE for ownership strength: it appends prompt_cache_key to the session id to ISOLATE threads and harden the pin (session_key.rs:135-137), not to spread cache load. No fan-out/rate anti-thrash logic exists. | `packages/providers/src/providers/codex/provider.ts:957-973` |
| Host gating for prompt_cache_key | ❌ | Only attaches prompt_cache_key when the account host is chatgpt.com / api.openai.com; a custom/self-hosted endpoint may 400 on the unknown field so it is omitted. — PF never synthesizes the field on the translated path so no host gate is needed today; native pass-through inherits the client's own host. If PF adopts derivation it must add this gate (COMPARISON.md checklist explicitly lists it). | `packages/providers/src/providers/codex/provider.ts:292-296 (isOpenAiPromptCacheEndpoint), :51 (OPENAI_PROMPT_CACHE_HOSTS)` |
| Session-id extraction + UUID validation | 🟡 | Session identity read from body.metadata.user_id (Claude Code convention) and validated against a UUID regex before it is used to key the cache. — PF derives session identity from HEADERS (x-codex-turn-state / session_id / x-session-id / x-request-id) and prompt_cache_key, not from body.metadata.user_id, and does not UUID-validate (session_key.rs:120-154). Same intent, different source of truth. | `packages/providers/src/providers/codex/provider.ts:938-955 (extractSessionId)` |
| 48-hex truncation / no-content-leak keying | ✅ | sha256 digest truncated to 48 hex chars (< OpenAI's 64-char key bound); raw session id and prompt content never appear in the key. — PF hashes every session-key input with full sha256_hex and stores only the hash (session_key.rs:28-32,127) - same content-free guarantee. PF keeps the full 64 hex internally since the value is never sent upstream (no 64-char API bound to fit). | `packages/providers/src/providers/codex/provider.ts:985-994,1002-1009` |
| Global prompt_cache_key kill-switch | ❌ | CCFLARE_CODEX_PROMPT_CACHE_KEY=0 disables prompt_cache_key derivation entirely. — No equivalent toggle; PF has no derivation to disable on the translated path. | `packages/providers/src/providers/codex/provider.ts:981,28 (CODEX_PROMPT_CACHE_KEY_ENV)` |
| cacheBodyStore two-tier body store | ❌ | In-memory store: staging map (in-flight requestId->{accountId,entry}) promoted to a per-account lastCachedRequest slot when a request creates a cache entry; feeds keepalive replay. — PF has no request-body cache store. Its M3 anchor/ownership map persists ZERO conversation content and is not a replayable body buffer (COMPARISON.md: 'PF anchor map persists zero conversation content'). | `packages/proxy/src/cache-body-store.ts:103-278` |
| cache_control hint gate for staging | ❌ | Byte-scans the request body for '"cache_control"'/'"cache-control"' and only stages requests that carry a prompt-cache marker (others create no cache entry to keep warm). — No staging pipeline exists in PF. | `packages/proxy/src/cache-body-store.ts:45-67,142 (hasCacheControlHint)` |
| CACHEABLE_PATH gate (/v1/messages only) | ❌ | Staging is restricted to the /v1/messages endpoint; other paths don't use the prompt cache. — No staging pipeline exists in PF. | `packages/proxy/src/cache-body-store.ts:33,138` |
| Replay-header sanitization (STRIP_HEADERS) | ❌ | Strips authorization/x-api-key/cookie, all internal x-better-ccflare-*, and hop-by-hop headers before storing a replay snapshot, so keepalive rebuilds a clean upstream request. — No replay snapshot mechanism in PF. (PF does maintain a conservative x-stainless-* egress drop-list on the live path, but that is native-vs-SDK normalization, not a cache-replay concern.) | `packages/proxy/src/cache-body-store.ts:83-101,144-149` |
| Staging cap + oldest-entry eviction | ❌ | MAX_STAGING_ENTRIES=200 in-flight cap; when exceeded the oldest-timestamp staging entry is evicted with a warning. — No staging map to bound. | `packages/proxy/src/cache-body-store.ts:36,162-177` |
| Staging age sweep (orphan cleanup) | ❌ | On every stage call, staging entries older than STAGING_MAX_AGE_MS (5 min) are swept, handling the worker-restart orphan case where onSummary never fires. — No staging map; PF's continuity map cleanup is a separate anchor-expiry concern, not a body-staging sweep. | `packages/proxy/src/cache-body-store.ts:39,196-210` |
| discardStaged on terminal error | ❌ | On terminal/all-accounts-failed paths where onSummary will never fire, the staged entry is explicitly discarded to prevent a memory leak. — No staging map. | `packages/proxy/src/cache-body-store.ts:188-190; packages/proxy/src/proxy.ts:549,571,578` |
| onSummary promotion on cache creation | ❌ | When the post-processor reports cacheCreationInputTokens>0, the staged entry is promoted to the account's lastCachedRequest slot; always deletes the staging entry. — PF's native path doesn't even compute cache_creation_input_tokens - request_log fills only method/path/provider/status/duration (COMPARISON.md: per-request token accounting is a gap). | `packages/proxy/src/cache-body-store.ts:216-228; packages/proxy/src/usage-collector.ts:237-238` |
| evictStaleEntries (TTL x ageMultiplier) | ❌ | At keepalive tick, per-account cached bodies older than ttlMinutes*3 are evicted so a days-old request whose prompt cache has long expired is never replayed. — No cached-body store to evict. | `packages/proxy/src/cache-body-store.ts:257-275` |
| cacheBodyStore feature toggle (setEnabled) | ❌ | setEnabled(false) clears both maps and short-circuits staging so the store costs nothing when keepalive TTL=0. — No such subsystem. | `packages/proxy/src/cache-body-store.ts:116-122` |
| CacheKeepaliveScheduler (fire-before-expiry) | ❌ | registerHeartbeat every max(60s,(ttl-1)*60s) replays each account's last cached request through the proxy to warm the prompt cache before it expires. — PF ships NO synthetic keep-warm automation - deliberately OFF (COMPARISON.md 'Scheduled keep-warm/limit-warmup: PF synthetic warming deliberately OFF'). PORTING triad #3 says defer until PF measures OpenAI's real retention window. | `packages/proxy/src/cache-keepalive-scheduler.ts:70-112` |
| max_tokens=1 replay patch | ❌ | Keepalive replays patch max_tokens to 1 (falling back to the raw body on JSON parse failure) to warm the cache while minimizing quota consumption. — No keepalive path. | `packages/proxy/src/cache-keepalive-scheduler.ts:145-158` |
| Keepalive loop-prevention + cooldown-skip tagging | ❌ | Replay tagged x-better-ccflare-keepalive so the proxy (1) skips staging to avoid an infinite replay loop and (2) skips rate-limit cooldown on a 429 (synthetic per-IP burst, not a real per-account limit). — No keepalive replays exist; PORTING triad explicitly warns any future keepalive 'needs strict loop-prevention discipline (skip staging AND rate-limit cooldown)' before it can be enabled. | `packages/proxy/src/cache-keepalive-scheduler.ts:130-133; packages/proxy/src/handlers/proxy-operations.ts:570-573,828-830,877-879,1018-1020` |
| Dynamic keepalive TTL reconfiguration | ❌ | Subscribes to config 'change' events; when cache_keepalive_ttl_minutes changes it re-enables/disables the store and restarts the interval without a process restart. — PF is env-only with no config file and no hot reload (COMPARISON.md 'Config file + hot reload: PF env-only'). | `packages/proxy/src/cache-keepalive-scheduler.ts:29-45,65-68` |
| Self-signed localhost HTTPS agent for self-loop replay | ❌ | When SSL_KEY/CERT are set, replays to localhost use an https.Agent{rejectUnauthorized:false} since the self-loop goes through the proxy's own self-signed cert. — No self-loop replay path. (PF also does not TLS-serve its own API/dashboard - COMPARISON.md TLS-serving gap.) | `packages/proxy/src/cache-keepalive-scheduler.ts:160-180` |
| Evict-stale-before-send + allSettled fan-out | ❌ | Each tick evicts stale entries, then replays all cached accounts concurrently via Promise.allSettled, draining each response to release the connection. — No keepalive scheduler. | `packages/proxy/src/cache-keepalive-scheduler.ts:92-112,182-183` |
| injectSystemCacheTtl 1h upgrade | ❌ | Rewrites ephemeral system-level cache_control blocks that have no ttl to ttl:'1h', upgrading Anthropic's default ~5-min ephemeral cache window to 1 hour. — Anthropic /v1/messages-specific. OpenAI/Codex cache TTL is provider-managed and not client-settable, so there is no Codex equivalent to inject (COMPARISON.md cache triad #3, deferred). | `packages/proxy/src/proxy.ts:585-627 (def), :182-184 (call)` |
| system_prompt_cache_ttl_1h config + env toggle | ❌ | getSystemPromptCacheTtl1h() gated by SYSTEM_PROMPT_CACHE_TTL_1H env / config file, default disabled; exposed in the dashboard SettingsTab. — No such config key; PF has no 1h TTL injection to gate. | `packages/config/src/index.ts:393-405; packages/http-api/src/handlers/config.ts:51,209` |
| out_of_credits classification (model-scoped, no-bench) | ❌ | 429 carrying 'anthropic-ratelimit-unified-overage-disabled-reason: out_of_credits' is labeled model/beta-scoped credit depletion; the account is NOT benched and the request fails over per-request. — PF has no per-response header taxonomy and never inspects Anthropic overage headers. Its rate_limited/quota_exceeded statuses come from usage polling (usage_refresh.rs), not from failure-response headers. Listed in COMPARISON.md 'Also worth stealing'. | `packages/providers/src/providers/anthropic/provider.ts:50-64; packages/proxy/src/handlers/proxy-operations.ts:821-845` |
| extra_usage_exhausted classification (400, no-bench) | ❌ | 400 invalid_request_error whose message contains 'extra usage' is labeled as OAuth extra-usage-credit depletion; account stays in rotation and the response passes through unchanged. — No 400-body billing-state taxonomy in PF; nothing inspects Anthropic error bodies for extra-usage state. | `packages/providers/src/providers/anthropic/provider.ts:74-93; packages/proxy/src/handlers/proxy-operations.ts:740-764` |
| parseRateLimit 429 handling | 🟡 | 429 -> isRateLimited with resetTime from x-ratelimit-reset (clamped) else a default 60s cooldown. — PF classifies a live 5h-window exhaustion as rate_limited via usage POLLING (usage_refresh.rs derive_gate:93-96) and select.rs recovers it once reset_at passes (:144-152). But PF does NOT write rate_limited from an actual 429 RESPONSE - failure-driven writeback is unbuilt (PORTING A2). cooldown_until is READ (select.rs:168-175) but never written. | `packages/providers/src/providers/anthropic/provider.ts:444-463` |
| 529 overloaded taxonomy + reset extraction | ❌ | 529 is treated as rate-limited even when unified-status=allowed; reset time sourced in order from unified-reset -> Retry-After (delta-seconds or HTTP-date) -> x-ratelimit-reset. — PF has no 529/overload classification; it does not parse Retry-After at all. Watchdog handles silence/streaming, not 529 status semantics. | `packages/providers/src/providers/anthropic/provider.ts:352-442` |
| 24h reset clamp (clampResetTime) | ❌ | Every reset epoch is clamped to <= now+24h and rejects past/NaN/Infinity values, defusing a pathological Retry-After that would otherwise bench an account for days/forever. — PF carries upstream reset_at straight from usage windows with NO upper clamp (usage_refresh derive_gate). COMPARISON.md 'Also worth stealing' calls out adopting this 24h clamp for Codex's 5h/weekly windows. | `packages/providers/src/providers/anthropic/provider.ts:21-38` |
| RateLimitReason audit taxonomy (8 reasons) | 🟡 | Union type of 8 cooldown reasons (upstream_429_with_reset, _no_reset_probe_cooldown, _default_5h[deprecated], model_fallback_429, all_models_exhausted_429, upstream_529_overloaded_with/no_reset, out_of_credits, extra_usage_exhausted) persisted per account. — PF persists a COARSE status + deactivation_reason (migration 0001) - codex-lb's four-state model - but no fine-grained rate-limit reason enum. The write-side that would populate finer reasons is unbuilt (PORTING A2/A6/A7). | `packages/types/src/account.ts:1-24` |
| Adaptive exponential-backoff cooldown | ❌ | applyRateLimitCooldown sets cooldown = min(upstream resetTime, BASE(30s)*2^(n-1) capped MAX(5min)) driven by the consecutive_rate_limits streak; single entry point for every 429 path. — Nothing writes cooldown_until on failure in PF. PORTING A2 specifies porting this exact exponential (0.2s*2^(n-1)*jitter with 30s floor) into a RuntimeState writeback - the keystone unbuilt work. | `packages/proxy/src/handlers/rate-limit-cooldown.ts:26-77; packages/core/src/constants.ts:66-75` |
| consecutive_rate_limits streak + stability reset | ❌ | A per-account 429 streak counter ramps the backoff and resets after RATE_LIMIT_RESET_STABILITY_MS (5min) of healthy operation; atomically incremented in the DB. — PF has no per-account error/streak counters written on failure (error_count column exists but is always neutral - COMPARISON.md HIGH gap #1). | `packages/core/src/constants.ts:56,82-85; packages/proxy/src/handlers/rate-limit-cooldown.ts:39,54,64` |
| 529 in-place retry (full-jitter backoff) | ❌ | A reset-less 529 (overloaded_error) is retried in-place up to N times with full-jitter exponential backoff before the account is cooled and traffic fails over; skipped for synthetic (keepalive/auto-refresh) requests. — PF's silence watchdog does same-account peek-before-relay retry for STREAM silence, but there is no 529-status-specific in-place retry, and general cross-account failover on failure is unbuilt (PORTING B4). | `packages/proxy/src/handlers/proxy-operations.ts:1097-1169` |
| Default 60s no-reset cooldown (env-tunable) | ❌ | A 429/529 with no usable reset header applies a probe cooldown defaulting to 60s, overridable via CCFLARE_DEFAULT_COOLDOWN_NO_RESET_MS. — PF applies no probe cooldown on a headerless failure; it never reaches the failure path that would set one. | `packages/core/src/constants.ts:44-48; packages/dashboard-web/src/components/overview/system-status/errorCodeMeta.ts:27-34` |
| errorCodeMeta dashboard taxonomy descriptions | ❌ | Maps each RateLimitReason to a title/description/suggestion/severity for the system-status UI, incl. context-aware model-fallback copy. — PF dashboard shows account status + live reset countdown pills but no per-error-code explanatory taxonomy surface. | `packages/dashboard-web/src/components/overview/system-status/errorCodeMeta.ts:17-131` |
| In-account model-fallback on 429 (model_fallback_429 / all_models_exhausted_429) | ❌ | On a 429 when the account has model mappings, alternate models are retried on the SAME account before failover; distinct audit reasons distinguish single-model vs all-models-exhausted. — PF has no per-account model-mapping fallback list; an aliased tier maps to one target model and a 429 has no in-account retry (and no cross-account retry - PORTING B4). | `packages/proxy/src/handlers/proxy-operations.ts:866-907,1003-1043; errorCodeMeta.ts:42-47,88-116` |
| Pace-based proactive throttling (bench before 429) | ❌ | getUsageThrottleStatus benches an account until resumeAt when its window utilization outruns the linear time-pace of the window (expectedPct = elapsed/duration), i.e. before it actually 429s. — COMPARISON.md 'Also worth stealing' #2 and 'Depletion/pace/burn-rate forecasting' gap. PF gates only on HARD exhaustion (used>=100%) via derive_gate; no linear-pace pre-emptive bench. | `packages/proxy/src/handlers/usage-throttling.ts:251-306` |
| Model-aware scoped throttling | ❌ | Per-model weekly_scoped caps only throttle when the request's model family matches (scopedMode 'match'); whole-account windows always throttle, unknown scoped caps are skipped in match mode. — PF has no per-model weekly cap concept; usage windows are account-level 5h/weekly only (usage_windows.rs). | `packages/proxy/src/handlers/usage-throttling.ts:73-97,234-249,267-292` |
| five-hour / weekly throttle enable toggles | ❌ | usage_throttling_five_hour_enabled / _weekly_enabled config+env flags gate which window classes drive pace-throttling; both default off. — No throttling subsystem to gate; PF has no such config keys. | `packages/config/src/index.ts:407-429; packages/proxy/src/handlers/usage-throttling.ts:234-249` |
| createUsageThrottledResponse (synthetic 529 + Retry-After) | ❌ | When all selected accounts are pace-throttled, synthesizes a 529 overloaded_error body with Retry-After:60 naming the throttled accounts. — PF returns a BARE 503 on an empty eligible pool (ingress no_eligible) with no synthetic 529 / Retry-After / soonest-reset hint (PORTING B5 anti-starvation, unbuilt). | `packages/proxy/src/handlers/usage-throttling.ts:317-335` |
| Multi-provider usage-window normalization | 🟡 | collectWindows normalizes Anthropic limits[]/flat (session/weekly_all/weekly_scoped), zai, nanogpt, alibaba window shapes into a common {utilization,resetAtMs,window} list for throttling. — PF resolves Codex 5h+weekly windows by DURATION (usage_windows.rs classify:48-56, resolve into ResolvedUsage) with a staleness cutoff - but only for the Codex provider; no multi-vendor normalization. | `packages/proxy/src/handlers/usage-throttling.ts:39-232` |
| SessionAffinityStrategy (clientId -> account stickiness) | 🟡 | Pins each client session (metadata.user_id / clientSessionId) to one account for affinityTtl so its prompt cache stays warm across the agentic loop; new sessions get least-used placement. — PF has a content-free Codex OWNERSHIP pin (apply_ownership, ingress.rs:431-432) keyed on the derived session_key for anchor continuity - but it is NOT a general clientId->account cache-affinity map, and the translated aliased path uses NoopContinuity + session_id:None (ingress.rs:727,746) so it has NO stickiness at all. COMPARISON.md 'Session affinity' = partial; PORTING adoption brief #2 = align the translated path. | `packages/load-balancer/src/strategies/session-affinity.ts:72-247` |
| No-delete-on-429 sticky mapping (snap-back on recovery) | 🟡 | A rate-limited pinned account is intentionally NOT unmapped; the client temporarily fails over to the least-used account and snaps back once the original recovers, because the prompt-cache window outlives the rate-limit window. — This is the exact cache<->routing coupling COMPARISON.md flags to align. PF's ownership pin IS persistent across transient failure (survives, not deleted), but there is no failover-then-snap-back wired on the translated path and general failover is unbuilt (PORTING B4). | `packages/load-balancer/src/strategies/session-affinity.ts:66-70,221-228` |
| Recency-penalty new-session spread | ❌ | A freshly-picked account gets a +100 utilization penalty for RECENT_PICK_WINDOW_MS (500ms) so concurrently-starting sessions fan out across the pool instead of dogpiling one least-used account; applied on both assign and failover. — PF's selector has M3 ownership + a capacity_weighted waterfall but no short-window recency penalty for new-session spread; in_flight lease accounting that would enable load-spreading is also unbuilt (PORTING C9). | `packages/load-balancer/src/strategies/session-affinity.ts:17-25,108-151` |
| Affinity map TTL GC + capacity eviction | 🟡 | Sticky entries are GC'd after affinityTtlMs and, when the map hits MAX_AFFINITY_ENTRIES (10k), the least-recently-touched entry is evicted to bound memory against adversarial unique clientIds. — PF's anchor/ownership map has expiry logic but no capacity-bounded eviction and no retention pruning - COMPARISON.md notes PF anchors/sessions grow unbounded (data-retention gap #8). | `packages/load-balancer/src/strategies/session-affinity.ts:36,158-169,192-199` |
| clientSessionId threading from metadata.user_id | 🟡 | The client session id from body metadata.user_id is threaded as RequestMeta.clientSessionId to key the affinity map (same identity used to derive the prompt_cache_key). — PF threads a session_id into SelectionCtx on the NATIVE path only (from headers, ingress); the translated aliased path passes session_id:None (ingress.rs:746), so the cache key and the routing pin are not co-keyed there. Co-keying them is the crux of PORTING adoption brief #2. | `packages/load-balancer/src/strategies/session-affinity.ts:41-49,201` |

## load-balancer  ·  ✅ 4 🟡 13 ❌ 37

| Feature | PF | PolyFlare status / where | Source |
|---|:--:|---|---|
| SessionStrategy (default 5h account-session stickiness) | 🟡 | Pins all traffic to the account with the most-recent active 5h session, rotating to a new account only when it expires/rate-limits. — PolyFlare has a content-free per-session ownership PIN for Codex continuity (apply_ownership, watchdog.rs:53), but no account-level 5h session stickiness and no prompt_cache_key affinity; COMPARISON marks 'Session affinity' 🟡. | `packages/load-balancer/src/strategies/index.ts:18 (SessionStrategy)` |
| LeastUsedStrategy (lowest-utilization spread) | 🟡 | Picks the available account with the lowest effective upstream utilization, spreading bursts across the pool. — PolyFlare's UsageWeighted selector (select.rs:333) is deterministic least-weekly-used and CapacityWeighted weights by remaining credits, but they run on inert utilization data (health_tier/error_count never written; PORTING Phase A). | `packages/load-balancer/src/strategies/least-used.ts:53` |
| SessionAffinityStrategy (per-client sticky hybrid) | 🟡 | Keys on client session id (metadata.user_id): first request → least-used account, then sticks that client→account for the affinity TTL for prompt-cache reuse. — PolyFlare pins per session_key for the native Codex path (session_key.rs derive_session_key), but the translated alias path uses NoopContinuity (no key/pin); no general client→account cache stickiness (COMPARISON adoption brief §2). | `packages/load-balancer/src/strategies/session-affinity.ts:72` |
| Config-selectable strategy registry | ✅ | StrategyName enum + isValidStrategy + DEFAULT_STRATEGY select one of session/least-used/session-affinity at runtime. — PolyFlare has a richer RoutingStrategy factory of 6 strategies (select.rs:453) with a global default (POLYFLARE_ROUTING_STRATEGY) plus per-pool overrides (POLYFLARE_POOL_STRATEGY, config.rs:39); COMPARISON marks this ✅ (PF 6 vs CC 1-2). | `packages/types/src/strategy.ts:3; packages/core/src/strategy.ts:6,11` |
| Per-account priority ordering (priority int, 0=first) | ❌ | account.priority is the primary sort key in every strategy (lower = higher priority), set via CLI --set-priority; ties broken by utilization. — PolyFlare has no ordinal priority field in selection; the closest analog is the coarse routing_policy burn_first/normal/preserve waterfall (select.rs:216) — a 3-bucket precedence, not an N-way priority. | `packages/load-balancer/src/strategies/index.ts:326 (priority sort)` |
| Utilization tiebreak (getAccountUtilization) | ✅ | Within equal priority, the account with the most remaining capacity (lowest utilization; null treated as 0/fresh) is chosen first. — PolyFlare's deterministic_min tiebreak sorts by remaining secondary credits then secondary/primary used% (select.rs:232), and UsageWeighted picks least secondary_used — same 'most-headroom-first' shape. | `packages/types/src/strategy.ts:49 (StrategyStore.getAccountUtilization)` |
| Recency-penalty burst spread (RECENT_PICK_WINDOW/PENALTY) | 🟡 | A freshly-picked account gets a +100 utilization penalty for 500ms so concurrent selects round-robin instead of all converging on one lowest-util account. — PolyFlare spreads bursts via weighted-random sampling over remaining credits (WeightedIndex, select.rs:254) seeded for determinism — a different mechanism; no time-windowed recency penalty. | `packages/load-balancer/src/strategies/least-used.ts:16-24,106-132` |
| peek() would-be-primary preview | ❌ | Side-effect-free selection that surfaces the account the next real select() would pick (incl. simulated auto-unpause), for dashboard Primary flagging. — PolyFlare's read_api/dashboard shows per-account usage/status but has no peek() that previews the next routing pick. | `packages/load-balancer/src/strategies/peek-availability.ts:48 (isPeekAvailable)` |
| Auto-unpause simulation (wouldAutoUnpause) | 🟡 | Shared predicate: a paused account with auto_fallback_enabled + safe pause_reason + elapsed window is treated as available; keeps peek()/select() in sync. — PolyFlare's eligibility() read-side self-heal recovers rate_limited/quota_exceeded accounts once reset_at passes (select.rs:144-164), but has no paused+auto_fallback concept and nothing writes those paused states from failures. | `packages/load-balancer/src/strategies/peek-availability.ts:16 (wouldAutoUnpause)` |
| StrategyStore abstraction seam | ✅ | Interface letting strategies read utilization / reset sessions / pause-resume without direct SQL. — PolyFlare's Selector trait + SelectionCtx over AccountSnapshot slices (traits.rs, select.rs) is the same 'pure selector, store details injected' seam. | `packages/types/src/strategy.ts:13 (StrategyStore)` |
| 5-hour session window constant | ❌ | ANTHROPIC_SESSION_DURATION_DEFAULT = 5h drives session-duration expiry for Anthropic/OAuth providers. — PolyFlare tracks 5h+weekly USAGE-QUOTA windows (usage_windows.rs, COMPARISON ✅) but has no 5h routing-session concept; the ownership pin has no fixed lifetime. | `packages/core/src/constants.ts:15` |
| hasActiveSession (provider-gated, rate-limit-aware) | ❌ | Session is 'active' only for session-tracking providers, within the 5h window, and only if not currently rate-limited. — No equivalent — PolyFlare's ownership pin is keyed on session_key/anchor, not a windowed active-session per account. | `packages/load-balancer/src/strategies/index.ts:81` |
| resetSessionIfExpired (fixed-duration OR window-reset) | ❌ | Starts a fresh session when the 5h duration elapses or the Anthropic rate-limit window resets, to better use usage windows. — No session lifecycle to reset; PolyFlare resets usage windows via /wham/usage polling (usage_refresh.rs), not session_start. | `packages/load-balancer/src/strategies/index.ts:33` |
| Session-bypass header (auto-refresh probes) | ❌ | x-better-ccflare-bypass-session skips session tracking so synthetic refresh/probe traffic doesn't pin or reset sessions. — No session tracking to bypass; PolyFlare has no synthetic keepalive/refresh-probe traffic (warming deliberately OFF, COMPARISON). | `packages/load-balancer/src/strategies/index.ts:180` |
| Sticky-mapping kept across rate-limit (issue #115) | 🟡 | When a pinned account is rate-limited, the client→account mapping is NOT deleted — fail over temporarily and snap back on recovery (cache window outlives rate-limit window). — PolyFlare keeps the ownership pin when the owner is ineligible and recovers via the watchdog (RouteDecision::Recover, ingress.rs:452), same 'don't drop the pin' intent — but only for anchored Codex turns. | `packages/load-balancer/src/strategies/session-affinity.ts:69; index.ts:96` |
| Client-session keying (metadata.user_id) | 🟡 | clientSessionId is extracted from request body metadata.user_id and threaded as the affinity/governor key. — PolyFlare derives a hashed session_key from turn-state/session headers + prompt_cache_key/input (session_key.rs:120), stronger than user_id — but the translated alias path never sets one (NoopContinuity). | `packages/proxy/src/proxy.ts:256; session-affinity.ts:201` |
| Session-storm governor: rolling-hour per-session counter | ❌ | recordSessionRequest counts /v1/messages per client session over a rolling hour to detect runaway subagent fan-out. — PolyFlare has no session-volume circuit breaker; COMPARISON adoption brief explicitly lists 'Session-storm governor' as worth stealing from ccflare. | `packages/proxy/src/session-governor.ts:84` |
| Governor warn threshold (observability-first) | ❌ | CCFLARE_SESSION_WARN_REQUESTS_PER_HOUR (default 300) logs a warning on suspected runaway sessions without rejecting. — No governor at all in PolyFlare. | `packages/proxy/src/session-governor.ts:31,36` |
| Governor enforce/reject (opt-in 429) | ❌ | CCFLARE_SESSION_MAX_REQUESTS_PER_HOUR (default 0=off) rejects overflow with an Anthropic-shaped 429 before account selection burns quota. — No governor; PolyFlare's only pre-selection gate is provider/pool partitioning + TA6 security-work filter (select.rs:291). | `packages/proxy/src/session-governor.ts:156 (buildSessionRejectResponse)` |
| Governor: rejected requests consume no budget | ❌ | A rejected request is not appended to the window so a client retrying every few minutes can't hold itself in permanent 429 lockout. — No governor. | `packages/proxy/src/session-governor.ts:127-136` |
| Governor memory bounding + idle sweep | ❌ | MAX_TRACKED_SESSIONS(2048)/MAX_TRACKED_TIMES(20k) caps + lastSeen-based least-recent eviction keep the tracker bounded under adversarial unique keys. — No governor tracker to bound. | `packages/proxy/src/session-governor.ts:202 (sweep)` |
| Governor wiring at proxy entry | ❌ | handleProxy calls the governor for /v1/messages and short-circuits with the 429 before account selection. — No pre-selection volume gate in ingress.rs. | `packages/proxy/src/proxy.ts:269-274` |
| Forced-account override header | ❌ | x-better-ccflare-account-id pins a request to one specific account (used for testing and force-routing). — PolyFlare offers pooled routing (/{pool}/responses selects only that pool's accounts, ingress.rs:332) as a coarser override, but no single-account force header. | `packages/proxy/src/handlers/account-selector.ts:91` |
| Forced-account auto-refresh bypass allow-through | ❌ | For bypass-session probes, an overage-paused or rate-limited forced account is still allowed through so the scheduler can trigger window-reset/auto-resume. — No forced-account path and no auto-refresh scheduler in PolyFlare. | `packages/proxy/src/handlers/account-selector.ts:109-124` |
| Exclude-providers header filter | 🟡 | x-better-ccflare-exclude-providers drops named providers from the candidate set (special-cases anthropic-oauth vs console keys) so e.g. Codex traffic never uses Anthropic OAuth. — PolyFlare enforces provider partitioning STRUCTURALLY (native /responses→Codex-only, /v1/messages→Anthropic or aliased-Codex; filter_by_provider_and_pool) rather than via a client-supplied exclusion header. | `packages/proxy/src/handlers/account-selector.ts:152-181` |
| Priority beats stickiness | ❌ | An active 5h session is abandoned if a strictly-higher-priority account is available (priority > cache locality). — PolyFlare has no priority ordinal and no session to preempt; ownership pin is honored while eligible regardless of a 'higher-priority' account. | `packages/load-balancer/src/strategies/index.ts:288-303` |
| Auto-fallback to higher-priority account on window reset | 🟡 | checkForAutoFallbackAccounts reactivates auto_fallback_enabled accounts (incl. auto-unpause of overage pauses) once their usage window resets, preferring the highest-priority one. — PolyFlare's eligibility() auto-recovers rate_limited/quota_exceeded accounts when reset_at passes (select.rs:144-164), but with no priority ordering and no paused-account reactivation. | `packages/load-balancer/src/strategies/index.ts:355 (checkForAutoFallbackAccounts)` |
| Combo definitions (Combo / ComboSlot / family assignment) | ❌ | Named ordered chains of (account, model-override) slots, each combo bound to a model family. — PolyFlare has no combos (COMPARISON 'Cross-provider fallback chains (combos)' ❌); model aliasing maps a client model to ONE target, not an ordered fallback chain. | `packages/types/src/combo.ts:1` |
| Per-family combo activation | ❌ | getActiveComboForFamily routes fable/opus/sonnet/haiku traffic through its active combo's slots in priority order. — No combos; PolyFlare's per-tier behavior is the cache_affinity_tier weighting (High/Medium/Low), not an ordered per-family account chain. | `packages/proxy/src/handlers/account-selector.ts:183-259` |
| Per-slot model override | ❌ | Each combo slot carries a model string that overrides the request model when that slot's account is used. — PolyFlare's ModelAlias sets a single target_model + reasoning_effort per client model (alias.rs), but there is no per-fallback-slot model override. | `packages/proxy/src/handlers/account-selector.ts:233; packages/proxy/src/proxy.ts:459-468` |
| Combo → SessionStrategy fallback | ❌ | When all combo slots are unavailable or every slot fails, routing falls back to normal SessionStrategy selection (clearing combo metadata). — No combos, so no combo-to-default fallback tier. | `packages/proxy/src/proxy.ts:503-552` |
| Combo slot/account desync guard | ❌ | Detects and logs when a combo slot's expected account id doesn't match the selected account at the same index. — No combo slot indexing in PolyFlare. | `packages/proxy/src/proxy.ts:462-466` |
| Combos UI feature flag | ❌ | BETTER_CCFLARE_SHOW_COMBOS gates the combos dashboard UI while the combo API stays live. — No combos feature to flag. | `BETA_FEATURE_COMBOS.md` |
| Cross-account failover loop | 🟡 | handleProxy iterates the ordered account list, calling proxyWithAccount which returns null to signal 'try next account' on any recoverable failure. — PolyFlare only fails over on the no-anchor recovery path (RecoveryPlan::None / ResendFull re-pick a fresh account, ingress.rs:456,499); no general bounded 3-account retry loop — PORTING B4 flags this as 'the single biggest missing behavior'. | `packages/proxy/src/proxy.ts:457-501` |
| 401 failover (invalid credentials) | ❌ | An upstream 401 returns null to fail over to the next account instead of surfacing the auth error. — PolyFlare's watchdog maps a hard upstream error to BAD_GATEWAY with no next-account attempt (watchdog.rs:151); OAuth 401s are handled at refresh time by marking the account, not by in-request failover. | `packages/proxy/src/handlers/proxy-operations.ts:1090,1175` |
| 429 cooldown writeback (applyRateLimitCooldown) | ❌ | Single entry point that, on a 429, sets rate_limited_until + rate_limited_at and atomically increments consecutive_rate_limits in the DB. — PolyFlare's select.rs READS cooldown_until/error_count but nothing writes them on an upstream failure — the keystone gap (PORTING A2; COMPARISON 'Live error/cooldown state written from failures' ❌). | `packages/proxy/src/handlers/rate-limit-cooldown.ts:26` |
| Consecutive escalating backoff (30s→5min) | ❌ | computeRateLimitBackoffMs ramps cooldown exponentially with the consecutive-429 count, capped at the upstream reset time. — select.rs has the symmetric read-side error_backoff_secs = min(300, 30·2^(n-3)) gate (select.rs:50), but no writer increments error_count, so the backoff never engages (PORTING A2/A3). | `packages/core/src/constants.ts:66 (computeRateLimitBackoffMs)` |
| Cooldown reset extraction (Retry-After/window/default) | 🟡 | extractCooldownUntil resolves cooldown from retry-after / x-ratelimit-reset header, else usage-window reset, else a 60s floor, clamped ≥60s. — PolyFlare writes reset_at from /wham/usage polling (usage_windows.rs) and eligibility honors reset_at/cooldown_until, but it does NOT parse Retry-After off a live 429 response (no 429 write path exists yet). | `packages/proxy/src/handlers/proxy-operations.ts:57` |
| In-place 529 overloaded retry (jittered backoff) | ❌ | On a reset-less 529 (overloaded_error), retries the same account with bounded full-jitter exponential backoff before applying cooldown/failover. — PolyFlare has no in-place retry for overload; the watchdog races only the FIRST chunk for silence, not upstream 529 status backoff. | `packages/proxy/src/handlers/proxy-operations.ts:1101-1169 (getOverloadRetryConfig constants.ts:97)` |
| out_of_credits per-request failover (no bench) | ❌ | A 429 with overage-disabled-reason:out_of_credits is model/beta-scoped, so the account is NOT benched — fail over per-request, leave it in rotation for other models (issue #261). — PolyFlare has no per-request billing-scoped failover; it also has no live 429 classification path at all yet. | `packages/proxy/src/handlers/proxy-operations.ts:827-860` |
| extra_usage_exhausted passthrough (no bench) | ❌ | A 400 invalid_request_error for depleted Claude-OAuth extra-usage credits is passed through to the client unchanged and not benched (issue #293). — No billing-rejection taxonomy in PolyFlare; COMPARISON adoption brief flags ccflare's out_of_credits/extra_usage taxonomy as worth stealing. | `packages/proxy/src/handlers/proxy-operations.ts:754-791` |
| Pool-exhausted 503 with next_available_at + Retry-After | 🟡 | createPoolExhaustedResponse builds a 503 listing each account's reason and the soonest recovery time + a Retry-After header. — PolyFlare returns a bare 503 'no eligible account' (ingress.rs no_eligible()) with no per-account detail, next_available_at, or Retry-After hint — PORTING B5/B10 propose the soonest-reset hint. | `packages/proxy/src/handlers/proxy-operations.ts:1265` |
| Passthrough on empty pool (opt-in) | ❌ | CCFLARE_PASSTHROUGH_ON_EMPTY_POOL=1 forwards unauthenticated instead of 503 when no accounts are available. — PolyFlare always 503s an empty eligible pool; no unauthenticated passthrough mode. | `packages/proxy/src/proxy.ts:347-359` |
| Reauth detection on total failure | 🟡 | When all accounts fail, isRefreshTokenLikelyExpired identifies OAuth accounts needing re-auth and surfaces per-account reauth CLI commands. — PolyFlare classifies permanent OAuth failures per-account (classify_failure → reauth_required/deactivated status, ingress.rs:282-293) at refresh time, but produces no aggregate 'these accounts need re-auth' message on pool failure. | `packages/proxy/src/proxy.ts:558-576` |
| Auto-recover eligibility on window reset | ✅ | Accounts rate-limited/paused are re-admitted automatically once their reset time passes (the read-side self-heal in selection). — PolyFlare's eligibility() zeroes usage/error state and re-admits rate_limited (reset zeros primary+error) and quota_exceeded (zeros primary+secondary) accounts once reset_at passes (select.rs:144-164) — a faithful codex-lb port. | `packages/load-balancer/src/strategies/index.ts:355; peek-availability.ts:16` |
| Anti-starvation backoff-fallback | ❌ | (ccflare implicit via auto-fallback + keeping mappings) serve accounts as they recover rather than hard-failing when the pool is tight. — PolyFlare explicitly defers codex-lb's serve-soonest-to-recover fallback: an empty eligible pool → 503 (select.rs:114-125 comment; COMPARISON ❌; PORTING B5). | `packages/load-balancer/src/strategies/index.ts:363-388` |
| Failure-threshold auto-pause | ❌ | The auto-refresh scheduler pauses an account (pause_reason='failure_threshold') after repeated probe failures, with throttled re-probes. — PolyFlare has no auto-refresh scheduler and no failure-threshold pause; only OAuth-permanent status writes and usage-status changes are live. | `packages/proxy/src/auto-refresh-scheduler.ts:722` |
| auto_pause_on_overage | ❌ | Accounts crossing a billing-overage boundary are paused (pause_reason='overage') and later auto-resumed on window reset. — No overage-pause concept; PolyFlare tracks quota via usage windows and marks quota_exceeded status but not a billing-overage pause. | `packages/proxy/src/usage-collector.ts:483` |
| Per-account model fallback list (getModelList/getModelMappings) | ❌ | Merges model_mappings arrays + legacy model_fallbacks into an ordered [primary, ...fallbacks] list per model family. — PolyFlare has no per-account model fallback list; model aliasing (alias.rs) maps a client model to a single target with no fallback chain. | `packages/core/src/model-mappings.ts:128 (getModelMappings)` |
| In-account model cycling on 404/400/429 | ❌ | On model-unavailable/rate-limited, iterates the fallback model list on the SAME account (re-patching the body per model) before failing over. — No in-account model cycling; a failed Codex turn recovers via account re-pick/full-resend (watchdog), never by swapping to a fallback model on the same account. | `packages/proxy/src/handlers/proxy-operations.ts:793-999` |
| Model-unavailable classification (isModelUnavailableError) | ❌ | Detects Anthropic not_found_error, OpenAI model_not_found, generic 'model not found', Bedrock ResourceNotFoundException, and treats all 429s as slot-failover triggers. — PolyFlare has no model-availability error taxonomy driving failover; upstream errors are handled generically by the watchdog. | `packages/proxy/src/handlers/proxy-operations.ts:358` |
| Re-patch model after transform (mapModelName defeat) | ❌ | After transformRequestBody remaps a non-Claude model back to the primary, forcibly re-writes the intended fallback model into the final upstream body. — No model-cycling path, so no post-transform re-patch needed. | `packages/proxy/src/handlers/proxy-operations.ts:964-989` |
| Per-(account,model) request-repair retries | ❌ | Learns cache_control rejectors and pre-strips cache_control, and filters invalid thinking-block signatures, retrying in-place before failover. — PolyFlare forwards native Codex bytes verbatim and does one-shot Anthropic→Responses translation; no adaptive per-(account,model) body-repair-and-retry. | `packages/proxy/src/handlers/proxy-operations.ts:320,712 (cache_control); :150,280,670 (thinking)` |
| Pace-based proactive usage throttling | ❌ | applyUsageThrottling benches an account BEFORE it 429s when 5h/weekly consumption outruns the linear pace of its window; returns a 529 if all are throttled. — PolyFlare polls usage windows every 600s (usage_windows.rs) and excludes on used%≥85/secondary≥90 (should_drain), but has no pre-emptive linear-pace bench; COMPARISON adoption brief flags ccflare's pace-throttling as worth stealing. | `packages/proxy/src/handlers/usage-throttling.ts:308; packages/proxy/src/proxy.ts:287-335` |

## api-dashboard-db-observability  ·  ✅ 2 🟡 10 ❌ 61

| Feature | PF | PolyFlare status / where | Source |
|---|:--:|---|---|
| [http-api] GET /health — pool + runtime status | ❌ | Health endpoint returning pool status (configured/paused/rate_limited/routable/usage_exhausted/next_available_at) + runtime health (asyncWriter, usageWorker, storage integrity), status computed ok/degraded/unhealthy with 200/503 code, 2s TtlCache. — PolyFlare has NO /health endpoint at all (app.rs routes: only /responses, /v1/messages, pooled variants, /models, /api/{pools,accounts,requests}, /dashboard). No pool-status aggregation, no routable/exhausted counters, no 503 liveness. | `packages/http-api/src/handlers/health.ts:61-249; router.ts:250` |
| [http-api] /health usage_exhausted counter | ❌ | Subset of routable accounts whose representative usage window is at 100% with a future reset — flags an effectively-drained pool that still looks 'available' locally (incident 2026-07-09). — PolyFlare computes used_percent + stale per window in read_api but never derives a pool-wide 'exhausted-but-looks-available' counter; there is no /health surface to carry it. | `packages/http-api/src/handlers/health.ts:80-86; rate-limit-status.ts:59-69` |
| [http-api] /health ?detail=1 per-account breakdown | ❌ | Optional per-account status list (available/rate_limited/paused + rate_limited_until/reason/at), gated behind HEALTH_DETAIL_ENABLED. — No /health endpoint; per-account status IS available via GET /api/accounts (status field), but there is no detail/gated variant and no rate_limited_reason/at fields. | `packages/http-api/src/handlers/health.ts:224-245` |
| [http-api] GET /api/stats — aggregated stats summary | ❌ | Rollup: totalRequests, successRate, activeAccounts, avgResponseTime, totalTokens, totalCostUsd, avgTokensPerSecond, top models, per-account stats, recent error groups; ?since=days & ?errorsSinceHours params. — PolyFlare has no /api/stats and no request-aggregation SQL layer. Its native request_log fills only method/path/provider/status/duration — success-rate/token/cost aggregates can't be computed even if an endpoint existed. | `packages/http-api/src/handlers/stats.ts:7-63` |
| [http-api] POST /api/stats/reset | ❌ | Clears request history (DELETE FROM requests) and zeroes account request_count / session_request_count. — No reset/clear-history endpoint. request_log grows unbounded and there is no admin op to truncate it. | `packages/http-api/src/handlers/stats.ts:69-84` |
| [http-api] GET /api/storage — DB storage metrics | ❌ | db_bytes, wal_bytes, integrity status/kind/last-check results (quick+full), orphan_pages, last_retention_sweep_at, null_account_rows_24h. — No storage-metrics endpoint; PolyFlare exposes no DB size, WAL, integrity, orphan-page or retention-sweep telemetry. | `packages/http-api/src/handlers/storage.ts:8-41` |
| [http-api] POST /api/storage/integrity/check | ❌ | On-demand SQLite integrity check: quick runs sync (200), full kicks off background worker (202), 409 if already running. — No integrity-check endpoint or worker in PolyFlare (sqlx WAL, no PRAGMA integrity_check tooling). | `packages/http-api/src/handlers/storage.ts:68-108` |
| [http-api] GET /api/analytics — full analytics query | ❌ | Single consolidated endpoint: totals, bucketed time-series, token breakdown, model distribution, account/apiKey performance, cost-by-model, account-model usage, model performance w/ p95, burn-rate; params range/mode(cumulative)/modelBreakdown/filters. — PolyFlare has no analytics endpoint and no charting dataset. COMPARISON.md marks the rich analytics dashboard 🟡 — countdowns/tables only, no time-series/p95/cost-split data source. | `packages/http-api/src/handlers/analytics.ts:27-570; router.ts:400` |
| [http-api] GET /api/usage-history — per-account window series + prediction | 🟡 | Returns per-window (5h/weekly) utilization time-series from usage_snapshots plus a computeUsagePrediction trajectory per window; params account/range/window. — PolyFlare persists the same time-series (usage_history table, written each usage poll) but exposes only the LATEST window per account via /api/accounts. No history-series endpoint and no prediction attached. | `packages/http-api/src/handlers/usage-history.ts:20-70` |
| [http-api] GET /api/insights/cache — cache-savings insights | ❌ | Per (account/model/project) cache hit-rate + actual vs counterfactual cost + savings USD, flags low-hit-rate rows, lists unknown-pricing models. — No insights endpoints. PolyFlare captures no per-request token/cache columns on the native path, so cache-savings math has no inputs. | `packages/http-api/src/handlers/insights.ts; services/cache-insights.ts:247-326` |
| [http-api] GET /api/insights/anomalies — anomaly detection | ❌ | Batch detectors over request rows: per-(account,model) token baselines, z-score token/output outliers, runaway-loop bursts, expensive-model misrouting. — No anomaly detection. Requires per-request token/cost/project rows PolyFlare doesn't persist natively. | `packages/http-api/src/services/anomaly-insights.ts:457-524` |
| [http-api] GET /api/insights/context — context composition | ❌ | Char/token breakdown of system/tools/messages from stored payloads, top repeated content contributors (hash-grouped), per-project context-growth curve. — No context insights; depends on stored request payloads, which PolyFlare deliberately never persists (no conversation content — permanent design limit). | `packages/http-api/src/services/context-insights.ts:451-539` |
| [http-api] Alerts API (history/config/ack/stream) | ❌ | GET /api/insights/alerts (history), GET+POST config, POST acknowledge-all, POST ack-by-id, GET /stream (SSE); threshold + anomaly alert engine (daily spend, tokens/hr, per-request tokens, webhook). — No alerts subsystem, no alerts table, no webhook, no alert SSE. PolyFlare has no threshold/anomaly alerting anywhere. | `packages/http-api/src/handlers/alerts.ts; services/alerts.ts:63-140; router.ts:416-430` |
| [http-api] GET /api/logs/stream — live log SSE | ❌ | Server-Sent Events stream of the in-process logBus; TransformStream writer, initial {connected:true}, off-listener cleanup on abort. — No SSE anywhere in PolyFlare. Logs are tracing stdout only; the dashboard cannot subscribe to a live log feed. COMPARISON.md: 'Live log streaming (SSE)' = ❌. | `packages/http-api/src/handlers/logs.ts:10-67; router.ts:398` |
| [http-api] GET /api/logs/history — past logs | ❌ | Reads last 1000 lines from the persisted log-file writer. — No log-file reader endpoint; no historical log retrieval API. | `packages/http-api/src/handlers/logs-history.ts:11-27` |
| [http-api] GET /api/requests — request summary list | 🟡 | Recent N requests (limit 1..1000, default 50) joined to account name, with tokens (prompt/completion/total/input/output/cache), cost, tokens/sec, model, agent, api-key, billing_type, combo, original/applied model, rateLimited flag. — PolyFlare HAS GET /api/requests with limit/offset+total, but returns only the 7 content-safe fields (requested_at/provider/method/path/aliased/status/duration_ms) — no tokens/cost/model/account-name join. | `packages/http-api/src/handlers/requests.ts:36-112; read_api.rs:179-205` |
| [http-api] GET /api/requests/detail — full payload list | ❌ | Last N requests with decoded request+response bodies (base64, 256KB cap, truncation flags) parsed from request_payloads — the conversation-history source. — PolyFlare persists NO request/response bodies by design (content-safety chokepoint in observability.rs), so there is nothing to return; this is a permanent, intentional gap. | `packages/http-api/src/handlers/requests.ts:117-182` |
| [http-api] GET /api/requests/payload/:id — lazy single payload | ❌ | Fetch one request's full stored payload on demand (perf optimization to avoid bulk JSON parse), 404 if absent. — No payload storage → no per-request payload endpoint. | `packages/http-api/src/handlers/requests.ts:188-201; router.ts:529-538` |
| [http-api] GET /api/requests/stream — live request SSE | ❌ | ReadableStream SSE emitting each request completion event from the in-process requestEvents bus (event: connected handshake, per-listener cleanup). — No live request event bus/SSE. PolyFlare's dashboard RequestsTable polls /api/requests every 15s instead of streaming. | `packages/http-api/src/handlers/requests-stream.ts:3-66` |
| [http-api] Token-health endpoints | ❌ | GET /api/token-health (all accounts), /reauth-needed, /account/:name — refresh-token expiry/health inspection for the dashboard. — No token-health inspection API. PolyFlare classifies OAuth failures at refresh time and flips account status, but exposes no per-account token-health report endpoint. | `packages/http-api/src/handlers/token-health.ts:12-84; router.ts:303-307` |
| [http-api] GET /api/version/check + /api/system/info + /api/features | ❌ | Dashboard meta endpoints: upstream version-update check, host system info, feature-flag list. — None present. No version-check, system-info, or feature-flag endpoints in PolyFlare's server. | `packages/http-api/src/handlers/{version,system,features}.ts; router.ts:395-397` |
| [http-api] API-keys management + per-key stats | ❌ | GET/POST /api/api-keys, GET /api/api-keys/stats, enable/disable/delete/role — hashed proxy API keys with usage_count and per-key success-rate stats. — No API-key subsystem, no api_keys table, no per-key stats. PolyFlare is fully unauthenticated (network-boundary trust; POLYFLARE_ADMIN_TOKEN is only planned). | `packages/http-api/src/handlers/api-keys.ts; database/src/repositories/api-key.repository.ts; router.ts:447-451` |
| [http-api] Debug/profiling endpoints | ❌ | GET /api/debug/heap, /snapshot, /rss — heap stats, V8 heap snapshot, RSS memory for ops debugging. — No debug/profiling endpoints exposed. | `packages/http-api/src/handlers/debug.ts; router.ts:442-444` |
| [http-api] Auth + role-based authz middleware | ❌ | AuthService authenticates every request and authorizes by API-key role (admin vs api-only), 401/403 before dispatch; role gates e.g. api-key role updates. — ALL PolyFlare endpoints (ingress, /api/*, dashboard) are unauthenticated — the documented headline security caveat. No auth/authz middleware exists. | `packages/http-api/src/router.ts:497-520; services/auth-service.ts` |
| [http-api] Config: retention get/set | ❌ | GET/POST /api/config/retention — read/update the request-history retention window used by the cleanup job. — No config API and no retention setting; PolyFlare is env-only with no request-log retention concept. | `packages/http-api/src/handlers/config.ts; router.ts:370-375` |
| [analytics-sql] Consolidated single-roundtrip analytics query | ❌ | WITH filtered_requests CTE then 13 scalar sub-selects (counts, success rate, avg latency, tokens, cost, plan/api cost split, tokens/sec, active accounts, token breakdown) in one query. — No analytics SQL layer at all. PolyFlare's only request query is a plain SELECT of 7 columns ordered by time (request_log_repo.list). | `packages/http-api/src/handlers/analytics.ts:46-83` |
| [analytics-sql] Time-series bucketing + cumulative + model-breakdown | ❌ | (timestamp/bucket)*bucket grouping into 1m/5m/1h/1d buckets by range, optional GROUP BY model, and running-total cumulative transform (global or per-model). — No time-series bucketing. request_log has requested_at but nothing computes bucketed series. | `packages/http-api/src/handlers/analytics.ts:142-177,472-520; utils/query-filters.ts:28-71` |
| [analytics-sql] 5-way UNION ALL breakdown query | ❌ | model_distribution, account_performance, cost_by_model, api_key_performance, account_model_usage combined via a fixed 11-column UNION contract in one roundtrip. — No breakdown queries. No account_used/model/api_key columns populated on the native request_log path to group by. | `packages/http-api/src/handlers/analytics.ts:180-327` |
| [analytics-sql] Per-model p95 latency (PERCENT_RANK window fn) | ❌ | ranked CTE with PERCENT_RANK() OVER (PARTITION BY model ORDER BY response_time_ms); p95 = MIN(response_time_ms) WHERE pr>=0.95, plus avg/max/error-rate/min-max tokens-per-sec. — No p95 anywhere. COMPARISON.md: 'Latency percentiles (p95)/phase breakdown' = ❌ — PolyFlare has a latency-regression test but no percentile analytics surface. | `packages/http-api/src/handlers/analytics.ts:375-451` |
| [analytics-sql] Burn-rate fixed-window aggregates | ❌ | 7d/30d plan+api cost sums with effectiveBurnRateDays clamp (divisor limited to actual data age) → avg daily/weekly plan & api cost, stable across dashboard range changes. — No burn-rate/cost-pace computation; no cost_usd captured on native path to sum. | `packages/http-api/src/handlers/analytics.ts:14-25,85-139` |
| [analytics-sql] Cache-hit-rate SQL definition | ❌ | cache_read*100/(input+cache_read+cache_creation) computed in the time-series query and mirrored in cache-insights. — No cache-token columns populated on the native path, so no cache-hit-rate metric. | `packages/http-api/src/handlers/analytics.ts:167-168; services/cache-insights.ts:75-82` |
| [analytics-sql] Shared range + filter builder | ❌ | getRangeConfig (range→startMs+bucket, unknown→24h) and buildRequestFilters (WHERE for timestamp/accounts/models/apiKeys/status with name→id subquery + NO_ACCOUNT_ID sentinel), reused by analytics & insights. — No shared query-filter layer; PolyFlare's request read takes only limit/offset (no range/account/model/status filtering). | `packages/http-api/src/utils/query-filters.ts:28-141` |
| [analytics-sql] StatsRepository.getAggregatedStats | ❌ | Windowed SUM/AVG over requests (default last 30d) for totals: requests, successes, latency, input/output/cache tokens, cost, tokens/sec. — No StatsRepository equivalent; PolyFlare's store exposes request_log list/count/insert only. | `packages/database/src/repositories/stats.repository.ts:41-107` |
| [analytics-sql] getAccountStats + batched success rates | ❌ | Per-account request counts (incl. unauthenticated via NO_ACCOUNT_ID) joined to a batched success-rate query, O(1) merge. — No per-account request stats. account_id is nullable on native request_log and there's no aggregation query. | `packages/database/src/repositories/stats.repository.ts:113-207` |
| [analytics-sql] getRecentErrorGroups (window-fn dedup) | ❌ | Groups errors by (error_message, account) with ROW_NUMBER/COUNT/MIN window fns, enriches with account rate-limit state (until/reason/at). — No error grouping. PolyFlare stores no error_message (content-safety excludes free-form upstream error text), so error-group analytics is impossible natively. | `packages/database/src/repositories/stats.repository.ts:229-324` |
| [analytics-sql] getTopModels / getApiKeyStats / getSessionStats | ❌ | Top models by usage % (CTE), per-api-key request+success stats, and per-account current-session token/cost rollups (plan vs api split). — None present; no model/api-key/session aggregation and no plan-vs-api cost split on the native path. | `packages/database/src/repositories/stats.repository.ts:329-508` |
| [analytics] Cache-savings cost math (pure) | ❌ | Per-model actual (cacheRead+cacheWrite rates) vs counterfactual (all at input rate) cost, savings, hit-rate, flagged low-cache rows, by account/model/project + totals with unknown-pricing model list. — No pricing engine and no cache-token capture; PolyFlare computes zero cost on the native path (COMPARISON.md: 'Pricing data + cost accounting' = ❌). | `packages/http-api/src/services/cache-insights.ts:96-326` |
| [analytics] Anomaly baselines + token/output z-score outliers | ❌ | Population mean/stddev of tokens per (account,model), flags requests ≥ zThreshold stddevs above baseline for total & output tokens. — No anomaly math; no per-request token rows to baseline. | `packages/http-api/src/services/anomaly-insights.ts:144-238` |
| [analytics] Runaway-loop burst detector | ❌ | Two-pointer forward/backward-maximal windows over (account,model,project) finding dense bursts of similar requests (coefficient-of-variation tolerance) merged into runs, requests/min reported. — No loop/burst detection. | `packages/http-api/src/services/anomaly-insights.ts:265-380` |
| [analytics] Model-misrouting detector | ❌ | Flags expensive models (output rate ≥ threshold $/1M) repeatedly used for trivially small calls (≤500 tokens), sorted by wasted cost. — No misrouting detection; needs per-request tokens + model pricing PolyFlare lacks. | `packages/http-api/src/services/anomaly-insights.ts:396-450` |
| [analytics] Context composition + contributors + growth curve | ❌ | Char→token composition of system/tools/messages from payloads, hash-grouped top repeated content contributors, per-project session-split context-growth curve from exact token columns. — No context analysis; requires stored payloads (never persisted) + per-request token columns (not captured natively). | `packages/http-api/src/services/context-insights.ts:261-539` |
| [usage-prediction] Least-squares exhaustion ETA | ❌ | Fits centered/hour-scaled utilization slope, segments on reset/refund drops (≥5pp) & resets_at changes, projects predictedAtReset, willExhaustBeforeReset, etaExhaustMs, low-confidence flag, states rising/stable/exhausted/insufficient. — No depletion/pace forecasting (COMPARISON.md 'Depletion/pace/burn-rate forecasting' = ❌). PolyFlare stores the usage_history series that would feed it but computes no slope/ETA. | `packages/http-api/src/services/usage-prediction.ts:25-149` |
| [usage-prediction] Exhaustion predicate + rate-limit-status precedence | 🟡 | isUsageExhausted (util≥100 & reset in future) plus a display precedence (usage-exhausted > unified-header snapshot > cooldown lock > OK) with minutes-left, provider-aware reset extraction. — PolyFlare's read_api resolves used_percent + reset_at + a stale flag per window and the UI tones a bar 'full' at ≥100%, but there is no shared exhaustion predicate feeding routing/health and no unified-header snapshot precedence. | `packages/http-api/src/handlers/rate-limit-status.ts:59-96,107-162` |
| [db-schema] requests table — token/cost/attribution columns | 🟡 | ~30 cols: prompt/completion/total/input/output/cache-read/cache-creation tokens, cost_usd, output_tokens_per_second, model, agent_used, project, billing_type, original/applied_model, api_key_id/name, combo_name, failover_attempts, error_message. — PolyFlare request_log (0004+0005) carries a CONTENT-SAFE subset: native path writes only 7 cols; the widened token/cost/session/service-tier cols exist but are importer-filled/NULL natively, and error_message/useragent/client_ip are deliberately excluded. | `packages/database/src/migrations.ts:131-158,825-946` |
| [db-schema] request_payloads table | ❌ | Full request+response JSON per request (optionally encrypted at rest), FK CASCADE to requests, timestamp-indexed for age-based cleanup — backs the conversation viewer. — No payload table. PolyFlare stores zero conversation content by design (the one permanent limit vs codex-lb/ccflare), so no conversation archive is possible. | `packages/database/src/migrations.ts:201-221` |
| [db-schema] usage_snapshots time-series table | ✅ | Append-only per-account (window_key) utilization 0-100 + resets_at captured each usage poll, dual-indexed (acct,window,time DESC) + (time) for retention pruning. — PolyFlare has the richer equivalent: usage_history (account_id, recorded_at, window, used_percent, input/output tokens, reset_at, window_minutes, credits_*), written every usage refresh; indexed (account_id, recorded_at). | `packages/database/src/migrations.ts:360-377; polyflare migrations/0001` |
| [db-schema] alerts table | ❌ | Persisted threshold/anomaly alert history (type, severity, title, message, value, threshold, account/model/project, request_id, acknowledged), time+ack indexed. — No alerts table and no alerting engine. | `packages/database/src/migrations.ts:177-198` |
| [db-schema] api_keys table + role column | ❌ | Hashed proxy API keys (hashed_key, prefix_last_8, usage_count, is_active) with a role column (admin/api-only) for authz, indexed by hash/active/role. — No api_keys table; no proxy-key auth model. | `packages/database/src/migrations.ts:253-273,974-991` |
| [db-schema] Performance indexes for request analytics | 🟡 | idx_requests_timestamp (DESC), idx_requests_account_used, composite idx_requests_timestamp_account for the join-heavy summary query; performance-indexes.ts adds more. — PolyFlare indexes request_log for its actual queries: (requested_at DESC, id DESC), (provider, requested_at), (account_id, requested_at) — appropriate for its content-safe subset, but far fewer than ccflare's analytics-tuned set. | `packages/database/src/migrations.ts:161-173; database/src/performance-indexes.ts; polyflare migrations/0004-0005` |
| [db] Async writer (fire-and-forget) + health telemetry | 🟡 | Non-blocking DB write queue with health (healthy flag, queued/dropped jobs, oldest-age, payload bytes pending) surfaced in /health runtime. — PolyFlare writes request_log fire-and-forget at completion (observability.rs record → repo.insert), but there is no queue-health struct, no drop accounting, and nothing exposes writer health. | `packages/database/src/async-writer.ts; handlers/health.ts:42-55` |
| [db] Vacuum + incremental-vacuum workers | ❌ | Background SQLite VACUUM / incremental auto-vacuum workers reclaiming space, adaptive scheduling, orphan-page accounting. — No vacuum tooling; PolyFlare relies on sqlx/WAL defaults with no space-reclaim worker. | `packages/database/src/{vacuum-worker,incremental-vacuum-worker}.ts` |
| [db] Integrity-check worker + scheduler | ❌ | Background quick/full PRAGMA integrity_check + foreign_key_check with tracked status feeding /health and /api/storage. — No integrity-check worker or status tracking. | `packages/database/src/{integrity-check-worker,integrity-check-runner}.ts` |
| [db] Retention / cleanup of old requests | ❌ | Retention sweep deleting aged requests + payloads (config-driven window), tracked via last_retention_sweep_at. — COMPARISON.md: PolyFlare's anchors/sessions/usage/request_log 'all grow unbounded'; deleted_at column exists but is importer-set only — no pruning job. | `packages/database/src/__tests__/cleanup-old-requests.test.ts; migrations.ts:33-36 (last_retention_sweep_at)` |
| [db] Dual SQLite + PostgreSQL migrations | ❌ | Every schema change ported to both migrations.ts (SQLite) and migrations-pg.ts (Postgres) with a bun-sql-adapter abstraction; Postgres is a first-class storage option. — PolyFlare is SQLite/WAL only (single-binary target). No Postgres option and no dual-dialect migration discipline. | `packages/database/src/migrations-pg.ts; adapters/bun-sql-adapter.ts` |
| [db] Payload encryption at rest | ❌ | Optional XChaCha/payload encryption of stored request_payloads JSON. — N/A — PolyFlare stores no payloads to encrypt. (It DOES encrypt account tokens at rest with XChaCha20-Poly1305, which ccflare stores plaintext — but that's token crypto, not payload crypto.) | `packages/database/src/payload-encryption.ts` |
| [db] migrate-from-ccflare importer | 🟡 | One-shot importer that migrates a legacy ccflare DB into better-ccflare's schema. — Different lineage: PolyFlare has its OWN importer (codex-lb Fernet→XChaCha) carrying ~148k usage rows + accounts with zero re-auth into request_log/usage_history — a peer capability, not this exact one. | `packages/database/src/migrate-from-ccflare.ts; polyflare-store/src/import.rs` |
| [dashboard] AnalyticsTab — range + filters + URL state | ❌ | Analytics view with range picker (1h/6h/24h/7d/30d), cumulative toggle, model-breakdown, account/model/apiKey/status filters, URL-synced state, dedup fetch. — No analytics tab. PolyFlare's SPA is a single page (Pools + Accounts + Recent requests) with no range picker, filters, or analytics view. | `packages/dashboard-web/src/components/AnalyticsTab.tsx; hooks/{useAnalyticsUrlState,useDeduplicatedAnalytics}.ts` |
| [dashboard] Chart primitive library (Recharts wrappers) | ❌ | Base{Area,Line,Bar,Pie,Scatter}Chart + ChartContainer + ChartTooltip + chart-utils shared across all analytics charts. — PolyFlare's dashboard ships no charting library at all — only HTML tables, usage bars, and countdown pills (no recharts/svg charts). | `packages/dashboard-web/src/components/charts/Base*.tsx, ChartContainer.tsx` |
| [dashboard] Request-volume + response-time charts | ❌ | RequestVolumeChart (requests over time) and ResponseTimeChart (latency over time) time-series visualizations. — No time-series charts of volume or latency. | `packages/dashboard-web/src/components/charts/{RequestVolumeChart,ResponseTimeChart}.tsx` |
| [dashboard] Cost chart with plan-vs-API split | ❌ | CostChart visualizing spend over time split by billing_type (plan vs api), plus account-cost table utils. — No cost visualization and no plan/api cost split (no cost captured natively). | `packages/dashboard-web/src/components/charts/CostChart.tsx; overview/account-cost-table-utils.ts` |
| [dashboard] Token usage + token-speed charts | ❌ | TokenUsageChart (input/output/cache breakdown) and TokenSpeedChart / ModelTokenSpeedChart (tokens/sec) visualizations. — No token or throughput charts. | `packages/dashboard-web/src/components/charts/{TokenUsageChart,TokenSpeedChart,ModelTokenSpeedChart}.tsx` |
| [dashboard] Model performance chart (p95 comparison) | ❌ | ModelPerformanceChart / ModelPerformanceComparison / MultiModelChart plotting per-model p95 latency, error rate, throughput. — No per-model performance charts and no p95 data to plot. | `packages/dashboard-web/src/components/charts/{ModelPerformanceChart,ModelPerformanceComparison,MultiModelChart}.tsx` |
| [dashboard] Conversation viewer | ❌ | ConversationView renders decoded request/response payloads as a readable conversation (messages, tool calls, tool results). — No conversation viewer — PolyFlare persists no conversation content, so there is nothing to render. Permanent, intentional limit. | `packages/dashboard-web/src/components/ConversationView.tsx` |
| [dashboard] Request details modal | ❌ | RequestDetailsModal showing one request's full metadata + payload, lazy-loading the payload by id. — No per-request detail modal; the request table shows only method/path/provider/status/latency with no drill-in. | `packages/dashboard-web/src/components/RequestDetailsModal.tsx` |
| [dashboard] Logs tab (live SSE consumer) | ❌ | LogsTab subscribing to /api/logs/stream for a live in-dashboard log console. — No logs tab and no log SSE to consume. | `packages/dashboard-web/src/components/LogsTab.tsx` |
| [dashboard] Requests tab with live stream | 🟡 | RequestsTab consuming /api/requests/stream (useRequestStream) for a live-updating request feed with batching. — PolyFlare HAS a 'Recent requests' table, but it 15s-polls /api/requests (no live SSE stream, no batching hook) and shows only the content-safe columns. | `packages/dashboard-web/src/components/RequestsTab.tsx; hooks/useRequestStream.ts; polyflare App.tsx:280-374` |
| [dashboard] Overview tab — pool metric cards + throttling card | 🟡 | OverviewTab with PoolMetricCard (per-pool utilization) and UsageThrottlingCard (proactive-throttle status). — PolyFlare renders PoolCards (name, active/total, pooled-path slug) but no per-pool utilization metric card and no usage-throttling card (no proactive throttling feature). | `packages/dashboard-web/src/components/overview/{PoolMetricCard,UsageThrottlingCard}.tsx; polyflare App.tsx:60-76` |
| [dashboard] Usage-history tab + chart + prediction overlay | ❌ | UsageHistoryTab + UsageHistoryChart plotting per-account window utilization over time with the prediction/ETA overlay. — No usage-history chart. PolyFlare shows only the current window as a bar + reset countdown per account — no historical trend or prediction line. | `packages/dashboard-web/src/components/usage-history/{UsageHistoryTab,UsageHistoryChart}.tsx` |
| [dashboard] Stats tab | ❌ | StatsTab surfacing the /api/stats rollup (totals, top models, per-account, recent errors). — No stats tab / rollup view. | `packages/dashboard-web/src/components/StatsTab.tsx` |
| [dashboard] Insights tab (cache/anomaly/context/alerts views) | ❌ | InsightsTab hosting CacheEfficiencyView, ContextCompositionView, AlertsView backed by the insights + alerts endpoints (alerts via useAlertStream SSE). — No insights tab and none of its sub-views; no insights/alerts endpoints or SSE to back them. | `packages/dashboard-web/src/components/insights/{CacheEfficiencyView,ContextCompositionView,AlertsView}.tsx; hooks/useAlertStream.ts` |
| [dashboard] Live-refresh SSE hooks | ❌ | useRequestStream + useAlertStream (EventSource subscriptions) and useRequestBatching for live dashboard updates without polling. — No EventSource/SSE hooks; PolyFlare's dashboard uses a single 15s setInterval poll of pools/accounts/requests plus a 1s clock tick for countdowns. | `packages/dashboard-web/src/hooks/{useRequestStream,useAlertStream,useRequestBatching}.ts; polyflare App.tsx:353-374` |
| [dashboard] Inline account settings edits | 🟡 | AccountsTab inline edits: rate-limit force-reset, pause/resume, priority, auto-fallback, billing-type, model-mappings, custom-endpoint, rename, remove. — PolyFlare has inline edits via PATCH /api/accounts/{id} but only for pool (assign/create/clear), routing_policy (normal/burn_first/preserve), and pause/resume — no priority/billing/model-mapping/endpoint/rename controls. | `packages/dashboard-web/src/components/accounts/*.tsx; polyflare write_api.rs:59-101, App.tsx:80-202` |
| [dashboard] Reset countdown pills + usage bars + theme toggle | ✅ | Live rate-limit reset countdowns, utilization progress bars (RateLimitProgress), and light/dark theme toggle with pre-paint init. — PolyFlare has all three: 1s-ticking reset countdown pills (with stale tagging + abs-time tooltip), toned usage bars (ok/high/full), and a light/dark theme toggle initialized from a pre-paint script. | `packages/dashboard-web/src/components/accounts/RateLimitProgress.tsx; theme-toggle.tsx; polyflare App.tsx:7-58,340-349` |

## oauth-accounts-config-security-ops  ·  ✅ 13 🟡 17 ❌ 62

| Feature | PF | PolyFlare status / where | Source |
|---|:--:|---|---|
| anthropic-oauth-authorize-url | ❌ | Builds the Anthropic OAuth authorize URL with PKCE challenge + random CSRF state, dual base (console.anthropic.com vs claude.ai). — PF has no Anthropic OAuth login flow at all; Anthropic accounts are imported and their stored access_token is used verbatim as the bearer (crates/polyflare-anthropic/src/executor.rs:46). | `packages/providers/src/providers/anthropic/oauth.ts:50` |
| anthropic-oauth-code-exchange | ❌ | Exchanges the Anthropic auth code (splits `code#state`) for access+refresh tokens via a JSON POST to the token endpoint. — No Anthropic code exchange in PF; only Codex has exchange_code (crates/polyflare-codex/src/oauth.rs:342). | `packages/providers/src/providers/anthropic/oauth.ts:67` |
| anthropic-dual-mode-oauth | ❌ | begin()/complete() OAuth flow supporting `console` (mint static API key) and `claude-oauth` (store refresh token) modes. — PF has no dual-mode Anthropic onboarding; no console/claude-oauth distinction exists. | `packages/oauth-flow/src/index.ts:134` |
| anthropic-console-apikey-mint | ❌ | `console` mode calls Anthropic's create_api_key endpoint with the temporary OAuth token to mint a permanent x-api-key. — Not present; PF never mints Anthropic API keys. | `packages/oauth-flow/src/index.ts:245` |
| anthropic-oauth-account-create | 🟡 | Inserts an anthropic account row storing refresh_token/access_token/expires_at/refresh_token_issued_at. — PF stores anthropic accounts (schema + executor exist) but only via the codex-lb importer or manual insert — there is no OAuth onboarding path that creates them. | `packages/oauth-flow/src/index.ts:278` |
| anthropic-reauth-in-place | ❌ | Re-authenticates an existing Anthropic account, UPDATE-ing tokens in place while preserving stats/priority/settings (init + callback handlers). — PF has no Anthropic re-auth; only Codex accounts can be re-authenticated (via `accounts login` upsert). | `packages/http-api/src/handlers/oauth.ts:649` |
| anthropic-token-refresh | ❌ | Refreshes an Anthropic access token from its refresh_token (refresh_token grant) via getValidAccessToken / the auto-refresh scheduler. — PF Anthropic path never refreshes — it bearer-auths with the stored access_token as-is, so Anthropic accounts silently expire (COMPARISON OAuth row). | `packages/providers/src/oauth/base-oauth-provider.ts:91` |
| codex-oauth-authorize-url | ✅ | Builds the Codex authorize URL with manual %20 encoding and codex_cli_rs originator + simplified-flow + id_token_add_organizations params. — PF has the equivalent build_authorize_url with the same OpenAI client_id/scopes/redirect (crates/polyflare-codex/src/oauth.rs:313). | `packages/providers/src/providers/codex/oauth.ts:36` |
| codex-oauth-code-exchange-pkce | ✅ | Exchanges the Codex authorization_code + PKCE verifier for access/refresh/id tokens. — PF exchange_code is at parity, also decoding id_token claims (crates/polyflare-codex/src/oauth.rs:342). | `packages/providers/src/providers/codex/oauth.ts:57` |
| codex-oauth-loopback-callback-1455 | ✅ | CLI spins up a local http server on 127.0.0.1:1455 to capture the OAuth redirect and extract the code. — PF's `accounts login` races a loopback :1455 listener AND a paste-the-URL fallback (headless-friendly, a PF superset), crates/polyflare-codex/src/login.rs:45. | `packages/cli-commands/src/commands/account.ts:895` |
| codex-device-code-flow | ❌ | RFC-8628 device-code auth (usercode request → poll → authorization_code exchange) mirroring codex-rs device_code_auth. — PF has no device-code flow; it covers the same headless gap with the paste-callback path in login.rs instead. | `packages/providers/src/providers/codex/device-oauth.ts:28` |
| codex-device-web-handlers | ❌ | HTTP init/status/reauth handlers that kick off a Codex device flow and poll for tokens in the background, keyed by an in-memory sessionId. — No web-driven Codex enrollment handlers in PF; onboarding is CLI-only. | `packages/http-api/src/handlers/oauth.ts:320` |
| codex-reauth | 🟡 | Re-runs the Codex device flow for an existing account and UPDATEs its tokens in place. — PF `accounts login` upserts by chatgpt_account_id: an existing account has its tokens refreshed in place and prints 're-authenticated' (crates/polyflare-server/src/main.rs:234-243) — but it's the authcode+PKCE path, not device flow. | `packages/http-api/src/handlers/oauth.ts:430` |
| pkce-generator | ✅ | S256 PKCE: 32-byte random verifier, SHA-256 challenge, base64url (no padding). — PF generate_pkce is equivalent (crates/polyflare-codex/src/oauth.rs:224). | `packages/providers/src/oauth/pkce.ts:37` |
| base-oauth-provider-abstraction | 🟡 | Reusable OAuth base class (generateAuthUrl / exchangeCodeForTokens / refreshTokens) with subclass parameter hooks. — PF has reusable free functions + an OAuthClient, but Codex-specific and with no generic multi-provider base class. | `packages/providers/src/oauth/base-oauth-provider.ts:20` |
| oauth-provider-registry | ❌ | getOAuthProvider(name) registry that dispatches to the right provider's OAuth implementation. — PF is single-OAuth-provider (Codex); no name-keyed OAuth provider registry. | `packages/providers/src/oauth/index.ts:1` |
| qwen-oauth-device-flow | ❌ | Qwen/DashScope OAuth device-code flow with its own PKCE and resource_url handling. — No Qwen support in PF. | `packages/providers/src/providers/qwen/device-oauth.ts:1` |
| multi-provider-onboarding | ❌ | addAccount supports ~17 account modes (claude-oauth, console, codex, qwen, xai, vertex-ai, bedrock, zai, minimax, kilo, openrouter, alibaba, anthropic-compatible, openai-compatible, ollama, ollama-cloud, nanogpt). — PF onboards only Codex (OAuth login) + Anthropic/Codex via import — 2 providers, no per-provider onboarding matrix. | `packages/cli-commands/src/commands/account.ts:1150` |
| xai-grok-credential-import | ❌ | Imports Grok CLI OAuth credentials from ~/.grok/auth.json (picks freshest entry) to create an xAI account. — PF imports only from a codex-lb DB, not from other CLIs' auth files. | `packages/cli-commands/src/commands/account.ts:755` |
| oauth-session-store | ❌ | DB-backed oauth_sessions table persisting {sessionId, accountName, verifier, mode, customEndpoint, priority} with a TTL for the begin→complete handoff. — PF login is a single in-process authcode+PKCE round-trip; no persisted OAuth session table (verifier is held in memory across the tokio::select race). | `packages/database/src/repositories/oauth.repository.ts:11` |
| oauth-session-cleanup | ❌ | Prunes expired oauth_sessions at startup and hourly via a registered cleanup job. — No OAuth session table in PF, so nothing to prune. | `apps/server/src/server.ts:751` |
| oauth-refresh-failure-classification | ✅ | Classifies token-refresh failures (permanent reauth-required vs transient) to drive account-health decisions. — PF classify_failure ports codex-lb's exact 12-code permanent set → reauth_required vs deactivated (crates/polyflare-codex/src/oauth.rs:105). | `packages/proxy/src/index.ts:17` |
| proactive-refresh-scheduler | 🟡 | AutoRefreshScheduler background loop that refreshes OAuth tokens before expiry (temporarily resuming paused accounts, exponential-backoff retries). — PF refreshes lazily on the request path (exp-driven) via ingress; there is no standalone background token-refresh scheduler (only usage-refresh + version loops are spawned). | `packages/proxy/src/auto-refresh-scheduler.ts:27` |
| refresh-singleflight | ✅ | Per-account in-flight refresh dedup (refreshInFlight map + registerRefreshClearer) so concurrent requests don't double-refresh a rotating refresh token. — PF RefreshLocks serializes concurrent same-account refreshes (crates/polyflare-server/src/refresh_locks.rs; ingress.rs:218). | `apps/server/src/server.ts:933` |
| exp-driven-refresh-timing | ✅ | Refresh decision keyed off token expiry (refresh shortly before expires_at). — PF should_refresh refreshes within REFRESH_MARGIN_SECS of the decoded token `exp`, with an age-gate fallback (crates/polyflare-codex/src/oauth.rs:89). | `packages/proxy/src/auto-refresh-scheduler.ts:199` |
| global-token-health-monitor | ❌ | Periodic refresh-token health checks across all accounts with formatted health reports/status. — PF has no standalone token-health monitor; token validity is only observed on the request path. | `packages/proxy/src/index.ts:31` |
| account-repo-crud | ✅ | Account repository: findAll/findById selecting the full account row, ordered by priority. — PF has a store-backed account repository (crates/polyflare-store/src/account.rs) with get/find_by_chatgpt_account_id/list. | `packages/database/src/repositories/account.repository.ts:10` |
| account-priority | 🟡 | Numeric per-account priority (lower = higher) with updatePriority + `--set-priority` CLI. — PF has no numeric priority; it models preference via routing_policy (normal\|burn_first\|preserve) + named pools instead. | `packages/database/src/repositories/account.repository.ts:215` |
| account-pause-resume | ✅ | paused flag + pause_reason with pause()/resume() and API reload/pause/resume endpoints. — PF PATCH /api/accounts/{id} sets status active\|paused (paused is a manual hold the refresh loop leaves alone), crates/polyflare-server/src/write_api.rs:57. | `packages/database/src/repositories/account.repository.ts:180` |
| account-rename | 🟡 | rename() updates an account's name. — PF accounts carry an `alias` column but no rename/alias-edit endpoint is wired. | `packages/database/src/repositories/account.repository.ts:208` |
| per-account-custom-endpoint | ❌ | custom_endpoint column lets each account override its upstream base URL (also encodes bedrock/vertex config). — PF upstream base URLs are global env (POLYFLARE_UPSTREAM_URL / _ANTHROPIC_ / _AUTH_URL); no per-account endpoint override. | `packages/database/src/repositories/account.repository.ts:23` |
| per-account-model-mappings | 🟡 | model_mappings/model_fallbacks JSON per account (opus/sonnet/haiku → concrete/cycling model lists). — PF has a single global synthetic_models alias map (tier→model+effort), not per-account mappings. | `packages/database/src/repositories/account.repository.ts:24` |
| account-removal | ❌ | `--remove <name>` CLI with an interactive removal-confirmation prompt. — PF CLI only imports/logs-in/sets-pool; there is no account-removal command or DELETE endpoint. | `packages/cli-commands/src/commands/account.ts:1150` |
| inline-dashboard-account-edit | ✅ | PATCH account settings (pool/policy/pause) from the dashboard. — PF PATCH /api/accounts/{id} updates pool / routing_policy / status (crates/polyflare-server/src/write_api.rs:59). | `packages/http-api/src/handlers/accounts.ts:1` |
| legacy-db-migration | 🟡 | One-shot migration of a legacy ~/.ccflare database into the better-ccflare store. — PF's analogue is the zero-re-auth codex-lb importer (`accounts import`, Fernet→XChaCha), a different source but the same 'adopt an existing pool' role (crates/polyflare-store/src/import.rs). | `packages/database/src/migrate-from-ccflare.ts:1` |
| consecutive-rate-limit-tracking | 🟡 | Per-account consecutive_rate_limits counter (increment on 429, reset on success) for backoff decisions. — PF's select.rs reads error_count/cooldown_until but the write side is inert (nothing increments them on upstream failure) — the Phase A gap in docs/PORTING-CODEXLB.md. | `packages/database/src/repositories/account.repository.ts:125` |
| tokens-plaintext-at-rest | ✅ | OAuth access/refresh tokens and API keys are stored in the accounts table in PLAINTEXT (better-ccflare's headline security gap). — PF closes this gap: the three token columns are XChaCha20-Poly1305 ciphertext (24-byte nonce prepended) with a chmod-0600 key file and ZeroizeOnDrop (crates/polyflare-store/src/crypto.rs; migration 0001). | `packages/oauth-flow/src/index.ts:287` |
| api-key-scrypt-hashing | ❌ | Proxy API keys are stored as salted scrypt hashes (salt:hash) verified with a timing-safe compare; raw key never persisted. — PF has no proxy/admin API-key system, so no key hashing exists. | `packages/types/src/api-key.ts:97` |
| payload-at-rest-encryption | ❌ | Optional AES-256-GCM encryption of stored request/response payloads (enc: prefix, random 12-byte IV, opt-in via PAYLOAD_ENCRYPTION_KEY, plaintext passthrough when unset). — PF persists no conversation payloads by design (its permanent limitation), so there is nothing to encrypt at that layer. | `packages/database/src/payload-encryption.ts:98` |
| refresh-token-issued-at | 🟡 | Tracks refresh_token_issued_at per account to reason about token age. — PF tracks a single last_refresh epoch on the account (migration 0001), used by should_refresh's age-gate fallback. | `packages/database/src/repositories/account.repository.ts:29` |
| json-config-file | ❌ | Loads/saves a JSON config at ~/.config/better-ccflare/config.json, auto-creating it with defaults on first run. — PF is env-only (ServeConfig::from_env) — no config file is read or written. | `packages/config/src/index.ts:190` |
| config-change-events | ❌ | Config extends EventEmitter and emits a `change` event on every set(), enabling live reconfiguration. — PF config is immutable after startup; no change-event bus. | `packages/config/src/index.ts:237` |
| strategy-hot-reload | ❌ | On a config `change` for lb_strategy the server rebuilds the load-balancing strategy live without restart. — PF selects routing strategy once at startup from POLYFLARE_ROUTING_STRATEGY / POLYFLARE_POOL_STRATEGY; no hot reload. | `apps/server/src/server.ts:1094` |
| config-precedence | 🟡 | Layered resolution: environment variable > config-file value > coded default, per setting. — PF resolves env > default only; there is no file layer (crates/polyflare-server/src/config.rs:67). | `packages/config/src/index.ts:246` |
| config-path-security | 🟡 | Config file path is run through a traversal/symlink/null-byte-hardened path validator before use. — PF validates the key file's length/existence but has no general path-traversal validator for its data paths. | `packages/config/src/index.ts:184` |
| retention-config-keys | ❌ | Configurable, clamped retention windows: data_retention_days, request_retention_days, usage_history_retention_days (env + file). — PF has no retention knobs because it has no retention/pruning; request_log/usage grow unbounded. | `packages/config/src/index.ts:294` |
| db-tuning-config | 🟡 | Validated SQLite tuning knobs: WAL mode, cache_size, mmap_size, busy_timeout, page_size, and a retry sub-config. — PF uses sqlx with WAL but exposes no env-tunable DB pragmas. | `packages/config/src/index.ts:92` |
| alert-config | ❌ | Alert settings: webhook URL (validated http/https), daily-spend/tokens-per-hour/request-token thresholds, anomaly toggle+interval, cooldown. — PF has no alerting subsystem or webhook config. | `packages/config/src/index.ts:585` |
| feature-flag-config | ❌ | Behavioral flags: usage-throttling (5h/weekly), cache-keepalive TTL, system-prompt 1h cache TTL, model-catalog OAuth-refresh, health-detail. — PF exposes none of these flags; the few env toggles it has are watchdog secs + routing strategy. | `packages/config/src/index.ts:407` |
| tls-serving | ❌ | Serves the API + dashboard over HTTPS when SSL_KEY_PATH & SSL_CERT_PATH are both set (Bun serve tls: {key,cert}). — PF serves plain HTTP via axum::serve (crates/polyflare-server/src/main.rs:166); its rustls stack is egress-only (upstream JA3 fingerprint), not for inbound TLS termination. | `apps/server/src/server.ts:571` |
| ssl-path-validation | ❌ | Validates SSL key/cert paths (traversal-safe) and existence before binding, redacting paths from error messages. — N/A — PF terminates no inbound TLS. | `apps/server/src/server.ts:580` |
| protocol-switch-reporting | ❌ | getProtocol() + startup banner reflect http vs https based on whether TLS is enabled. — PF is always http for its listener; no protocol switch. | `apps/server/src/server.ts:1738` |
| host-binding-config | ✅ | Configurable listener host via BETTER_CCFLARE_HOST (defaults 0.0.0.0) plus --port / PORT. — PF binds POLYFLARE_BIND (default 127.0.0.1:8080), crates/polyflare-server/src/config.rs:68. | `apps/server/src/server.ts:1111` |
| dashboard-security-headers | ❌ | Dashboard responses carry CSP, X-Frame-Options: DENY, X-Content-Type-Options: nosniff, Referrer-Policy, X-XSS-Protection. — PF's embedded dashboard handler serves assets without these hardening headers (crates/polyflare-server/src/dashboard.rs). | `apps/server/src/server.ts:154` |
| api-key-auth | ❌ | Authenticates proxy + management requests via x-api-key or Authorization: Bearer against active keys. — PF leaves ALL endpoints (ingress, /api/*, dashboard) unauthenticated, relying only on the network boundary; write_api.rs is explicitly marked UNAUTHENTICATED. Planned POLYFLARE_ADMIN_TOKEN. | `packages/http-api/src/services/auth-service.ts:201` |
| auth-auto-enable | ❌ | Authentication is disabled until at least one active API key exists (countActiveApiKeys > 0), then enforced everywhere. — No auth toggle in PF. | `packages/http-api/src/services/auth-service.ts:30` |
| rbac-roles | ❌ | Two roles — admin (full) vs api-only (proxy endpoints only) — enforced per request. — PF has no roles or RBAC. | `packages/http-api/src/services/auth-service.ts:93` |
| endpoint-authorization | ❌ | authorizeEndpoint() scopes api-only keys to /v1/* & /messages/* and gates /api/debug/* to admin keys (heap snapshots contain secrets). — No per-endpoint authorization in PF. | `packages/http-api/src/services/auth-service.ts:93` |
| auth-path-exemptions | ❌ | Static exemptions (health, /api/oauth*, /api/version/check, dashboard static/index) bypass auth so setup + SPA load work. — N/A — every PF path is already open. | `packages/http-api/src/services/auth-service.ts:133` |
| api-key-extraction | ❌ | Extracts the key from x-api-key or a case-insensitive `Authorization: Bearer` header. — PF reads no inbound API key. | `packages/http-api/src/services/extract-api-key.ts:5` |
| api-key-mgmt-endpoints | ❌ | CRUD for proxy keys: list, generate (name+role), get, enable, disable, delete, stats, update-role. — No key-management surface in PF. | `packages/http-api/src/handlers/api-keys.ts:15` |
| api-key-last8-prefilter | ❌ | Cheap prefix_last_8 pre-filter short-circuits the expensive scrypt verify for non-matching keys. — N/A in PF. | `packages/http-api/src/services/auth-service.ts:57` |
| api-key-usage-tracking | ❌ | Tracks last_used + increments usage_count on each successful key auth. — N/A in PF. | `packages/database/src/repositories/api-key.repository.ts:131` |
| bootstrap-key-no-auth | ❌ | POST /api/api-keys is allowed without auth only when no keys yet exist (first-key bootstrap); all other key ops require auth. — N/A in PF. | `packages/http-api/src/services/auth-service.ts:176` |
| agent-discovery | ❌ | Discovers Claude Code sub-agents by parsing markdown frontmatter from global, workspace, and plugin dirs (with symlink/id dedup, 30s cache). — PF has no agent registry; agent traffic is treated as ordinary requests. | `packages/agents/src/discovery.ts:242` |
| agent-model-preference | ❌ | Resolves each agent's model (opus/sonnet/haiku/fable/inherit/exact) from frontmatter, defaulting to inherit. — No agent-level model resolution in PF. | `packages/agents/src/discovery.ts:190` |
| agent-workspace-persistence | ❌ | Registers workspaces and persists them to ~/.better-ccflare/workspaces.json (with legacy ~/.ccflare migration). — No workspace concept in PF. | `packages/agents/src/workspace-persistence.ts:98` |
| agent-workspace-prune | ❌ | Prunes workspaces not seen in 7 days (run at startup). — N/A in PF. | `packages/agents/src/discovery.ts:542` |
| agent-edit | ❌ | updateAgent rewrites an agent's markdown frontmatter (description/model/tools/color) and clears any stale DB model preference. — N/A in PF. | `packages/agents/src/discovery.ts:566` |
| plugin-agent-discovery | ❌ | Feature-gated (env-flagged) discovery of plugin-provided agents via a parsed plugin manifest with path-traversal validation. — N/A in PF. | `packages/agents/src/discovery.ts:356` |
| cli-account-lifecycle | 🟡 | CLI account ops: --add-account (per-mode), --list, --remove, --reauthenticate, --set-priority. — PF CLI covers `accounts import`, `accounts login` (onboard/re-auth), and `accounts set-pool` — no list/remove/priority (crates/polyflare-server/src/main.rs:40-107). | `packages/cli-commands/src/runner.ts:1` |
| cli-maintenance | ❌ | Maintenance CLI: --reset-stats, --clear-history, --stats, --analyze. — PF has no maintenance CLI subcommands. | `packages/cli-commands/src/commands/stats.ts:1` |
| cli-db-doctor-repair | ❌ | database-doctor (diagnostics) and database-repair CLI commands for SQLite health. — No DB doctor/repair tooling in PF. | `packages/cli-commands/src/commands/database-doctor.ts:1` |
| cli-token-health | ❌ | token-health CLI command reporting each account's refresh-token health. — No token-health command in PF. | `packages/cli-commands/src/commands/token-health.ts:1` |
| cli-api-key-mgmt | ❌ | api-key CLI command to create/list/manage proxy keys from the terminal. — N/A — PF has no API-key system. | `packages/cli-commands/src/commands/api-key.ts:1` |
| browser-auto-open | ✅ | Opens the OAuth authorize URL in the default browser during CLI onboarding. — PF opens the URL only under `--open` (best-effort per-OS spawn), otherwise prints it for headless use (crates/polyflare-codex/src/login.rs:173). | `packages/cli-commands/src/utils/browser.ts:1` |
| dockerfile-multiarch | ❌ | Dockerfile that pulls a prebuilt release binary for linux/amd64 or linux/arm64 (buildx TARGETARCH), with an exec sanity check. — PF ships no Dockerfile; it's a single native tokio binary built from source. | `Dockerfile:1` |
| docker-nonroot-user-volume | ❌ | Runs as a non-root `ccflare` user (uid 1000) with a persistent /data volume and constrained perms. — No container packaging in PF. | `Dockerfile:60` |
| docker-healthcheck | ❌ | Container HEALTHCHECK curls /health on an interval. — PF exposes no /health route (routes: /responses, /v1/messages, pooled, /models, /api/*, /dashboard) — nothing to health-check. | `Dockerfile:66` |
| docker-compose | ❌ | docker-compose.yml with restart policy, CPU/memory limits+reservations, env_file, healthcheck, and a named data volume. — No compose file in PF. | `docker-compose.yml:1` |
| env-example-template | ❌ | .env.example documenting PORT, SSL paths, LB_STRATEGY, log level/format, retention days, and PAYLOAD_ENCRYPTION_KEY. — PF ships no .env template; env vars are documented in-code/docs only. | `.env.example:1` |
| graceful-shutdown | 🟡 | SIGTERM/SIGINT handler that stops scheduler triggers, drains the usage collector + async writer, dedupes concurrent signals, and bounds shutdown with a 30s watchdog. — PF relies on axum/tokio default shutdown; there's no explicit drain-and-watchdog shutdown sequence. | `apps/server/src/server.ts:1628` |
| memory-monitor | ❌ | Logs RSS/heap every 60s and warns at +512MB / errors at +1GB growth vs baseline (leak detection). — No process-memory monitor in PF. | `apps/server/src/server.ts:1323` |
| deploy-env-config | 🟡 | Deployment env config: BETTER_CCFLARE_DB_PATH, XDG_CONFIG_HOME, BETTER_CCFLARE_LOG_DIR, PORT (NODE_ENV=production). — PF has env-driven data paths (POLYFLARE_DATA_DIR → store.db + key, POLYFLARE_BIND) but no container/prod-env packaging (crates/polyflare-server/src/config.rs:113). | `Dockerfile:40` |
| request-payload-retention | ❌ | Dual-window cleanupOldRequests prunes request metadata and (separately, shorter-window) large stored payloads. — PF's request_log grows unbounded; no pruning job exists (COMPARISON Retention row). | `apps/server/src/server.ts:792` |
| usage-history-pruning | ❌ | Prunes usage snapshots older than the configured usage-history retention window. — PF usage_history is never pruned. | `apps/server/src/server.ts:832` |
| retention-cleanup-job | ❌ | Hourly registered cleanup job driving retention deletes + adaptive vacuum. — PF has no periodic retention job. | `apps/server/src/server.ts:847` |
| incremental-vacuum | ❌ | Migrates the DB to auto_vacuum=INCREMENTAL and reclaims free pages adaptively via an off-thread worker (bounded per-tick, drains surplus after a retention drop). — PF does no VACUUM management (sqlx-managed SQLite). | `packages/database/src/incremental-vacuum-worker.ts:1` |
| wal-checkpoint-job | ❌ | Runs PRAGMA optimize + wal_checkpoint(PASSIVE) every 5 minutes to bound WAL growth. — PF relies on sqlx/SQLite defaults; no periodic checkpoint job. | `apps/server/src/server.ts:857` |
| integrity-scheduler | ❌ | Periodic quick_check (few-hourly) + full integrity_check/foreign_key_check (daily, in a worker), surfaced via /api/storage. — No integrity scheduler in PF. | `apps/server/src/server.ts:681` |
| ratelimit-state-cleanup | 🟡 | Clears expired rate_limited_until across accounts at startup and hourly (plus force-reset on capacity restore). — PF's usage-refresh loop reconciles reset_at/status live (crates/polyflare-server/src/usage_refresh.rs) but there is no dedicated expired-rate-limit sweeper. | `apps/server/src/server.ts:770` |
| startup-maintenance | ❌ | One-shot boot maintenance: retention cleanup, OAuth-session cleanup, expired-rate-limit clear, agent-workspace prune, plus a one-time auto_vacuum=INCREMENTAL bootstrap migration before the listener binds. — PF has no startup-maintenance pass; it opens the store, warms the version cache, spawns usage-refresh, and serves. | `apps/server/src/server.ts:236` |
